/*
 * Runtime origin config for the landing site. The container entrypoint replaces
 * the marker with a JSON-encoded object after validating both public origins.
 */
// prettier-ignore
window.__BT_LANDING__ = __BT_LANDING_CONFIG__;

name: bettertrack

# Production Docker Compose topology — PROJECTPLAN.md §4.6.
# All images are built from the repo root (context: ..).
# Copy infra/.env.production.example → infra/.env and fill in values before running.
#
# First-run flow:
#   docker compose build
#   docker compose run --rm api node dist/scripts/migrate.js
#   docker compose run --rm api node dist/scripts/seed.js
#   docker compose up -d

services:
  # Front proxy: serves the user + admin SPAs and reverse-proxies the api origin.
  # One image, both topologies — BT_MODE selects the nginx layout at start (§4.6).
  web:
    build:
      context: ..
      dockerfile: apps/web/Dockerfile
    restart: unless-stopped
    environment:
      # Topology (§11) — the same scheme the api derives its origins from.
      BT_MODE: '${BT_MODE:-subdomains}'
      BT_DOMAIN: '${BT_DOMAIN:-localhost}'
      BT_TLS: '${BT_TLS:-}'
      BT_SUB_API: '${BT_SUB_API:-api}'
      BT_SUB_WEB: '${BT_SUB_WEB:-web}'
      BT_SUB_ADMIN: '${BT_SUB_ADMIN:-admin}'
      BT_PORT_API: '${BT_PORT_API:-3000}'
      BT_PORT_WEB: '${BT_PORT_WEB:-8080}'
      BT_PORT_ADMIN: '${BT_PORT_ADMIN:-8081}'
      BT_API_ORIGIN: '${BT_API_ORIGIN:-}'
      API_UPSTREAM: 'api:3000'
    # Publish the front port (subdomains, TLS terminated ahead of it) AND the
    # three service ports (ports mode). Ports the active mode doesn't listen on
    # simply have nothing behind them — trim this list to your mode when locking
    # a host down.
    ports:
      - '${BT_HTTP_PORT:-80}:80'
      - '${BT_PORT_WEB:-8080}:${BT_PORT_WEB:-8080}'
      - '${BT_PORT_ADMIN:-8081}:${BT_PORT_ADMIN:-8081}'
      - '${BT_PORT_API:-3000}:${BT_PORT_API:-3000}'
    depends_on:
      api:
        condition: service_healthy

  api:
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: '3000'
      DATABASE_URL: '${DATABASE_URL}'
      REDIS_URL: '${REDIS_URL}'
      SESSION_SECRET: '${SESSION_SECRET}'
      # Topology (§11): the api derives api/web/admin origins → CORS + cookies.
      BT_MODE: '${BT_MODE:-subdomains}'
      BT_DOMAIN: '${BT_DOMAIN:-localhost}'
      BT_TLS: '${BT_TLS:-}'
      BT_SUB_API: '${BT_SUB_API:-api}'
      BT_SUB_WEB: '${BT_SUB_WEB:-web}'
      BT_SUB_ADMIN: '${BT_SUB_ADMIN:-admin}'
      BT_PORT_API: '${BT_PORT_API:-3000}'
      BT_PORT_WEB: '${BT_PORT_WEB:-8080}'
      BT_PORT_ADMIN: '${BT_PORT_ADMIN:-8081}'
      BT_API_ORIGIN: '${BT_API_ORIGIN:-}'
      BT_WEB_ORIGIN: '${BT_WEB_ORIGIN:-}'
      BT_ADMIN_ORIGIN: '${BT_ADMIN_ORIGIN:-}'
      SMTP_HOST: '${SMTP_HOST:-}'
      SMTP_PORT: '${SMTP_PORT:-}'
      SMTP_USER: '${SMTP_USER:-}'
      SMTP_PASS: '${SMTP_PASS:-}'
      SMTP_FROM: '${SMTP_FROM:-}'
      ADMIN_EMAIL: '${ADMIN_EMAIL:-}'
      ADMIN_PASSWORD: '${ADMIN_PASSWORD:-}'
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ['CMD-SHELL', 'wget -qO- http://localhost:3000/api/v1/health || exit 1']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # Worker (BullMQ processors) — placeholder for Phase 1+.
  # Uncomment when apps/api/src/worker.ts exists and add it to tsup entry.
  #
  # worker:
  #   build:
  #     context: ..
  #     dockerfile: apps/api/Dockerfile
  #   command: ["node", "dist/worker.js"]
  #   restart: unless-stopped
  #   environment:
  #     NODE_ENV: production
  #     DATABASE_URL: "${DATABASE_URL}"
  #     REDIS_URL: "${REDIS_URL}"
  #     SESSION_SECRET: "${SESSION_SECRET}"
  #     BT_MODE: "${BT_MODE:-subdomains}"
  #     BT_DOMAIN: "${BT_DOMAIN:-localhost}"
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy

  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: '${POSTGRES_USER:-bt}'
      POSTGRES_PASSWORD: '${POSTGRES_PASSWORD}'
      POSTGRES_DB: '${POSTGRES_DB:-bettertrack}'
    volumes:
      - pgdata:/var/lib/postgresql/data
      - pgbackups:/backups
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-bt} -d ${POSTGRES_DB:-bettertrack}']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    restart: unless-stopped
    command: ['redis-server', '--appendonly', 'yes']
    volumes:
      - redisdata:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
  redisdata:
  pgbackups:

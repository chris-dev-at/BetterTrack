# Include only when the browser-facing origin is HTTPS. The nginx hop may still
# be HTTP when a trusted edge terminates TLS in front of it.
add_header Strict-Transport-Security "max-age=31536000" always;

# BetterTrack — Project Plan (v2)

| | |
|---|---|
| **Status** | Approved blueprint v2 — supersedes plan v1 per owner issue #79 |
| **Last updated** | 2026-07-01 |
| **Owner** | chris-dev-at |
| **Repo** | https://github.com/chris-dev-at/BetterTrack |

This document is the single source of truth for what BetterTrack is and how it is built. Sections 1–13 describe the **V1 scope** ("BetterTrack Core + Tiny Friend Sharing", per the owner's V1 definition in issue #79). Everything beyond that lives in **Section 14 — Future Features**, deliberately kept rich so features can be picked up later without re-designing. Where this v2 plan changes something the codebase already implements, the milestone phases in §13 schedule the rework.

---

## 1. Vision & Goals

BetterTrack is a self-hosted **personal finance and investing workspace**: users track multiple portfolios, browse assets, build **Conglomerates** (ETF-style weighted baskets), backtest strategies, turn budgets into buy lists, share portfolios and ideas with friends, and — later — connect their own tools through an API.

The app is organized around **five main navigation areas**, final from V1 onward:

**Portfolio | Workboard | Assets | Social | Profile Menu**

**Core promises:**

1. **Track** — record real buys/sells; see value, profit/loss, top winners/losers and history of everything you own, market-listed or not (your car, your house, an unlisted stock). Multiple portfolios (private, firm, shared) are the design target; V1 ships one default portfolio on a multi-portfolio data model.
2. **Play** — the Workboard is a financial playground: watchlists, Conglomerates with percentage weights, backtests, and a budget calculator that turns "1000 €" into a concrete buy list (whole-share or fractional) that never overshoots.
3. **Browse** — the Assets section is a full asset browser with a **local search index**: searching "bayer" hits BetterTrack's own catalog, not a live provider round-trip. Asset pages chart history and offer quick actions into the rest of the app.
4. **Share** — add friends inside BetterTrack; share a portfolio read-only with friends (V1); later: shared/editable portfolios, ideas, public profiles and links.
5. **Private & polite** — closed by default (admin decides the registration mode); the server does almost nothing while nobody interacts; upstream data providers are never spammed — everything is cached and coalesced.

**Non-goals (V1):** not a broker (no order execution), no investment advice, no mobile app / Firebase, no bank APIs, no public social network (feed/comments/likes/chat), no OAuth app marketplace, no second-by-second Live Mode streaming, no Google Drive backups.

**Success criteria for V1:** an invited user logs in, finds BAYN.DE instantly via local search, watches it, builds a 13/40/47 Conglomerate, backtests it, turns 1000 € into a buy list, records those buys next to their house in their portfolio, flips "share with friends" on, and their friend sees the portfolio read-only under Shared With Me — with a bell notification and a logged email on the way. The admin manages accounts and global app settings from a separate admin area and has **no** portfolio of their own. All five tabs exist; not-yet-built surfaces say "Coming soon" instead of not existing.

---

## 2. Glossary

| Term | Meaning |
|---|---|
| **Asset** | Anything trackable with a price/value over time. Market assets come from a data provider; **custom assets** (house, car, unlisted stock) get their values from the user. One unified concept — charts, portfolio math and backtests treat them identically. |
| **Asset catalog** | BetterTrack's own Postgres-backed index of known assets, powering **local search** (§6.2). Providers fill it; users search it. |
| **Provider** | A pluggable source of asset data implementing the `AssetProvider` interface. V1: `yahoo` (market data) and `manual` (custom assets). |
| **Quote** | Latest known price of an asset + day change, currency, timestamp. |
| **Portfolio** | A record of real ownership, derived from transactions. A user can have several (V1: one default; schema is multi-portfolio). Has a **visibility**: `private` or `friends` (read-only). |
| **Transaction** | One buy or sell: asset, quantity, price, fee, timestamp. |
| **Custom investment** | A user-created asset with manually entered **value points**; values carry forward between points. |
| **Workboard** | The playground tab: watchlist, Conglomerates, backtests, calculators; later comparisons and saved ideas. Watching ≠ owning. |
| **Conglomerate** | A named basket of assets with percent weights summing to 100 — the user's "own ETF". |
| **Position** | One entry of a Conglomerate: asset + weight percent. |
| **Backtest** | Historical performance of a Conglomerate, computed from daily closes as a weighted buy-and-hold index. |
| **Invest Calculator** | Turns (Conglomerate + budget) into a concrete buy list, whole-share or fractional, never exceeding budget. |
| **Friend** | A mutual, accepted connection between two users. Created via friend request (by username or email); either side can remove it. |
| **Live Mode** | An asset-chart mode showing short real-time windows (1 m … 12 h) fed by **one shared stream per asset** regardless of viewer count. V1: button exists, marked Coming soon. |
| **Alert** | A rule on an asset (price above/below, ±X%) that fires a notification. Designed (schema exists), **post-v1**. |
| **Notification channel** | Pluggable delivery: in-app + email (V1); phone push / Telegram / Discord later. |
| **Email log** | Per-user record of every email the system sent (or failed to send) for that user; visible to the admin. |
| **Registration mode** | Global admin setting for how accounts come to exist: closed · invite/token · approval · open (§6.12). |
| **Account kind** | `user` (uses the app) or `admin` (management-only; no portfolio, no user features — §3). |
| **Coming Soon page** | A real routed page/section rendered as a designed placeholder, so the final structure is visible before the feature ships. |
| **Base currency** | Currency everything is converted to for totals/charts. V1: EUR (per-user setting is a Future Feature). |

---

## 3. Users & Access Model

- **User** — a normal account. Has email (required), username, password. Uses the entire app. Sees other users' data only through Social: portfolios friends explicitly shared, plus (later) shared links/profiles.
- **Admin** — a **management-only account kind**, not a superpowered user. Admins administer the system in their own admin area (served on the admin origin, §4.6/§11): accounts, invites, audit log, email log, global app settings. Admins have **no portfolio, no workboard, no friends — no user-app features at all**; the user API rejects admin sessions and vice versa. If an admin wants to try the app, they create a separate test user account. Admin cannot browse users' portfolios (privacy stance — deliberate).
- **Friends** — users connect via friend requests (username or email). Friendship is mutual and revocable; it is the only V1 sharing audience.
- **How accounts come to exist** is governed by the global **registration mode** (§6.12): V1 runs in **closed mode** (admin-created accounts + invite links, exactly as today); invite/token, approval and open modes are designed and visible in admin settings, enabled post-v1.

**Email is mandatory on every account** — it is the identity for invites, password handling and the primary notification channel.

---

## 4. System Architecture

### 4.1 Three layers, strictly separated

```
┌────────────────────────────────────────────────────────────┐
│ DISPLAY LAYER — runs on the user's machine                 │
│   BetterTrack Web (React SPA, static files)                │
│   future: mobile app (Firebase push), CLI, widgets,        │
│   third-party API clients — all equal API clients          │
└──────────────▲─────────────────────────▲───────────────────┘
               │ REST /api/v1 (JSON)     │ WebSocket (push, post-v1)
┌──────────────┴─────────────────────────┴───────────────────┐
│ BUSINESS LAYER — BetterTrack Core (Node/Express service)   │
│   HTTP API · /docs · Services · Domain logic               │
│   Notifications · Jobs/Scheduler · Domain events           │
└──────▲──────────────▲──────────────▲───────────────────────┘
       │              │              │ AssetProvider interface
┌──────┴──────┐ ┌─────┴─────┐ ┌──────┴──────────────────────┐
│ DATA LAYER  │ │           │ │ External market data        │
│ PostgreSQL  │ │   Redis   │ │  yahoo (V1) · manual (V1)   │
│ (records)   │ │cache/queue│ │  crypto/gold providers later│
└─────────────┘ └───────────┘ └─────────────────────────────┘
```

**Principles (rules, not suggestions):**

1. **The display layer is dumb.** The SPA is static files served with long-lived cache headers; after load it runs entirely in the browser. Server cost per user ≈ API calls only.
2. **API-first.** Every feature is an API endpoint first; the web app is merely the first consumer. A future mobile app (or the owner's smart fridge) uses the same `/api/v1` — zero backend changes. The internal API is documented at **`/docs`** on the API origin (§6.13).
3. **All logic lives in the business layer.** The SPA never computes anything authoritative.
4. **Be polite upstream.** Every provider byte is cached; concurrent identical requests coalesce into **one** upstream fetch; N users viewing the same asset (or, later, the same live stream) cost one provider request. This is a correctness requirement, not an optimization (§5.3).
5. **Data sources are pluggable.** Nothing outside `providers/` knows Yahoo exists.
6. **Modular monolith.** One deployable backend with strict internal module boundaries. Realtime push (Socket.IO gateway, rooms per §4.5) is designed in but **implemented post-v1** — V1 clients stay fresh via TanStack Query refetch, which the architecture must keep sufficient.

### 4.2 Repository layout (pnpm monorepo, TypeScript everywhere)

```
bettertrack/
├── apps/
│   ├── web/                  # DISPLAY: React SPA (user app + admin app)
│   └── api/                  # BUSINESS: Express service (+ worker entrypoint)
├── packages/
│   ├── contracts/            # Shared zod schemas, TS types, typed API client
│   └── config/               # Shared tsconfig, eslint, prettier
├── infra/
│   ├── docker-compose.yml    # prod topology
│   ├── docker-compose.dev.yml# db+redis only, apps run locally
│   ├── nginx/                # both deployment modes (§11)
│   └── .env.example
├── factory/                  # autonomous build factory (prompts, runner)
├── docs/                     # auxiliary docs (runbooks) — see below
├── CLAUDE.md · MODELUSE.md · PROJECTPLAN.md · README.md   # root docs (stay at root)
```

**Root docs tidy (executed in P0):** auxiliary documents move to `docs/` — `STARTAUTOMATE.md` → `docs/STARTAUTOMATE.md` (factory runbook) and `FableBackExecute.md` → `docs/FableBackExecute.md` (kept as the outage-runbook template). `CLAUDE.md`, `PROJECTPLAN.md`, `MODELUSE.md`, `README.md` stay at root. Anything referencing the moved files updates its links.

`packages/contracts` is the keystone: every request/response body is a zod schema defined once, used by the API for validation, by the SPA for types, by `/docs` for OpenAPI generation, and later by mobile/third-party clients.

### 4.3 Backend internal structure (`apps/api/src/`) — target module layout

The existing layout already matches; v2 is **additive** (new modules marked ✚). A T3 agent creates the new directories as their phases land; no existing files move.

```
config/          # env schema — incl. deployment topology & derived origins (§11)
http/            # Express routes/controllers — THIN: parse → call service → respond
  routes/        # auth, admin, assets, search, workboard, portfolio, customAssets
                 #   ✚ conglomerateRoutes ✚ socialRoutes ✚ notificationRoutes
                 #   ✚ settingsRoutes ✚ docsRoutes
  middleware/    # session, csrf, rateLimit (progressive, §10), validate
docs/          ✚ # OpenAPI generation from contracts + the /docs page (§6.13)
services/        # business orchestration; one directory per module:
                 #   auth/ sessions/ password/ crypto/ audit/ admin/
                 #   assets/ customAssets/ currency/ portfolio/ workboard/
                 #   email/ (gains emailLog)
                 #   ✚ search/ (local catalog index)  ✚ conglomerates/
                 #   ✚ social/ (friends + sharing)    ✚ notifications/
                 #   ✚ appSettings/ (global admin settings)
domain/          # PURE functions, no I/O: holdings, backtest, ✚ allocation
                 #   (post-v1: alertEval). 100% unit-testable, the most-tested code.
data/            # Drizzle schema + repositories (all SQL lives here)
providers/       # AssetProvider interface, registry, yahoo, manual,
                 #   cache/coalescing/circuit-breaker/request-queue (§5.3)
jobs/            # BullMQ queues, processors, cron schedules
events/          # Domain event bus (typed), producers/consumers
realtime/        # post-v1: Socket.IO gateway (rooms per §4.5) — do not create until built
scripts/         # migrate, seed, worker entrypoints
testing/         # shared test harness
```

**Dependency rule:** `http`/`realtime`/`jobs` → `services` → `domain` + `data` + `providers`. Never the reverse; `domain` imports nothing but types.

### 4.4 Tech stack

| Concern | Choice | Why |
|---|---|---|
| Language | TypeScript end-to-end | One language, shared types via `contracts` |
| Frontend | React 19 + Vite + Tailwind 4 + react-router 7 | Static SPA build, fast dev loop |
| Client data | TanStack Query + typed API client | Caching, retries, focus-refetch = polite to the server |
| Charts | `lightweight-charts` + Recharts (donuts/bars) | Finance-grade charts, tiny footprint |
| Backend | Node 22 + Express 5 | Boring, proven |
| Validation | zod (schemas in `contracts`) | Single definition for API + client + OpenAPI |
| ORM / DB | Drizzle ORM + **PostgreSQL 17** | Typed SQL; also hosts the search index (`pg_trgm` + tsvector) |
| Cache / queues / sessions | **Redis 7** | Quote cache, BullMQ jobs, session store, rate-limit state |
| Jobs | BullMQ | Scheduled + retryable background work |
| Auth | Session cookies (httpOnly) + argon2id | Right fit for a closed-by-default app; API keys/OAuth post-v1 (§6.13) |
| Email | Nodemailer over SMTP — **Gmail app password is the first-class preset** (§6.10) | Owner runs a Gmail sender; every send is written to the per-user email log |
| Market data | `yahoo-finance2` behind the provider interface | Free, EU coverage; unofficial → aggressively cached, coalesced, swappable |
| API docs | OpenAPI 3 generated from contracts, rendered at `/docs` | People can integrate BetterTrack into their own tools |
| Realtime (post-v1) | Socket.IO (Redis adapter) | Rooms, reconnects — for alerts, Live Mode, live shares |
| Mobile (future) | Same REST API + Firebase Cloud Messaging for push | Designed-for, not built; a new NotificationChannel + API client, no backend rewrite |
| IDs | UUIDv7 · Deploy: Docker Compose · CI: GitHub Actions | Sortable ids; reproducible deploys; typecheck/lint/test/build per push |

### 4.5 Realtime gateway (designed now, built post-v1)

Socket.IO endpoint at `/ws` on the API origin, authenticated via the session cookie on handshake. Rooms: `user:{id}` (notifications, portfolio changes), `asset:{id}` (quote updates, Live Mode frames), `portfolio:{id}` (shared-portfolio viewers). Services publish typed **domain events** on the internal bus (`events/`, already implemented); the gateway and the notification dispatcher are subscribers. V1 explicitly ships without the socket: the SPA's refetch behavior must keep every V1 feature fully functional — the gateway is an enhancement layer for alerts, Live Mode and live shared views when they land.

### 4.6 Deployment topology

BetterTrack must be **flexible to set up anywhere**. Two supported modes, one global config (full env scheme in §11):

- **Subdomains mode** (default): three origins on one domain — `api.<domain>`, `web.<domain>`, `admin.<domain>`. Subdomain names are configurable; TLS terminates at the front proxy.
- **Ports mode**: no subdomains — everything on one host, each service on its own configurable port (e.g. `host:8080` web, `host:8081` admin, `host:3000` api).

| Service | Image / role | Notes |
|---|---|---|
| `web` | nginx serving `apps/web/dist` | Serves the SPA for **both** the web and admin origins (host- or port-based server blocks, §11); injects runtime config telling the SPA which app to mount |
| `api` | Node (apps/api, entry `server.ts`) | HTTP API + `/docs` (+ WebSocket post-v1) |
| `worker` | Same image, entry `worker.ts` | BullMQ processors |
| `db` | postgres:17 | Volume `pgdata`; nightly `pg_dump` to volume `backups` |
| `redis` | redis:7 | AOF persistence on |

The API is its own origin in both modes; the SPA calls it cross-origin with credentials. CORS allowlist and cookie attributes are **derived** from the topology config (§10, §11) — never hand-maintained. Health: `GET /api/v1/health`.

---

## 5. Data Layer

### 5.1 The provider abstraction (the "trackable" core)

Everything price-like flows through one interface:

```ts
type AssetRef = { providerId: string; providerRef: string }; // e.g. { 'yahoo', 'BAYN.DE' }

interface AssetProvider {
  readonly id: string; // 'yahoo' | 'manual' | future: 'coingecko', 'metals', ...

  search(query: string): Promise<AssetSearchResult[]>;   // catalog-fill only, never user-facing hot path (§6.2)
  getQuote(ref: AssetRef): Promise<Quote>;               // price, currency, prevClose, dayChangePct, asOf
  getHistory(ref: AssetRef, range: Range, interval: Interval): Promise<PricePoint[]>;
  getMeta(ref: AssetRef): Promise<AssetMeta>;            // name, symbol, exchange, currency, type
}
```

- A central **registry** maps `providerId → instance`. Services call `registry.for(asset)` — nobody else knows which provider is behind an asset.
- **`manual` provider** backs custom investments: `getQuote` returns the latest value point, `getHistory` the value points with carry-forward. Portfolio charts and P/L need zero special-casing for a house vs. a stock.
- **FX pairs are just assets** (`yahoo:EURUSD=X`), reusing the same caching/backfill machinery.
- Failure handling: timeouts (5 s), retry-once, circuit breaker; on failure the API serves the last cached value marked `stale: true` rather than erroring (stale-while-revalidate).
- Adding crypto/gold later = registering symbols or a new provider file. Nothing else changes.

### 5.2 Market data: Yahoo Finance via `yahoo-finance2`

Capabilities used: `search()` (catalog-fill), `quote()`, `chart()` (OHLC history, dividend/split-adjusted closes — backtests are total-return). Known risk: unofficial API, can change or rate-limit. Mitigations: (a) every byte cached and coalesced (§5.3), (b) outbound calls run through a queue with bounded concurrency + exponential backoff on 429/5xx, (c) user-facing search never blocks on Yahoo (§6.2), (d) the provider interface makes replacement a contained change, (e) UI disclaimers (§10).

### 5.3 Caching & upstream-politeness (a keystone, owner priority)

**The rule: one piece of upstream data is fetched once per TTL window, no matter how many users want it.** Never compromise freshness by more than the TTL; never multiply requests by users.

| Data | Where | TTL | Notes |
|---|---|---|---|
| Quote | Redis | 60 s | One fetch serves every user viewing that asset |
| History 1D (1 m candles) | Redis | 60 s | |
| History 1W (15 m) | Redis | 5 min | |
| History 1M (30 m) | Redis | 15 min | |
| History 6M/1Y (1 d) | Redis | 1 h | |
| History 5Y (1 wk) / Max (1 mo) | Redis | 6 h | |
| Provider search results (catalog-fill) | Redis | 24 h | Keyed by normalized query; also upserted into the asset catalog |
| Negative results (unknown symbol, 404) | Redis | 15 min | So repeated misses don't hammer the provider |
| **Daily closes** (backtests/portfolio history) | **Postgres `price_history`** | permanent | Nightly refresh + on-demand backfill (§9) |
| FX daily rates | Postgres (FX assets in `price_history`) | permanent | |

Mandatory mechanics (all already partially in `providers/`; P1 hardens them to this spec):

- **Request coalescing** on every cache key (`{providerId}:{providerRef}:{kind}:{range}`): concurrent misses trigger exactly **one** upstream fetch; all callers await the same promise (in-process) backed by a short Redis lock (cross-process).
- **Serve-stale-while-revalidate**: an expired entry is returned immediately (marked `stale`) while one background refresh runs.
- **Per-provider request budget**: bounded concurrency + minimum spacing; 429 from upstream opens the circuit breaker and stretches TTLs instead of erroring users.
- **Live Mode (post-v1) inherits this**: one polling loop per hot asset feeds a Redis ring buffer; N viewers = 1 upstream stream, auto-stops when nobody watches.

### 5.4 Currency handling

- Every asset keeps its **native currency**; all stored amounts are native.
- Conversion happens at read/computation time in one place: `services/currency` (current rates for quotes/totals, historical daily rates for backtests and portfolio history).
- Base currency is **EUR**, but always a parameter (`toBase(amount, currency, date?)`) — per-user base currency (Future Feature) is a settings field + passthrough, not a refactor.
- Display: money 2 dp, quantities up to 6 dp, weights 1 dp (stored at higher precision).

### 5.5 Database schema (PostgreSQL, via Drizzle)

Conventions: `id uuid pk` (UUIDv7), `created_at/updated_at timestamptz` (UTC, displayed Europe/Vienna); `numeric(20,8)` quantities, `numeric(20,6)` prices/values, `numeric(6,3)` weights. Existing tables carry over; v2 changes are marked ✚ (new) or Δ (changed).

```
users               id, email UNIQUE NOT NULL, username UNIQUE NOT NULL,
                    password_hash, role ENUM(user, admin), status ENUM(active, disabled),
                    must_change_password bool, base_currency char(3) DEFAULT 'EUR',
                    Δ pin_hash NULL, Δ pin_enabled bool DEFAULT false,      -- §6.1 PIN option
                    last_login_at, created_at, updated_at
                    -- Δ role is an ACCOUNT KIND, not a permission flag: role=admin is a
                    --   management-only account with no user-app data (§3). Admin accounts
                    --   never get a default portfolio; user endpoints reject them.

invites             id, email NOT NULL, token_hash UNIQUE, created_by → users,
                    expires_at (default +7 days), used_at NULL, revoked_at NULL

app_settings      ✚ key text PK, value jsonb, updated_at, updated_by → users
                    -- global admin settings: registration_mode ENUM(closed, invite_token,
                    --   approval, open) DEFAULT closed; beta_mode bool; future flags (§6.12)

assets              id, provider_id, provider_ref, owner_id NULL → users,
                    type ENUM(stock, etf, index, fx, commodity, crypto, custom),
                    symbol, name, exchange NULL, currency char(3), meta jsonb,
                    Δ search_text tsvector GENERATED (symbol+name), GIN index;
                    Δ pg_trgm GIN index on (symbol, name) for fuzzy matching   -- §6.2
                    UNIQUE(provider_id, provider_ref, owner_id)
                    -- owner_id NULL = global catalog asset; set = that user's custom asset

price_history       asset_id → assets, date, close numeric, UNIQUE(asset_id, date)
                    -- daily adjusted closes; FX pairs and custom value points included

workboard_items     id, user_id, asset_id, sort_order int, note NULL, UNIQUE(user_id, asset_id)

alerts              (exists; feature is post-v1 — kinds/lifecycle spec preserved in §14)

notifications       id, user_id, type, title, body, payload jsonb, read_at NULL, created_at
                    -- v2 types: friend.request, friend.accepted, portfolio.shared,
                    --   account.invite, account.temp_password (alert.triggered post-v1)

notification_settings  user_id, channel ENUM(inapp, email, telegram, discord),
                       enabled bool, config jsonb, UNIQUE(user_id, channel)

email_log         ✚ id, user_id NULL → users, to_email, template, subject,
                    status ENUM(sent, failed, suppressed), error_code NULL,
                    triggered_by NULL → users, created_at
                    -- every outbound email, queryable per user in admin (§6.10); no bodies stored

friend_requests   ✚ id, from_user → users, to_user → users, status ENUM(pending, accepted,
                    declined, cancelled), created_at, responded_at NULL,
                    UNIQUE(from_user, to_user) WHERE status='pending'

friendships       ✚ user_a → users, user_b → users, created_at, PK(user_a, user_b)
                    -- stored with user_a < user_b (one row per pair); created on accept

conglomerates       id, owner_id → users, name, description NULL,
                    status ENUM(draft, active), created_at, updated_at

conglomerate_positions  id, conglomerate_id, asset_id, weight_pct numeric(6,3),
                        sort_order int, UNIQUE(conglomerate_id, asset_id)

share_links         (exists; conglomerate share links are post-v1 — spec preserved in §14)

portfolios          id, user_id, name DEFAULT 'Main', UNIQUE(user_id, name),
                    Δ visibility ENUM(private, friends) DEFAULT private,   -- §6.8/§6.9
                    Δ sort_order int
                    -- V1: exactly one auto-created per user; ALL portfolio queries are
                    --   portfolio_id-scoped so multi-portfolio is purely additive (§6.8)

transactions        id, portfolio_id, asset_id, side ENUM(buy, sell),
                    quantity numeric(20,8) > 0, price numeric(20,6) >= 0,  -- native currency
                    fee numeric(20,6) DEFAULT 0, executed_at timestamptz, note NULL

api_keys            (post-v1, designed: id, user_id, name, token_hash, scopes jsonb,
                     last_used_at, revoked_at — API keys NEVER expire, only revoke; §6.13)

audit_log           id, actor_id NULL → users, action, target_type, target_id,
                    ip, meta jsonb, created_at
```

Sessions live in **Redis** (not Postgres) — 30-day expiry semantics in §6.1. Deleting a user cascades to everything they own; friendships and pending requests involving them are removed.

---

## 6. Business Layer — Feature Specifications

Each spec defines behavior precisely enough to implement and test against. V1 scope markers: features are **V1** unless flagged **[Coming Soon]** (visible placeholder in V1) or **[post-v1]** (in §14).

### 6.1 Authentication, accounts & sessions

- **Login** (`POST /auth/login`, email-or-username + password): argon2id verify; on success rotate session ID, set `last_login_at`, audit-log. Generic error on failure (no user enumeration); a **disabled** account with the correct password gets `ACCOUNT_DISABLED` (see §16, 2026-06-16). An **admin** account logging into the user app gets a clear error pointing at the admin origin (and vice versa) — the two apps authenticate the two account kinds disjointly.
- **Sessions:** httpOnly, `SameSite=Lax`, `Secure` (prod) cookie → Redis session. **Sessions expire after 30 days.** The 30-day window renews on login and on **PIN entry** (below) — so the logout only happens after 30 days without either. Password change or account disable kills all sessions of that user instantly. **API keys (post-v1) never expire** — revocation only.
- **PIN option (V1):** a user can enable a PIN in Settings → Security. With PIN on, every time the website is (re)opened the SPA shows a PIN screen before the app; a correct PIN (`POST /auth/pin/verify`, argon2id-hashed, rate-limited like login) **renews the 30-day session window**. The PIN is a convenience re-confirmation layered on the session, not a second factor; 5 consecutive failures fall back to full login.
- **2FA (planned, post-v1):** TOTP-based two-factor login is designed-for (enrollment + recovery codes under Settings → Security); noted here so session/login code keeps a hook for a second verification step.
- **Forced password change:** temp-password accounts have `must_change_password=true`; every API call except `change-password`/`logout` returns `403 PASSWORD_CHANGE_REQUIRED`; the SPA traps this into the change screen.
- **Invite accept** (`GET /invite/:token` page → `POST /auth/accept-invite`): token valid = unused, unrevoked, unexpired (7 days). Email fixed from the invite; user picks username + password; logged in immediately. Invites always create **user**-kind accounts.
- **Registration modes** beyond invites are governed by admin global settings (§6.12); V1 enforces `closed`.
- **Password policy:** ≥ 10 chars, no composition rules, top-10k-common-passwords blocklist. Temp passwords: 16-char random, shown to the admin exactly once.
- **Progressive rate limiting** (spec in §10): normal users should almost never notice limits; escalating short cooldowns replace long blocks.
- **Login UI:** the login card sits **top-middle-ish** — horizontally centered, vertically a little above center (not bottom-anchored, not glued to the top).

### 6.2 Search & the local asset catalog (owner priority)

Search is **local-first**: BetterTrack maintains its own asset catalog and searches it — a user typing "bayer" must never trigger a synchronous Yahoo round-trip, and partial/fuzzy queries must work.

- **Catalog:** the global rows of `assets`, indexed with tsvector + `pg_trgm` (§5.5). Filled by: (a) provider search results (every provider search upserts its results), (b) history backfill/metadata jobs, (c) an optional seed list of common symbols (major indices, DAX/ATX/S&P constituents) shipped with the app, (d) users' custom assets (owner-scoped).
- **Query path** (`GET /search?q=`): normalize query → catalog lookup (prefix + trigram similarity + full-text over symbol/name, ranked: exact symbol > symbol prefix > name match > fuzzy) merged with the user's custom assets. Responds immediately from Postgres.
- **Provider fallback:** if the catalog yields < 3 results, a provider search runs **in the background** (budgeted, coalesced per §5.3, 24 h-cached per normalized query) and upserts into the catalog; the client refetches after a short delay ("Searching providers…" affordance) and sees the enriched results. Misspellings and partial names therefore work from the catalog's trigram index even when providers would 404.
- Results: symbol, name, exchange, type badge, currency. Actions on every result: **→ Watchlist**, **→ Conglomerate** (picker), **→ Portfolio** (buy dialog).
- First time any user touches a market asset, it is upserted into `assets` and a history backfill job is enqueued.
- UI: search lives in the **Assets** section + global **Cmd/Ctrl-K palette** from anywhere. Debounce 300 ms, min 2 chars.

### 6.3 Assets section & asset detail

**Assets** is the browser/discovery tab for everything BetterTrack knows. Subnavigation: **Overview | Search | Stocks | ETFs | Crypto | Commodities | Custom Assets**. V1 implements Overview (simple entry: search box + recently viewed + catalog counts), Search, and asset detail; the per-category browse pages and advanced filters are **[Coming Soon]** placeholders.

**Asset detail page:**
- Chart (lightweight-charts, area style) with ranges **1D · 1W · 1M · 6M · 1Y · 5Y · Max** (range→interval mapping = cache table §5.3). Custom assets: value points, step-line.
- Header: name, symbol, exchange, native price + day change (green/red), EUR-converted price if foreign, `asOf` timestamp + `stale` marker, "data may be delayed" footnote.
- Stats row (when the provider supplies it): prev close, day range, 52-week range, market state.
- **Live Mode [Coming Soon in V1]:** a `LIVE` toggle on the chart switches from historical ranges to short real-time windows (1 m · 10 m · 30 m · 1 h · 3 h · 12 h — exact windows adjustable later). Design (built post-v1): the worker polls each hot asset **once** into a Redis ring buffer and fans out to all viewers via `asset:{id}` rooms — four users watching live BAYN.DE = one provider stream; polling auto-stops when nobody watches. V1 ships the button marked Coming soon; the V1 chart may refresh the quote every 60 s (cache-served) as a light preview, with no second-level streaming.
- **Quick actions:** add to Portfolio (buy dialog), add to Watchlist, add to Conglomerate; *Appears in* section (your conglomerates/holdings). [Coming Soon]: share to Social, suggest to a friend, alerts on this asset.

### 6.4 Workboard — the playground

The Workboard is where users **experiment** before committing money: try baskets, backtest, calculate — a toolbox, not an accounting page. Subnavigation: **Overview | Conglomerates | Watchlists | Backtests | Calculators | Comparisons | Saved Ideas**.

V1 implements:
1. **Overview** — entry cards: watchlist summary, "My Conglomerates" grid (name, positions count, mini-chart, → detail, "New Conglomerate"), quick links to calculator/backtest.
2. **Watchlist** — table of watched assets: sparkline (1M), price, day ±%; drag-to-reorder, per-row note, remove. Empty state points to Assets search. (V1: one watchlist; named multiple watchlists **[Coming Soon]**.)
3. **Conglomerates** — list + Builder + detail (§6.5) with backtest (§6.6) and calculator (§6.7).

**[Coming Soon]** in V1: Comparisons, Saved Ideas, watchlist sharing, standalone Backtests/Calculators pages beyond the conglomerate-embedded ones. **[post-v1]**: price alerts & the alerts panel (spec preserved in §14 — schema already exists).

### 6.5 Conglomerates & the Builder

**Model rules:** 1–50 positions; weights `0 < w ≤ 100` with ≤ 3 decimals; **status `active` requires Σ weights = 100 ± 0.01**; `draft` allows any state. Names unique per owner (case-insensitive). Deleting (confirm dialog) hard-deletes.

**The Builder** (`/workboard/conglomerates/new`, `…/:id/edit`) is a full-screen experience — the flagship UX:
- **Left — Add assets:** embedded search; click adds a position at weight 0.
- **Center — Positions:** per row: symbol/name, weight number-input (0.001 precision) + slider (0–100, step 0.5), **lock toggle** 🔒, remove. Footer: live **sum pill** — green "100.0%" when valid, amber "87.5% — 12.5% left" otherwise.
  - **Auto-balance:** distributes `100 − Σ(locked)` equally across unlocked positions.
  - **Normalize:** scales unlocked positions proportionally to hit exactly 100 (locked untouched; error if locked alone ≥ 100).
- **Right — Live preview:** allocation donut + backtest chart (1Y/3Y/5Y/Max) + headline stats, recomputed **debounced 500 ms** after any weight change (fast because `price_history` is warm).
- **Autosave:** every change persists to the `draft` immediately ("Draft — saved" pill). "Activate" validates and flips to `active`.

**[Coming Soon]** in V1: sharing conglomerates to friends, public share links, JSON export/import (incl. the legacy `conglomerate.py` format — full spec preserved in §14), nested conglomerates, rebalanced backtests.

### 6.6 Backtest engine

Pure function in `domain/backtest.ts` (implemented):

```
Inputs:  positions[{assetId, weight}], range, baseCurrency (EUR)
Data:    daily adjusted closes from price_history; daily FX from FX assets
Method:  1. Window = requested range, clipped to the latest first-available
            date across positions ("common start"). If clipped, the response
            carries a notice: "Limited by TEM (data since 2024-06-14)".
         2. Convert each series to EUR using that day's FX rate;
            carry forward last close over non-trading days per asset.
         3. index(t) = 100 · Σᵢ wᵢ · Pᵢ(t)/Pᵢ(t₀)   (buy-and-hold of initial
            weights — no rebalancing; documented limitation, rebalanced mode
            is a Future Feature. Adjusted closes ⇒ dividends included.)
Outputs: series[{date, value}], stats: total return %, CAGR, max drawdown,
         annualized volatility (σ of daily returns × √252), best/worst day,
         per-position contribution; optional benchmark overlay series.
```

- Benchmarks: overlay toggle for `^GSPC` (S&P 500), `^GDAXI` (DAX), `URTH` (MSCI World) — same pipeline, weight 100.
- API: `GET /conglomerates/:id/backtest?range=5y&benchmark=^GSPC`; unsaved Builder state: `POST /backtest/preview`.
- Results memoized in Redis 1 h keyed by hash(positions+range) — slider-wiggling stays cheap.

### 6.7 Invest Calculator (budget → buy list)

Pure function in `domain/allocation.ts`. Inputs: active conglomerate, budget `B` in EUR, mode `whole | fractional` (+ optional fractional step, e.g. 0.0001). Prices: current quotes EUR-converted. **Hard guarantee: total cost ≤ B. Never overshoot.**

- **Fractional:** `qtyᵢ = (B·wᵢ)/pᵢ` rounded **down** to the step ⇒ spend ≈ B minus dust.
- **Whole shares:**
  ```
  1. targetᵢ = B · wᵢ
  2. qtyᵢ = floor(targetᵢ / pᵢ)                       # never above target
  3. leftover = B − Σ qtyᵢ·pᵢ
  4. while any share price ≤ leftover:
       among affordable assets, buy 1 share of the one that most reduces
       Σᵢ |actualᵢ − targetᵢ|  (tie-break: larger target weight first)
       update leftover
  5. emit per-position qty, cost, actual % vs target %, Δpp; totals + leftover
  ```
- **Worked example** (illustrative prices): B = 1000 €, BAYN.DE 30% @ 25 €, NVDA 60% @ 150 €, GOOGL 10% @ 140 €.
  Step 2: BAYN 12×25 = 300 €, NVDA 4×150 = 600 €, GOOGL 0×140 = 0 €. Leftover 100 € < 140 € ⇒ no fill possible. **Result: 900 € spent, 100 € left, GOOGL 0% vs target 10% — flagged: "GOOGL share price (140 €) exceeds its 100 € slice; raise the budget to ≥ ~1400 € or use fractional mode."** Unreachable weights are surfaced exactly like this, never silently mis-weighted.
- Edge cases: any single price > B ⇒ position flagged unbuyable; market closed ⇒ last close with notice; stale quotes ⇒ warning banner.
- **Buy flow:** result table → **Add to Portfolio** → dialog pre-filled with one BUY per non-zero position (qty/price from the calculation, `executed_at = now`, all editable) → confirm ⇒ bulk-insert. Two clicks from plan to recorded reality.

### 6.8 Portfolio — the home tab

Portfolio is the first tab and the app's home (`/` redirects here): *what do I own, what is it worth, how is it doing*.

- **Multi-portfolio model, one default in V1:** every user gets an auto-created default portfolio; **all queries, endpoints and services are `portfolio_id`-scoped** (`/portfolios/:id/…`) so additional portfolios are purely additive. V1 UI shows a **portfolio switcher placeholder** (current portfolio name + a disabled "New portfolio — Coming soon" affordance). Multi-portfolio UI, shared-editable portfolios and per-section privacy are **[Coming Soon]**.
- **Transactions are the source of truth**; holdings are derived, never stored.
  - BUY: quantity, price (native), fee, timestamp, note. SELL: same; rejected if held quantity would go negative.
  - **Average-cost basis:** buys re-average `avg_cost = (held_qty·avg_cost + qty·price + fee) / (held_qty + qty)`; sells reduce quantity, avg cost unchanged; realized P/L of a sell = `qty·(price − avg_cost) − fee` (shown on the row; FIFO/tax lots are Future Features).
- **Overview page (V1):** totals header (market value, invested, unrealized P/L €/%, day change €/%), performance chart (1M/6M/1Y/Max), holdings list, **top winners / top losers** among holdings (by day % and by total P/L toggle), allocation donuts (by asset / by type), recent transactions, quick add-transaction. Widget-based customizable layout is **[Coming Soon]** — V1 uses a fixed sensible arrangement of the same blocks.
- **Value-over-time chart:** daily series from first transaction: `Σ qty_held(d) · price_EUR(d)` from `price_history` + historical FX; custom assets contribute carried-forward value points. Cached 1 h, invalidated on any transaction/value-point change.
- **Custom investments:** create (name; category: real estate · vehicle · collectible · cash · unlisted stock · other; currency; optional initial BUY). **Value points** editor: (date, value) rows, one per day, carry-forward between points. A custom asset then behaves like any asset — chartable, holdable, watchable, even usable in conglomerates.
- **Visibility (V1):** per portfolio, `private` (default) or `friends` — the "Share this portfolio with friends: Yes/No" toggle (§6.9). Broker CSV imports (Trade Republic, George, …) are **[Coming Soon]** here and in Settings.

### 6.9 Social — friends & tiny sharing (V1 scope is deliberately small)

Subnavigation: **Friends | Shared With Me | My Shared Items** (+ **Ideas** and **My Public Profile** as **[Coming Soon]** pages).

The V1 friend system, complete:
1. A user enters another user's **username or email** → `POST /social/requests`. No user enumeration: the response is identical whether or not the target exists; a request to a nonexistent address simply never appears anywhere.
2. The target sees the request (bell notification + Friends page) and **accepts or declines**.
3. Accepted ⇒ one `friendships` row; both see each other in **Friends**. Either side can **remove** the friend (row deleted, no notification).
4. If a friend's portfolio has `visibility = friends`, it appears under **Shared With Me** → read-only portfolio view (holdings, totals, performance chart — no transactions detail beyond what the owner's overview shows, no edit affordances anywhere).
5. **My Shared Items** lists what I'm currently sharing (V1: my portfolios with the friends toggle on) with quick toggle-off.

Explicitly **not** V1: feed, comments, likes, chat, groups, public discovery, activity timeline, per-asset privacy rules, edit permissions, public profiles/links (all §14).

**Privacy boundary (test-gated):** every social read is scoped by an existing friendship **and** the owner's visibility flag at query time; revoking either instantly closes access (no caching of authorization). Non-friends receive 404, never 403.

### 6.10 Notifications, channels & the email log

```ts
interface NotificationChannel {
  readonly id: 'inapp' | 'email' | 'push' | 'telegram' | 'discord' | string;
  send(user: User, n: NotificationMessage): Promise<void>;
}
```

- **Registry + per-user settings** (`notification_settings`): in-app always on; email on by default. Adding a channel (phone push via Firebase, Telegram, Discord — all post-v1) = implement the interface, register it, add a settings toggle. The dispatcher fans out one `NotificationMessage` to all enabled channels, at-least-once, deduped per (user, event key).
- **V1 notification types:** `friend.request`, `friend.accepted`, `portfolio.shared` ("Anna shared her portfolio with friends"), `account.invite`, `account.temp_password`. (`alert.triggered` arrives with alerts, post-v1.)
- **In-app:** bell with unread badge, dropdown list, mark-read/mark-all; full list in Settings → Notifications. V1 freshness via TanStack Query polling/refocus-refetch; socket push upgrades this post-v1.
- **Email — Gmail app password first:** Nodemailer over SMTP; the documented, tested first-class configuration is a **Gmail account + app password** (`smtp.gmail.com:465`, §11). Any other SMTP still works. Minimal clean HTML templates: invite, temp password, welcome, friend request, portfolio shared.
- **Email log (V1):** every send attempt writes an `email_log` row — user, recipient, template, subject, sent/failed/suppressed, coarse error code, no bodies/secrets. Admin can view the log **per user** (§6.12) and globally. Failures retried 3× exponential; suppressed = channel disabled/unconfigured.

### 6.11 Settings

Settings is the central place for account- and system-level personal settings (reached via the profile menu). Subnavigation: **Account | Notifications | Imports & Exports | Connections | Backups | API Access | Security**.

V1 implements:
- **Account** — username/email display, change password, base-currency display (fixed EUR, marked), and privacy/sharing toggles (portfolio visibility, mirrors §6.8/§6.9).
- **Notifications** — per-channel toggles (in-app, email), full notification list.
- **Security** — sessions info ("signed in since …, expires after 30 days of inactivity"), **PIN enable/change/disable** (§6.1), 2FA section marked planned.

**[Coming Soon]** pages (routed, designed placeholders): Imports & Exports (broker CSV, account data export), Connections (Google login, third-party), Backups (Google Drive), API Access (API keys, OAuth apps — §6.13).

**Profile menu** (top-right icon, compact dropdown): **My Portfolio · Settings · Invite Others [Coming Soon] · Share Profile [Coming Soon] · Logout**.

### 6.12 Admin area & global app settings

- **Separate world, separate account kind:** the admin app is served on the **admin origin** (`admin.<domain>` or the admin port, §4.6/§11) with its own login and minimal layout. Backend routes under `/api/v1/admin/*` require an admin-kind session; user routes reject admin sessions (§3). Non-admins get 404 (no information leak). Audit-logged throughout. (During transition the SPA keeps `/admin/*` path-routing working in ports/single-origin dev; the admin origin is the production face.)
- **Users:** table (email, username, status, kind, last login, created) with search/filter. Actions: **create user** (temp password shown once), **invite** (7-day tokenized link, copy or auto-send via email), **disable/enable** (disable kills sessions instantly), **reset password**, **delete** (type-username-to-confirm; cascades). Plus per-user **email log** view (§6.10).
- **Global app settings (V1 page, `app_settings`):**
  - **Registration mode** — the four modes, visible as a selector:
    1. **Closed** *(V1 default, fully enforced)* — only admin-created users + invites.
    2. **Invite/access-token** *(post-v1 activation)* — self-serve registration page that requires a valid token.
    3. **Approval** *(post-v1)* — open registration form, accounts land as `pending` until admin approves.
    4. **Open** *(post-v1)* — automatic registration.
    In V1 the selector renders all four with the non-closed modes disabled and marked Coming soon; the enforcement plumbing reads the setting from day one so activating a mode later is a switch, not a rebuild.
  - **Beta mode** toggle placeholder; future app-wide feature toggles and access rules live here too.
- **Overview cards:** user count, active (≤ 30 d), pending invites, tracked assets, job queue health, emails sent (24 h).
- **Audit log:** filterable table.
- Explicitly **not** V1: admin browsing user portfolios (privacy), full feature flags, announcements, analytics, moderation tools (§14).

### 6.13 API access & `/docs`

- **API docs page (V1):** the API serves human-readable endpoint documentation at **`GET /docs` on the API origin** (e.g. `api.<domain>/docs`), rendered from an **OpenAPI 3 document generated from the zod contracts** (`GET /openapi.json`). Every `/api/v1` route appears with schemas, auth requirements and error envelope; CI fails if a route is missing from the spec. The page is public (it contains no secrets and the API itself remains session-guarded) — this is the front door for people integrating BetterTrack into their own tools.
- **API access as a product feature (post-v1, designed now):** users will mint **API keys / personal access tokens** (Settings → API Access) with scoped permissions, and later OAuth apps — enabling third-party phone apps, smart-fridge dashboards, home widgets, scripts. Rules already fixed: **API keys never expire** (revocation only, §5.5), key auth bypasses the PIN/session machinery but honors scopes, and every key-authenticated call is attributable. V1 ships the Settings placeholder + the `/docs` page; token issuance/OAuth is §14.

---

## 7. Display Layer — Web App

### 7.1 Principles

- Pure static SPA (Vite build → `dist/`), served by nginx with immutable cache headers; `index.html` no-cache. All rendering/routing/state in the browser — the server only answers JSON.
- **One build, two apps:** nginx injects a tiny runtime config (`window.__BT__ = { app: 'user' | 'admin', apiOrigin }`) per origin/port; `App.tsx` mounts `UserApp` or `AdminApp` accordingly (path-based `/admin/*` remains the dev fallback). The SPA talks to the API origin cross-origin with credentials (§4.6, §10).
- TanStack Query for all server state: per-endpoint `staleTime` mirroring server cache TTLs, refetch-on-focus, request dedup — a polite client by construction. No socket in V1; refetch keeps everything fresh.
- Dark theme default, light toggle later. Desktop-first, fully usable on mobile (responsive tables → cards). Skeleton loaders, error boundaries with retry, designed empty states, and a shared **ComingSoon** component for placeholder surfaces. Locale: `de-AT` formatting (1.234,56 €).
- Auth: cookie-based; 401 → login redirect; `403 PASSWORD_CHANGE_REQUIRED` → forced-change screen; PIN gate (§6.1) wraps the app when enabled.

### 7.2 Routes (user app — five sections)

| Route | Page | V1 status |
|---|---|---|
| `/login`, `/invite/:token` | Login (card top-middle-ish) · Accept invite | ✔ (restyle) |
| `/` | → redirect `/portfolio` | ✔ |
| `/portfolio` | Portfolio overview (default portfolio; switcher placeholder) | ✔ |
| `/portfolio/transactions` · `/portfolio/custom-assets` | Transactions · Custom investments | ✔ |
| `/workboard` | Workboard overview | ✔ |
| `/workboard/watchlist` | Watchlist | ✔ |
| `/workboard/conglomerates` · `…/new` · `…/:id` · `…/:id/edit` | List · **Builder** · Detail (backtest + calculator) | ✔ |
| `/workboard/comparisons` · `/workboard/ideas` | Comparisons · Saved Ideas | Coming Soon |
| `/assets` | Assets overview (search entry, recently viewed) | ✔ |
| `/assets/search` | Search results (+ global ⌘K palette) | ✔ |
| `/assets/:id` | Asset detail (chart, quick actions, LIVE button = Coming soon) | ✔ |
| `/assets/stocks` · `/assets/etfs` · `/assets/crypto` · `/assets/commodities` · `/assets/custom` | Category browsers | Coming Soon |
| `/social` → `/social/friends` | Friends (requests in/out, list) | ✔ |
| `/social/shared-with-me` | Shared With Me (+ friend portfolio read-only view) | ✔ |
| `/social/my-shared` | My Shared Items | ✔ |
| `/social/ideas` · `/social/profile` | Ideas · My Public Profile | Coming Soon |
| `/settings/*` | Settings subpages per §6.11 | ✔ + Coming Soon mix |
| `/pin` | PIN gate (when enabled) | ✔ |
| admin origin (or `/admin/*`) | Admin app: login, users, invites, email, audit, **settings** | ✔ |

Header: **BetterTrack wordmark — Portfolio — Workboard — Assets — Social — 🔔 — profile icon** (dropdown per §6.11). The main nav never grows beyond these; deeper tools live in each section's subnav.

### 7.3 Target directory layout for `apps/web/src` (P0 executes these moves)

```
apps/web/src/
├── main.tsx · App.tsx          # runtime-config mounting (user vs admin app, §7.1)
├── index.css · vite-env.d.ts
├── lib/                        # apiClient + per-area API modules (unchanged location)
├── ui/                         # shared presentational components incl. charts/ (unchanged)
│   └── ComingSoon.tsx        ✚ # the placeholder component
├── components/                 # cross-app bits (Wordmark) (unchanged)
├── admin/                      # admin SPA: AdminApp, AuthContext, components/, pages/
│   └── pages/SettingsPage.tsx ✚ # global app settings (§6.12)
├── user/
│   ├── UserApp.tsx · AuthContext.tsx · RequireUser.tsx
│   ├── components/             # AppLayout (5-tab nav + profile dropdown + bell),
│   │                           #   dialogs, AssetSearchBox, CmdKPalette, ui
│   ├── hooks/
│   ├── auth/                 ✚ # LoginPage, InvitePage, ForcedPasswordChangePage, PinGate
│   ├── portfolio/            ✚ # PortfolioPage (+ overview blocks), TransactionDialog-consumers,
│   │                           #   CustomInvestmentDialog, ValuePointEditor, switcher
│   ├── workboard/            ✚ # WorkboardOverview, WatchlistPage,
│   │   └── conglomerates/    ✚ #   list, BuilderPage, DetailPage, calculator
│   ├── assets/               ✚ # AssetsOverview, SearchPage, AssetDetailPage, category stubs
│   ├── social/               ✚ # FriendsPage, SharedWithMePage, SharedPortfolioView,
│   │                           #   MySharedItemsPage, stubs (Ideas, PublicProfile)
│   └── settings/             ✚ # SettingsLayout + Account/Notifications/Security
│                               #   + placeholder pages (Imports, Connections, Backups, ApiAccess)
```

**Mechanical move table (P0):**

| From (`apps/web/src/user/…`) | To |
|---|---|
| `pages/LoginPage.tsx`, `pages/InvitePage.tsx`, `pages/ForcedPasswordChangePage.tsx` | `auth/` |
| `pages/PortfolioPage.tsx` (+ its `.test`) | `portfolio/` |
| `pages/WorkboardPage.tsx` (+ test) | `workboard/` (becomes WatchlistPage; new thin WorkboardOverview) |
| `pages/SearchPage.tsx`, `pages/AssetDetailPage.tsx` (+ tests) | `assets/` |
| `components/CustomInvestmentDialog.tsx`, `components/ValuePointEditor.tsx` | `portfolio/` |
| `pages/placeholders.tsx` | dissolved into per-section Coming Soon pages using `ui/ComingSoon` |

`components/TransactionDialog.tsx`, `AssetSearchBox`, `CmdKPalette`, `Dialog`, `ui.tsx` stay in `user/components/` (used across sections). Imports updated mechanically; tests move with their files.

### 7.4 Component inventory (the reusable core)

`AssetSearchBox` · `CmdKPalette` · `PriceChart` (ranges, area/step, benchmark overlay, future LIVE mode slot) · `Sparkline` · `AllocationDonut` · `WeightRow` (input+slider+lock) · `SumPill` · `BudgetCalculator` · `TransactionDialog` (single & bulk) · `ValuePointEditor` · `NotificationBell` · `MoneyText` · `StatCard` · `EmptyState` · `ComingSoon` · `PortfolioSwitcher` (placeholder) · `SubNav` (per-section tabs).

### 7.5 API client

Generated from `packages/contracts` (zod → typed fetch wrapper): fully typed end-to-end, errors normalized to the API envelope, one place for auth/redirect/429-toast policy, `credentials: 'include'` against the configured API origin. The same package is what a future mobile app imports.

---

## 8. API Reference (V1 surface)

Conventions: base `/api/v1`, JSON, camelCase; errors `{ error: { code, message, details? } }`; cursor pagination `?limit&cursor` where lists grow; all routes require a session unless marked **P** (public). Versioning: additive-only within v1; breaking ⇒ `/api/v2`. The whole surface is documented at `/docs` (§6.13).

| Area | Endpoints |
|---|---|
| Auth | **P** `POST /auth/login` · `POST /auth/logout` · `GET /auth/me` · `POST /auth/change-password` · `POST /auth/pin/verify` · `PUT/DELETE /auth/pin` (enable/change/disable) · **P** `GET /auth/invite/:token` · **P** `POST /auth/accept-invite` |
| Search | `GET /search?q=` (local catalog, §6.2) |
| Assets | `GET /assets/:id` · `GET /assets/:id/quote` · `GET /assets/:id/history?range=` |
| Workboard | `GET /workboard` · `POST /workboard` `{assetId}` · `DELETE /workboard/:itemId` · `PATCH /workboard/reorder` |
| Conglomerates | `GET /conglomerates` · `POST /conglomerates` · `GET/PATCH/DELETE /conglomerates/:id` · `PUT /conglomerates/:id/positions` (bulk replace — autosave) · `POST /conglomerates/:id/activate` · `GET /conglomerates/:id/backtest?range&benchmark` · `POST /backtest/preview` · `POST /conglomerates/:id/allocate` `{budgetEur, mode, step?}` |
| Portfolios | `GET /portfolios` (V1: the default) · `GET /portfolios/:id` (holdings+totals) · `PATCH /portfolios/:id` (name, **visibility**) · `GET /portfolios/:id/history?range=` · `GET /portfolios/:id/transactions?cursor=` · `POST /portfolios/:id/transactions` (single or bulk buy-flow) · `PATCH/DELETE /portfolios/:id/transactions/:txId` · `POST /custom-assets` · `PATCH/DELETE /custom-assets/:id` · `GET/PUT /custom-assets/:id/value-points` |
| Social | `GET /social/friends` · `DELETE /social/friends/:userId` · `GET /social/requests` (in/out) · `POST /social/requests` `{usernameOrEmail}` · `POST /social/requests/:id/accept` · `POST /social/requests/:id/decline` · `DELETE /social/requests/:id` (cancel) · `GET /social/shared-with-me` · `GET /social/shared-with-me/portfolios/:id` (read-only, friendship-scoped) |
| Notifications | `GET /notifications?cursor=` · `POST /notifications/mark-read` `{ids|all}` |
| Settings | `GET/PATCH /settings/notifications` · `GET /settings/security` |
| Admin | `GET /admin/users` · `POST /admin/users` · `PATCH /admin/users/:id` · `POST /admin/users/:id/reset-password` · `DELETE /admin/users/:id` · `GET /admin/users/:id/emails` (email log) · `GET/POST /admin/invites` · `POST /admin/invites/:id/revoke` · `GET/PATCH /admin/settings` (global app settings) · `GET /admin/stats` · `GET /admin/audit?cursor=` · `GET /admin/emails?cursor=` |
| Meta | **P** `GET /health` · **P** `GET /docs` · **P** `GET /openapi.json` |

Post-v1 additions (designed): `/alerts`, share links (`/s/:token`), conglomerate export/import, `/settings/api-keys`, WebSocket `/ws`.

---

## 9. Background Jobs & Domain Events

| Job (BullMQ) | Schedule | What it does |
|---|---|---|
| `prices.refreshDaily` | nightly 03:00 Europe/Vienna | Upsert yesterday's daily closes into `price_history` for every asset referenced anywhere + all FX pairs in use. |
| `prices.backfill` | on demand | First use of an asset: fetch max-range daily history (queued, ~1 asset/sec — polite). |
| `fx.refreshSpot` | hourly | Refresh current FX rates for `services/currency`. |
| `catalog.enrich` | on demand | Background provider search for catalog misses (§6.2) + metadata refresh; budgeted per §5.3. |
| `notifications.dispatch` | on event | Fan out a domain event to the user's enabled channels; every email attempt writes `email_log`; per-channel retry 3× exponential. |
| `alerts.evaluate` | post-v1 | Every 1 min; spec preserved in §14. |

Retries: 3 attempts, exponential backoff, dead-letter list visible in admin stats. All schedules live in code.

**Domain events** (typed, `events/`): `notification.created` · `friend.request` · `friend.accepted` · `portfolio.shared` · `portfolio.changed` · `quote.updated` · `conglomerate.updated` (+ `alert.triggered` post-v1). Producers: services/jobs. Consumers: notification dispatcher (V1), realtime gateway (post-v1) — new consumers subscribe without touching producers.

---

## 10. Security & Privacy

- **Closed by default:** every route requires a session except login/invite/health/docs; admin routes require the admin account kind; user routes reject admin sessions (§3). Object access is always ownership-scoped in repositories (`WHERE owner_id = session.user` — no IDOR by construction); social reads additionally require live friendship + visibility (§6.9).
- **Passwords & PIN:** argon2id (memory 64 MB, iterations 3, parallelism 1) for both; session rotation on login; all-session invalidation on password change/disable. Sessions: 30 days, renewed by login or PIN verify (§6.1). API keys (post-v1): hashed at rest, never expire, revocation-only, never logged.
- **Cookies/CSRF/CORS:** httpOnly + `SameSite=Lax` + `Secure`; state-changing requests require `X-Requested-With: BetterTrack`. The API's CORS allowlist is exactly the derived `WEB_ORIGIN` + `ADMIN_ORIGIN` (§11) with credentials; all origins share one registrable domain (or one host in ports mode), keeping `Lax` cookies flowing. Strict `Origin` check on state-changing requests (and the future socket handshake).
- **Headers:** helmet with strict CSP (self + the API origin for connect-src; no third-party assets — fonts/icons bundled).
- **Validation:** every request body/query parsed by the shared zod schema; unknown fields rejected.
- **Progressive rate limiting** (Redis, replaces the old fixed-window blocks — owner directive #79):
  - **Principle:** normal use is never limited; bursts get short pauses; only sustained abuse gets long cooldowns. Limits reset quickly after good behavior (escalation level decays after ~15 min without violations).
  - **General API:** generous steady-state allowance (≈ the current 4500/15 min/user); on exceeding it: cooldown **10–30 s**, then per repeated violation **1 min → 3 min → 10 min** (cap). 429 responses carry `retryAfter`; the SPA shows the existing toast.
  - **Login (stricter, security-sensitive):** ~**10 failed attempts → 30 s cooldown**, next batch → **a few minutes**, escalating to **10 min+** for persistent hammering; per-IP and per-account tracked separately. PIN verify uses the login schedule.
  - Search keeps its own modest per-user budget; admin endpoints get the general schedule.
- **Share/invite tokens:** ≥ 128-bit CSPRNG, url-safe, constant-time compare, revocable, never logged.
- **Outbound safety:** provider HTTP fixed to known hosts (no user-supplied URLs ⇒ no SSRF surface); secrets only via env; `pnpm audit` in CI.
- **Privacy:** PII = email only; user deletion cascades all data incl. friendships; email log stores metadata, never bodies; no analytics/trackers; logs (pino, JSON) exclude bodies and tokens. Backups: nightly `pg_dump`, 14-day rotation, restore procedure in README.
- **Disclaimers (footer + asset pages):** market data comes from an unofficial source and may be delayed or inaccurate; BetterTrack is not investment advice.

---

## 11. Configuration & Environments

**Principle: the app must be flexible to set up anywhere.** One global config drives the whole topology; every name and port is overridable; nothing origin-shaped is hardcoded.

| Variable | Example / default | Notes |
|---|---|---|
| `BT_DOMAIN` | `example.at` | The single global domain |
| `BT_MODE` | `subdomains` \| `ports` | Deployment mode (§4.6) |
| `BT_SUB_API` / `BT_SUB_WEB` / `BT_SUB_ADMIN` | `api` / `web` / `admin` | Subdomain names (subdomains mode) |
| `BT_PORT_API` / `BT_PORT_WEB` / `BT_PORT_ADMIN` | `3000` / `8080` / `8081` | Host ports (always used for binding; the public origin in ports mode) |
| `BT_API_ORIGIN` / `BT_WEB_ORIGIN` / `BT_ADMIN_ORIGIN` | *(derived)* | Escape hatch: explicit overrides beat derivation for exotic setups |
| `DATABASE_URL` · `REDIS_URL` | `postgres://bt:…@db:5432/bettertrack` · `redis://redis:6379` | |
| `SESSION_SECRET` | 64 random bytes | rotatable (comma-separated array) |
| `SMTP_HOST/PORT/USER/PASS/FROM` | `smtp.gmail.com` / `465` / gmail address / **app password** / `BetterTrack <addr@gmail.com>` | Gmail app password is the documented preset (§6.10); any SMTP works; empty ⇒ email channel disabled, app still runs, sends logged as `suppressed` |
| `ADMIN_EMAIL` / `ADMIN_PASSWORD` | — | seeds the first admin on first boot only |

**Origin derivation** (in `config/env.ts`, single source of truth consumed by CORS, cookies, link generation, the SPA runtime config and nginx templating):
- `subdomains` mode: `https://{BT_SUB_X}.{BT_DOMAIN}` for each service (TLS assumed at the front proxy).
- `ports` mode: `http(s)://{BT_DOMAIN}:{BT_PORT_X}` — one hostname (may be an IP or `localhost`), no subdomains, everything separated by port.

**nginx layout** (`infra/nginx/`): templated at container start from the same env.
- *Subdomains mode:* one nginx with three `server` blocks by `server_name` — `web.*` serves the SPA (user runtime config), `admin.*` serves the same SPA (admin runtime config), `api.*` proxies to `api:3000`.
- *Ports mode:* the same three server blocks keyed by `listen` port instead of hostname.
- In both modes the SPA server blocks inject `window.__BT__` (app kind + API origin) via a generated `config.js` — one web image serves every topology.

**Dev:** `docker compose -f infra/docker-compose.dev.yml up` (db+redis) → `pnpm dev` (vite + tsx watch + worker); defaults to ports mode on `localhost`. Migrations `pnpm db:migrate`; seed creates the first admin + a demo user. **Prod:** `docker compose up -d` in `infra/` — five services (§4.6); deploy = build, migrate, restart.

---

## 12. Testing & Quality

- **Domain layer = the testing crown jewels.** Table-driven Vitest suites for `allocation` (incl. the §6.7 worked example, unreachable weights, budget edges, fractional steps), `backtest` (clipping, FX conversion, carry-forward, drawdown math), `holdings` (avg-cost sequences, oversell rejection) — and post-v1 `alertEval`. Milliseconds-fast, gate every commit.
- **Service/API tests:** Supertest against test Postgres (PGlite fast path / real Postgres integration job), providers mocked with recorded fixtures — auth flows incl. account-kind separation and PIN, ownership scoping, invite lifecycle, **friendship/visibility privacy boundaries** (revocation closes access immediately; non-friends get 404), registration-mode enforcement, progressive rate-limit schedules (violation → cooldown → escalation → decay), search-index ranking and coalescing (concurrent misses ⇒ one upstream call).
- **Contracts:** zod schemas type-checked against handlers; the OpenAPI generator fails CI when a route is undocumented (§6.13).
- **E2E (thin):** one Playwright happy path — login → search (local) → watch → build conglomerate → allocate → add to portfolio → enable friend sharing → second account accepts request and sees the portfolio. Nightly, not per-commit.
- **CI (GitHub Actions):** install → typecheck → lint → unit/service tests → build web + api images. Tags push images.

---

## 13. Milestones — the V1 build order

Encodes the owner's V1 definition (#79): **V1 = BetterTrack Core + Tiny Friend Sharing** — all five tabs exist and look final; some surfaces are designed Coming-Soon placeholders. Each phase ends in something usable; "done" = acceptance criteria met + tests green. Ordering is pragmatic given what already exists (auth, admin, providers/caching v1, search v1, asset page, workboard watchlist, portfolio, backtest engine, jobs — built; conglomerate CRUD in flight).

| Phase | Scope | Done when |
|---|---|---|
| **P0 — v2 shell & restructure** | Final 5-tab navigation + profile dropdown + subnavs; route tree per §7.2 with `ComingSoon` placeholders for every future surface; file moves per §7.3 move table (mechanical); login card top-middle-ish; root docs tidy → `docs/` (§4.2); README pointers updated | The app looks like final BetterTrack: every tab and subnav opens, placeholders say Coming soon, all tests still green after the moves |
| **P1 — Asset catalog, local search & cache rework** | Catalog indexes on `assets` (tsvector + pg_trgm) + seed list; local-first `/search` with ranking + background provider enrichment (`catalog.enrich`); caching hardening to §5.3: coalescing everywhere (in-process + Redis lock), negative caching, serve-stale, per-provider budgets | "bayer", "bay", "bayr" all return BAYN.DE instantly with zero synchronous provider calls; concurrent identical misses produce exactly one upstream fetch (test); provider outage degrades to stale, never errors |
| **P2 — Identity, sessions & topology** | Admin/user account-kind split (§3: admin = management-only, mutual endpoint rejection, no default portfolio, seeds updated); 30-day sessions + PIN option (§6.1); progressive rate limiting (§10); topology env scheme + derived origins + nginx templates for both modes + admin-origin serving + runtime SPA config (§11) | Admin session cannot reach any user endpoint (test) and vice versa; PIN renews the 30-day window; limiter follows the escalation/decay schedule (test); the stack boots in subdomains mode and ports mode from env alone |
| **P3 — Portfolio v2 & multi-portfolio prep** | `portfolio_id`-scoped API (`/portfolios/:id/…`) with one auto-created default; switcher placeholder; overview page blocks: totals, performance chart, holdings, **top winners/losers**, donuts, recent transactions, quick add; `visibility` flag + settings/privacy toggle (flag only — consumed in P5) | Existing portfolio flows work through the scoped API; overview shows winners/losers; a second portfolio row created in SQL appears in `GET /portfolios` without code changes |
| **P4 — Workboard playground: Conglomerates + calculator** | Workboard subnav + overview; finish conglomerate CRUD + **Builder** (locks/auto-balance/normalize/autosave/live preview §6.5) + detail with backtest UI (§6.6); `domain/allocation` + calculator UI + bulk buy-flow (§6.7); Coming-Soon stubs (Comparisons, Saved Ideas, sharing) | Build 13/40/47 Pepsi/Coke/Bayer, activate, 5Y backtest vs S&P 500 with clipping notice; 1000 € → buy list ≤ budget with deviation table → recorded in portfolio in two clicks |
| **P5 — Social minimal** | `friend_requests` + `friendships` schema; request by username/email (no enumeration), accept/decline/cancel/remove; Friends page; portfolio `visibility=friends` consumption: Shared With Me + read-only friend portfolio view; My Shared Items | Two-account flow works end-to-end; privacy tests: non-friends 404, visibility-off 404, unfriending instantly closes access |
| **P6 — Notifications & the email log** | Notification bell + unread + mark-read (REST refetch; no socket); `notification_settings` toggles; types `friend.request`/`friend.accepted`/`portfolio.shared` wired to P5 events; Gmail app-password SMTP preset; `email_log` on every send; admin per-user + global email log views | A friend request produces a bell item and a logged email within seconds; admin sees that user's email log; SMTP-less deploys log `suppressed` and never crash |
| **P7 — Settings section** | Settings subnav per §6.11: Account (incl. privacy/sharing toggles), Notifications, Security (sessions info, PIN management) implemented; Imports & Exports, Connections, Backups, API Access as designed placeholders; profile dropdown finalized | Every settings subpage is reachable; implemented pages function; the rest render designed Coming-Soon states |
| **P8 — Admin global settings & registration modes** | `app_settings` + admin Settings page; registration-mode selector (closed enforced; invite-token/approval/open visible, disabled, Coming soon) with enforcement reading the setting from day one; beta-mode placeholder; admin overview cards refresh (emails 24 h, queue health) | Settings persist and audit-log; registration behavior provably follows the stored mode (closed blocks a hand-crafted register call); selector shows all four modes |
| **P9 — API `/docs`** | OpenAPI 3 generation from contracts; `GET /openapi.json` + human-readable `GET /docs` on the API origin; CI coverage gate (every route documented) (§6.13) | `api.<domain>/docs` renders complete, accurate endpoint docs; CI fails on an undocumented route |
| **P10 — Polish, e2e & v1 gate** | Empty states/skeletons/error boundaries sweep; responsive pass; disclaimers; deploy guide for **both** topology modes; backups verified; Playwright e2e (§12); pre-release Fable review of the whole branch | Fresh invite → first stock → conglomerate → buy list → friend share, on a phone browser, without confusion; `docker compose up` works from the README alone in either mode; e2e green |

**V1 scope summary:** IN V1 = everything marked ✔ above. **Coming-Soon placeholders** (visible, routed, designed): multi-portfolio UI, portfolio widgets/custom layout, broker CSV imports, watchlist/conglomerate sharing, comparisons, saved ideas, asset category browsers, Live Mode, social Ideas/public profiles, settings Imports/Connections/Backups/API-Access, non-closed registration modes, Invite-Others/Share-Profile menu items. **Post-v1 (no placeholder needed):** alerts + evaluator, realtime gateway, share links, JSON export/import, API keys/OAuth, 2FA, Firebase/mobile, finance/expense tracking (§14).

**After V1:** when all P0–P10 acceptance criteria are met, the factory planner creates a **"check v1"** issue labeled `awaiting-owner` (not `autopilot`) and **pauses feature planning for owner feedback** — per the owner's directive, it may meanwhile queue only bug-fix/hardening work, never new §14 features. **Done 2026-07-05:** the owner's sign-off arrived as feedback #248; the V2 build order (§13.2 below) is the owner's directive. The composer resumes normal planning against §13.2, decomposing phases in small batches exactly as it did for §13.

---

## 13.2 Milestones — the V2 build order

Owner-approved 2026-07-05 from the v1 feedback (#248). **V2 = fix everything the v1 feedback found + Cash balance + Multi-portfolio UI + Sharing expansion + Notifications v2 + Self-service reset + 2FA (TOTP + email codes).** Future ideas from #248 not listed here are parked in §14.

**Binding planning rules (from the v1 cost ledger):**
- The composer sizes every work item **S / M / L** before filing.
- **S-items never ship alone.** Compose them into **surface bundles**: one issue/PR per bundle, 3–6 items, ONE surface/package, ONE tier, acceptance criteria as per-item checkboxes.
- **Bundle escape hatch:** if a single bundle item draws blocking review findings twice, the writer spins it off into its own issue and the bundle merges without it.
- M/L items ship standalone. Every composed issue carries `mf-meta` touch claims so disjoint arcs run in parallel on the multi-factory.

| Phase | Scope | Done when |
|---|---|---|
| **V2-P0 — Quick-win bundles (S×4, sonnet)** | (a) Branding: passion tagline replaces the "not investment advice" footer app-wide (e.g. "BetterTrack — finances under your control"; final line owner-tunable) + top-bar bell icon restyle; (b) Graph polish: cleaner display-option controls + timescales 1D/1W/1M/3M/1Y/Max; (c) Auth polish: clear all client state on logout (no stale name/email after account switch) + common-password blocklist (common-passwords list only, NO complexity rules) + slightly lower minimum length; (d) Social design: clear visual hierarchy between section headings (Incoming/Pending) and the actual request entries | Four bundles merged; "password1234" rejected on user creation (test); logout → login as another user shows no stale data (test); graph offers 1D…Max |
| **V2-P1 — Search, watchlist & asset page rework (M/L arc, sonnet/opus)** | Search results get direct actions: Portfolio → inline Buy/Sell dialog, Conglomerate → add flow, watchlist icon that adds **in place** (state-aware: shows already-added, no error-on-reclick, does not close the search); result rows redesigned; clicking a result (incl. Ctrl+K) opens the asset page; search works from 1 character (ticker "V") with debounce; watchlist page refetches on mount/add (no manual reload); asset page redesign: actions reachable near the top, watchlist as a small state-aware icon button, "Workboard" naming cleaned up where it means watchlist; UI designed multiple-watchlists-ready (one-click add to General + a specific-list picker affordance) | Owner flow works first-try: search "V" → open the Visa asset page → icon-add to watchlist → item appears on the watchlist page without manual reload |
| **V2-P2 — PIN app-lock (M, opus)** | PIN actually enforces (WhatsApp model): lock overlay on app open when enabled; optional AFK auto-lock after configurable idle minutes (Settings → Security); wrong-PIN attempts rate-limited per §10; sessions stay valid per §6.1 — the lock gates the UI, not the session | Reload with PIN enabled → lock screen before any data renders; AFK timer N min → locks after N idle; PIN off → never asks (tests) |
| **V2-P3 — Admin rework (M/L, opus)** | User detail view: profile + every user action in one place (send test email, deactivate, delete, edit username/email, working password reset) + per-user audit log + per-user email log; user list slimmed to essential columns (no early horizontal scroll, phone-friendly); bulk select + bulk actions (disable, …) | Every user action reachable from the detail view; editing email/username persists and audit-logs; bulk-disable works; list fits phone width |
| **V2-P4 — Auth architecture: admin/user separation + reset overhaul (L, opus build, fable review)** | Admin and user become truly separate systems: no `/admin` on the user web origin in either topology mode, separate session surfaces, mutual endpoint rejection re-verified; password-reset overhaul fixing both #248 bug chains: reset outcome independent of any admin session elsewhere (no context leakage), a completed reset never asks for the password twice, admin accounts recoverable via a working admin reset path; self-service reset via email (tokened flow, pulled from §14) | Neither #248 reset bug chain is reproducible (regression tests for both); the user origin serves no admin surface in either mode; a user self-resets via email end-to-end |
| **V2-P5 — 2FA (M/L, opus)** | TOTP + email-code channel (owner wants both); enroll/disable in Settings → Security; challenge at login when enabled; recovery codes; email codes through the existing email channel + email_log | Enable TOTP → login requires the code; email-code path works and logs to email_log; a recovery code unlocks; disable works |
| **V2-P6 — Portfolio cash balance (M/L, fable domain core)** | The #220 §14 note in full: per-portfolio cash ledger, deposit/withdraw, "Pay from cash balance" / "Add proceeds to cash balance" with live cash-after preview and no silent negative balances, sticky per-portfolio default, cash as a first-class overview line, TWR correctness: cash-funded buys are NOT external flows | Deposit → buy from cash → overview reconciles (current cash = sum of movements; test); performance-% curve unaffected by internal cash→stock conversion (test) |
| **V2-P7 — Calculator & Workboard upgrades (M, fable domain + sonnet UI)** | Opt-in "**at least 1 share**" mode (toggle, default OFF): a position whose weight can't afford one whole share within budget gets exactly one share, remainder rebalances across the rest by their weights (existing behavior for share-price > whole budget stays); calculator → "Add to portfolio" flow: buy list lands in the portfolio in a couple of clicks (incl. pay-from-cash once V2-P6 is in); clarify the conglomerate "Activate / Re-activate" concept in the UI (what "active" means, or rename it) | The €240-share-on-€1000-budget case buys 1 share only with the toggle ON (test); a calculated buy list can be committed to a portfolio in ≤3 clicks; an owner-naive user can tell what Activate does |
| **V2-P8 — Multi-portfolio UI (M, sonnet/opus)** | Create/rename/archive portfolios, switcher in the portfolio header, per-portfolio routing (the data model ships ready since V1); default-portfolio semantics preserved | Two portfolios switchable everywhere; archived portfolios hidden but restorable; the V1 scoped-API tests keep passing |
| **V2-P9 — Sharing expansion (L, opus privacy)** | Share conglomerates + watchlists to friends (read-only views, revocable), integrated into Shared With Me and the §14 friend-rows direction; default-visibility setting (Everyone-of-my-friends / selected friends / Private — exact semantics planner-refined against §6.9) | Privacy tests at P5 grade: non-friend 404, revoke instantly closes access; owner shares a conglomerate and a watchlist to a friend end-to-end; visibility default applies to newly created portfolios |
| **V2-P10 — Notifications v2 (M, opus)** | Per-type channel matrix in settings: each notification type → in-app bell / email / both / muted; diagnose and fix the missing friend-request notifications reported in #248; fan-out respects the matrix everywhere | Matrix persists per user; a friend request honors bell-only, email-only and muted (tests); friend-request notifications provably arrive again |
| **V2-P11 — Polish, e2e & v2 gate** | Extend the Playwright happy path with the new flows (search → asset page → watchlist; cash-funded buy; share a watchlist); empty/error-state sweep over all new surfaces; pre-release Fable review of the whole V2 branch; then file **check v2** (awaiting-owner) and pause feature planning per the V1 protocol | Nightly e2e green incl. the new flows; Fable review findings fixed; check-v2 filed |

**After V2 (done 2026-07-06):** the owner signed V2 off in chat the same day the final gate merged; his feedback was captured live into the V3 scope below (sign-off recorded on #317). §13.3 is the owner's directive — the composer resumes normal planning against it, decomposing phases in small batches exactly as it did for §13 and §13.2.

---

## 13.3 Milestones — the V3 build order

Owner-approved 2026-07-06, selected interactively from the full candidate menu (30 items in; explicitly parked for later, all still specced in §14: backtest custom-benchmark + side-by-side stats table, rebalanced backtest mode, broker CSV imports, Telegram/Discord channels, Google login + passkeys). **V3 = Net Worth & cash sources + tax engine (AT) with dividends & realized P/L + Analytics deep-dive + sharing audiences everywhere + multiple watchlists + social expansion (friend rows, public profiles, chat) + realtime & Live Mode + price alerts + backtest late-listing modes + i18n EN/DE + platform (PWA + web-push, session manager, FCM prep) + go-live surfaces for bettertrack.at.**

**Binding planning rules** (carried from §13.2, still in force): the composer sizes every item **S / M / L**; S-items ship only in **surface bundles** (3–6 items, ONE surface/package, ONE difficulty, per-item checkbox criteria, spin-off escape hatch after 2 blocking review rounds); M/L standalone; every composed issue carries `mf-meta` touch claims. Work is labeled with **difficulty** (`diff:easy…diff:max`) per docs/multi-factory.md — the legacy tier names map sonnet→easy/normal, opus→intermediate, fable→hard/max.
**New V3 rules:**
- **i18n discipline:** every phase after V3-P1 writes user-facing strings through the i18n layer with EN + DE keys in the same PR; a hardcoded string is a blocking review finding.
- **Privacy friction ladder (§16):** any control that can expose data beyond specific friends ships its warning — strong explicit-acknowledgment warning for `public`, light confirm for `all friends`, none for `specific friends`.

| Phase | Scope | Done when |
|---|---|---|
| **V3-P0 — v2 feedback quick wins (S bundle, diff:normal)** | (a) **BUG:** selecting a non-default portfolio and then opening Transactions or Custom Assets silently switches back to the default portfolio — root-cause the selection/route state loss and fix it, don't paper over; (b) rename **"Total Value" → "Net Worth"** app-wide (overview, graphs, shared/read-only views, emails, fixtures); (c) **liquidity display redesign:** replace the invested-vs-liquid percent bar with a design that belongs in the overview (owner: it "looks out of place"; e.g. fold into the stat tiles or a subtle ring — design judgment, same data); (d) **calculator precision stepper:** the amount stepper stops counting 1, 2, 3 — it steps at a selectable decimal precision (1 / 0.1 / 0.01 / 0.001), "how far off the comma you want"; (e) **BUG (owner, 2026-07-07):** withdrawing the full cash balance leaves €0.01 behind — root-cause the rounding/validation in the cash ledger (exact cents math, no float residue) so a withdraw-all lands at exactly €0.00 | (a) is a passing regression test; zero "Total Value" strings survive (grep gate); the calculator steps amounts at 0.01 granularity; withdraw-all leaves exactly €0.00 (regression test); (c) queued for owner-eye check at the v3 gate |
| **V3-P1 — i18n foundation (L, diff:intermediate)** | Locale-file architecture: **EN is the source of truth and default, DE the first full translation**; runtime language picker in Settings (per-user persisted); extraction of ALL existing UI strings; locale-aware date/number formatting; email templates localized; DE machine-seeded now, human-eye corrected at V3-P13. Adding a future language must be **one locale file + a registry entry**, zero code edits (documented) | Picker persists per user; nav/portfolio/workboard/settings/auth fully switch to DE; the add-a-language recipe is documented and provably code-free; CI gate fails PRs introducing hardcoded UI strings |
| **V3-P2 — Custom assets v2 (M, diff:intermediate)** | (a) **User categories:** custom assets take a real category (the catalog taxonomy: stock / ETF / crypto / commodity / cash-like / other) at create/edit; the CUSTOM slice/grouping disappears everywhere — a custom "stock" counts as Stocks in donuts, category views and analytics filters; migration maps existing custom assets to `other` + one-time re-categorize banner; (b) **value smoothing:** per-asset toggle (default off) interpolating linearly between value marks instead of step-jumps, applied in every series reconstruction, adjustable any time | A custom "stock" appears under Stocks in the donut (test); smoothing changes the curve between marks while every mark-day value stays exact (test); no CUSTOM category remains in UI or API responses |
| **V3-P3 — Cash sources (L, diff:max — extends the V2-P6 cash core)** | The V2 cash ledger becomes **Main cash** and gains named siblings: create/rename/archive **cash sources** ("Bank account X", "Retirement fund Y"; type label bank / retirement / cash / custom), each with own balance + movement history; **transfers** between sources (atomic paired movements, double-entry style); buys/sells keep defaulting to Main (sticky per-portfolio default stays) with a source picker on deposit/withdraw/buy/sell; **set balance** (owner, 2026-07-07): besides deposit/withdraw, every cash source offers "set balance to X" — the app computes the signed difference itself and records it as a normal movement (audit trail intact, no head-math when updating to what the bank says); overview + Net Worth roll up all sources; the liquidity view shows the split; V2-P6 TWR rules extend to sources — internal transfers are NEVER external flows | Transfer 500 € Main→Bank: paired movements in both histories, Net Worth unchanged (test); setting a source from €123.45 to €200.00 records a +€76.55 movement and the balance reads exactly €200.00 (test); per-source reconciliation (balance = Σ movements) holds after a cash-funded buy from a named source (test); performance-% curve unaffected by any internal transfer (test) |
| **V3-P4 — Realized P/L, tax engine & dividends (L, diff:max domain core; depends on V3-P3)** | (a) **Realized-P/L engine:** moving-average cost basis per position (Austria-correct), realized gain/loss on every sell, per-calendar-year aggregation; (b) **Tax mode** (Settings → Taxes): `None` (default, exact current behavior) / `Manual per trade` (every sell + dividend offers an optional tax amount-or-% entry, recorded + reported, zero automation) / `Country-specific` with a country picker shipping **Austria only**: flat 27.5 % KESt on each sell's realized gain and on dividends, **losses offset gains within the same calendar year** — a loss sell refunds tax already paid that year down to the year's net position; the year resets hard on Jan 1, **no carry across years** (a February loss cannot offset last November's gain — §16); tax withholdings/refunds post as distinct cash movements against the trade's cash source; (c) **Dividends:** dividend transaction type (holding, date, gross amount) landing in a chosen cash source, tax-mode aware per (b); (d) **Reports:** per-year Realized P/L + Dividends + Taxes summary page per portfolio with per-position drill-down. Mode switches never rewrite history — they apply forward from the switch (planner refines the exact cutover semantics and logs them in §16) | The owner's example is a required integration test: +450 € taxed gains, then a −100 € loss sale → total tax held for the year = 27.5 % × 350 €, refund movement visible; Nov-gain/Feb-loss produces NO offset (test); dividends appear in the cash-source history and the year report; `None` mode is byte-identical to v2 behavior (regression) |
| **V3-P5 — Sharing v3: audiences everywhere (L, diff:intermediate privacy; two arcs)** | (a) **Audience model:** ONE shared picker + enforcement layer for every shareable thing — **each portfolio** (not only the default), each conglomerate, each watchlist — audiences `private` (default) / `specific friends` (multi-select) / `all friends` / `public link` (≥128-bit revocable token URL per the §14 share-links spec; revoke kills instantly); lossless migration of V2 sharing data; the **friction ladder** lives in this picker (strong explicit-acknowledgment warning on public — "anyone with the link sees your holdings and net worth", light confirm on all-friends, none on specific); (b) **Multiple watchlists:** create/rename/delete named lists (General stays default; the V2-P1 multiple-watchlists affordances go live), per-list audience via (a), list picker in every add-to-watchlist flow | Privacy tests at V2-P9 grade over every kind × audience combination (non-friend 404, revoked link dead, unfriending instantly closes specific/all-friends access); a second portfolio shared to exactly one friend works end-to-end; two watchlists with different audiences behave independently; the public confirm cannot submit without the explicit acknowledgment (test) |
| **V3-P6 — Social surfaces: friend rows + public profiles (M/L, diff:intermediate; depends on V3-P5)** | (a) **Expandable friend rows** per the §14 design: Friends stays a list; a row expands in place to the per-friend panel — everything they share with me grouped by kind with direct links, everything I share with them, and friend actions (remove; nickname/mute if cheap — planner's call); collapsed rows show kind counts ("2 portfolios · 1 watchlist"); Shared With Me stays as the item-centric view; (b) **Public profiles:** opt-in public page (username-slugged URL) composing chosen public-audience items + a bio line; enabling passes the strong warning; disabling unpublishes instantly | Expanding a friend row answers "what did this friend share with me" in one click (e2e); the profile URL renders logged-out with exactly the chosen items and 404s when disabled; a non-public item can never render on a profile (sweep test across kinds) |
| **V3-P7 — Realtime gateway + Live Mode (L, diff:hard infra; two arcs)** | (a) **Realtime gateway** per §4.5: authenticated WebSocket bridged to the event bus — notification/bell push without refetch, quote push for open asset/analytics views, shared-view invalidation; polling fallback stays (flagged rollout); (b) **Live Mode v1** per §6.3: shared per-asset polling loop → Redis ring buffer → `asset:{id}` fan-out, windows 1 m–12 h, auto-stop on idle, asset-page live chart entry | Two browsers: a friend request pops the second browser's bell without refetch (e2e); Live Mode on one asset streams to two concurrent viewers off ONE upstream loop (provider-call-counter test); gateway down ⇒ graceful poll fallback, zero user-visible errors |
| **V3-P8 — Friend chat (L, diff:intermediate; depends on V3-P7a)** | 1:1 friend DMs: conversation list + thread, unread badges, realtime delivery over the gateway (poll fallback), new notification type `chat.message` honoring the V2-P10 matrix, **share-in-chat:** send an asset reference or one of your shareable items as a rich chip — **sending never widens access**, the recipient resolves the chip only if its audience already allows them; no groups/reactions/read-receipts in v3 | Two accounts chat live in two browsers (e2e); a private-item chip shows the recipient a "not shared with you" state, never data (test); unread counts survive reload; muted chat notifications stay silent (test) |
| **V3-P9 — Analytics deep-dive page (L, diff:hard web + domain; depends on V3-P2)** | New **Portfolio → Analytics** sub-page — "see anything in the most detail": configurable main graph (per-asset hide/show, include/exclude by category/type incl. custom categories, free date range + standard scales, value vs performance-% modes, smoothing-aware); per-asset contribution table (value, cost, P/L, weight, contribution to period change); the **overlay-assets mode moves here from the overview** (the overview keeps only the simple curve — owner: "too technical" there); **compare mode:** overlay ANY benchmark — catalog asset/index via local search, another of the user's portfolios, or a conglomerate (backtest-priced) — with side-by-side stats (total %, CAGR, max drawdown, best/worst day) for both series; **inflation mode:** real-terms normalization against AT/EU HICP, US CPI, or a custom flat %/yr (composes with performance-%; a small maintained static monthly series is acceptable for v3 — source planner-refined) | Hiding an asset recomputes curve + stats live (test); compare vs an index AND vs a second portfolio both render side-by-side stats; a custom 10 %/yr inflation rate visibly bends a flat nominal curve downward in real mode (test); overlay mode is gone from the overview and lives here |
| **V3-P10 — Backtest modes, alerts & market data (M/L, four disjoint arcs)** | (a) **Late-listed constituent modes** (diff:max, engine) — the full §14 design: backtest mode selector `clip` (current, stays default) / `cash until listing` / `redistribute until listing` (equal split, entry-day rebalance to target weights), applied per late asset independently; chart entry markers + mode-adaptive notice; idle-cash visibility in cash mode; benchmark always runs the full window; builds the **entry-day rebalance primitive** (scheduled rebalancing itself stays parked); (b) **Price alerts** (diff:intermediate) — the §14 spec verbatim: `price_above/below`, `pct_up/down_from_ref`, `pct_day_up/down`, minute evaluator on cached quotes, one-shot vs repeat (24 h cooldown), idempotency key per (alert, window), Workboard alerts panel + asset-page inline alerts, delivery via the notification matrix incl. the realtime bell; (c) **Commodities & crypto** (diff:easy): register gold/silver (`XAUEUR=X`, `GC=F`) + crypto (`BTC-EUR`, `ETH-EUR`) through the provider abstraction; category pages fill; (d) **Per-user base currency** (diff:hard, §5.3 core): Settings picker (EUR default; initial set ≥ USD/CHF/GBP, planner-refined), every valuation/graph/report renders in the user's currency incl. historical series conversion | The SpaceX-case conglomerate backtests in all three modes with visibly different results + entry markers (snapshot test); an above-price alert fires exactly once per window (idempotency test) and lands per the matrix; BTC-EUR is searchable and chartable; a USD user sees consistent $ everywhere incl. the tax report (test) |
| **V3-P11 — Platform & security (M/L, diff:intermediate; three arcs)** | (a) **Session manager** (Settings → Security): active sessions list (device/browser label, created, last-seen, current marker), revoke one ("log out that device") + revoke-all-others; (b) **PWA + web-push:** manifest, icons, service worker (precached shell, instant loads, offline read-only cached state, install hint) **plus a web-push notification channel** (VAPID; opt-in from notification settings only — no unsolicited permission prompt; a "browser push" column in the matrix; desktop/Android first-class, iOS best-effort); (c) **Mobile push prep:** `device_tokens` table + authenticated register/refresh/delete endpoints + an **FCM `NotificationChannel`** plug-in behind the standard channel interface, env-gated like SMTP (no Firebase config ⇒ channel invisible, nothing crashes); "phone push" matrix column appears only when configured. Server-side only — the phone app itself is a separate future project (§14 Mobile) | Revoking a session logs that browser out on its next request (two-session e2e); the app is installable (Lighthouse PWA pass) and an offline reload shows the shell + cached portfolio read-only; with push enabled, a friend request raises a desktop OS notification with the tab closed (manual + channel-log test); with a mock FCM config a notification records an FCM send (integration test), with none the column is absent |
| **V3-P12 — Go-live surfaces for bettertrack.at (M, diff:normal)** | (a) **Product landing page** at the root origin: small, fast, static marketing page — what BetterTrack is, 3–4 feature highlights with real screenshots, dark aesthetic matching the app, link to web.bettertrack.at, "invite-only" note; ships as its own tiny static site in the compose stack; (b) **mobile.bettertrack.at placeholder:** one elegant page — "the BetterTrack app is in development, use the desktop web app meanwhile" (this origin later becomes the store redirect); mobile user-agents on the login page may be pointed here (planner-refined, never a hard block); (c) **Production topology config** (§11 extension): the 5-origin subdomain layout `bettertrack.at` / `web.` / `admin.` / `api.` / `mobile.` behind Cloudflare — nginx templates, env derivation, CORS/cookie domains for the new origins, deploy-guide update for the always-on-Mac host (Cloudflare-fronted; tunnel vs port-forward is a go-live ops decision, document both) | The stack boots the 5-origin mode from env alone; product + mobile pages render fast and pass the i18n gate (EN/DE); web/admin/api behave identically to the current subdomains mode (existing topology tests keep passing) |
| **V3-P13 — DE sweep, polish, e2e & the v3 gate** | Human-eye German pass over EVERY surface incl. all v3 strings (correcting the V3-P1 machine seed; du-Form, consistent finance vocabulary); empty/error/loading-state sweep over all new surfaces; responsive pass; extend Playwright: cash-source transfer → AT-mode sell with refund → analytics compare → share-to-one-friend → second watchlist → chat message → alert fire (mocked quote); pre-release Fable review of the whole V3 branch; then file **check v3** (`awaiting-owner`) and pause feature planning per the V1 protocol | Nightly e2e green incl. all new flows; no untranslated string on any surface in DE mode (sweep checklist); Fable findings fixed; check-v3 filed |

---

## 13.4 Milestones — the V4 build order

Owner-approved 2026-07-07 (owner selected every COD-starred item from the post-v3 menu; everything unpicked moved to §13.5). **V4 = mobile platform enablement + running-a-real-product operations + the Workboard's analytical completion + social/notification rounding.** Execution playbook: `factory/V4_KICKOFF.md`. Scope theme: *make the phone app possible, make the public site a real product.* **P0 family filled 2026-07-15** from the owner's check-v3 feedback (#459): row V4-P0 plus three feedback arcs V4-P0b/P0c/P0d; one feedback item went to the mobile app track (its board #36) and one pulled V6-2 forward to v5 (V5-P13b).

**Parallel track:** the Android app itself is built OUTSIDE the factory (owner-driven Gemini/Android-Studio flow per `~/Desktop/Coding/BETTERTRACK_APP_EXECUTE_THIS.md`, starting after v3). **V4-P1…P3 are its platform blockers — the composer schedules them before everything except the V4-P0 quick-win bundle; the V4-P0b/c/d feedback arcs queue after P3.** The Chief supports the app track by answering integration questions and filing API-gap issues as they surface.

**Binding rules carried from §13.2/§13.3, still in force:** S/M/L sizing with S-only-in-surface-bundles, mf-meta touch claims on every issue, **i18n discipline** (every user-facing string ships EN + DE keys in the same PR; hardcoded string = blocking review finding), **privacy friction ladder** (strong ack for `public`, light confirm for `all friends`, none for `specific friends`).

**Owner-provided items (none block launch — all env-gated until provided):** Firebase project + service-account key (P3), Google OAuth client credentials (P4), Drive/rclone credentials for offsite backup (P6), optional Telegram bot token (P10), one physical Android device test at the gate (P3).

| Phase | Scope | Done when |
|---|---|---|
| **V4-P0 — v3-feedback quick wins (S/M bundle, diff:normal)** | Filled 2026-07-15 from the owner's check-v3 feedback (#459 — bigger asks became rows V4-P0b/P0c/P0d): (a) **PIN entry hardening:** PIN digits must NEVER render — masked dots only (today each digit flashes briefly), and the lock screen captures keystrokes page-wide (typing anywhere lands in the PIN boxes — implicit focus, backspace edits); (b) **portfolio time spans:** add **1D, 1W, 5Y** to the portfolio chart range picker (graceful when history is shorter); (c) **inflation-mode BUG + transparency:** premade presets (AT HICP, EU HICP, US CPI) do not deflate the series — custom flat % works, presets must too; show each preset's effective %/yr next to it; (d) **cash-source action UX:** deposit/withdraw/transfer/set-balance get icons (icon+label or icon-only — whichever reads better); (e) **registration legal consent:** register form states "By signing up you agree to …" linking Terms, Privacy, Impressum, Cookie note (EN+DE; the versioned re-accept flow stays V6-5); (f) **sign-up prominence:** replace the bottom anchor "Don't have an account? Create one" with a designed, stand-out "… Sign up" treatment whose layout already reserves the spot for the P4 Google button; (g) **remember-me always on:** the "remember me" (username) toggle disappears — a successful login always remembers the name; the separate "stay signed in" choice remains; (h) **Social tab order:** friends list to the top, pending/sent requests to the bottom | Every item carries a regression test or an owner-eye check queued for the v4 gate; (a) tests that the DOM never contains a plain PIN digit; (c) unit-tests preset deflation against fixture index series |
| **V4-P0b — Follows rework: into the Friends tab (M/L, diff:intermediate, social; owner 2026-07-15)** | The standalone **Following page is retired** (the v3 #438/#439/#455 server model stays; UI + one server rule change): following lives in the **Friends tab** — expanding a friend row opens a profile-in-place: everything they share with me (existing dropdown/groups), plus a **"News about this person" follow toggle** and (when the friend shares alert activity) the alert-follow switches. Following a FRIEND must work **without a public profile** — the server permits person-follows on any friend; public profiles remain the follow path for non-friends. The per-user "share my alert activity with followers" setting (#455 alerts/sharing) gets a proper home in Settings; **an item turning public always notifies followers** (owner-specified, via existing follow.published). Old /following routes redirect to Friends. Phone guidance: same content on the app's Friends subpage (board note) | Following nav entry + page gone, /following redirects (e2e); a friend WITHOUT a public profile is followable from their row and a newly-public portfolio fires follow.published (e2e); alert-follow switches render in the row expansion and honor the sharer's setting (test); social suite fully green (no capability regression) |
| **V4-P0c — Notification UX: lean defaults, archive-on-read, deep links (M, diff:intermediate; owner 2026-07-15)** | (a) **Lean email defaults:** email OFF for every notification type EXCEPT the account/security category — for new accounts AND a one-time migration of existing users (announced in-app; exact re-enable semantics planner-refined and §16-logged); bell/in-app defaults unchanged; (b) **read = archive:** reading a notification archives it — inbox shows unread only, an Archive view keeps history (bulk actions stay); (c) **click-through deep links:** every notification type navigates on click — price alert → the triggering asset/alerts panel, friend request → Social requests, chat → the DM thread, share/follow news → the item or person, account/security → the matching settings page; the route-key contract lands in docs/mobile-push.md so FCM payloads (P3) carry identical deep-link keys | New account: only account-category email enabled (test); migration flips exactly the intended rows (test); read archives + Archive lists it (e2e); every type click-lands on its target (e2e matrix); deep-link keys documented in the P3 contract |
| **V4-P0d — Admin: chat ban, account defaults, IA cleanup (M, diff:intermediate, admin; owner 2026-07-15)** | (a) **Per-user chat ban:** admin toggle on user detail; banned user cannot send DMs (server-enforced with a neutral in-app notice; existing threads stay readable; unban restores instantly); (b) **account-defaults panel:** admin configures what NEW accounts start with — default notification matrix (pre-seeded with the P0c lean default), chat enabled y/n, portfolio starter defaults (e.g. default visibility), and a stored **developer-status default** consumed when V6-9 ships (inert until then); applies at registration, never retroactively; (c) **light admin IA regroup:** order the grown admin surface into sane sections/nav — structural tidy only, the deep redesign stays V6-1 | Banned user's send returns 403 even with a valid session/token and the UI explains it (test + e2e); a changed default applies to the NEXT registration only (test); defaults panel + regrouped nav pass owner-eye at the gate |
| **V4-P1 — Mobile API enablement I: scopes & bearer parity (M/L, diff:hard, security)** | (a) **Scope expansion:** extend `packages/contracts` API_KEY_SCOPES beyond portfolio/workboard r+w, market:read, social:read — add `social:write`, `notifications:read`/`notifications:write`, `account:security`, `chat:read`/`chat:write` (exact naming planner-refined, §16-logged); enforcement in the bearer middleware per route group; scope pickers (Settings → API Access, admin OAuth apps, OAuth consent screen) show the new scopes with plain-language descriptions; strictly additive — existing tokens keep working; (b) **Bearer parity:** every route group the phone needs — settings, security (password change, 2FA enroll/manage), sessions, notifications (list/read/prefs), chat, friends/sharing writes — accepts Bearer auth with correct scope checks (writer inventories current cookie-only coverage in the PR description first); CSRF remains cookie-only concern | A bearer token with `chat:write` sends a DM (test); one without `notifications:read` gets 403 on the inbox (test); a route×scope matrix test covers every group; zero cookie-session flows regress (suite) |
| **V4-P2 — Mobile API enablement II: idempotency, account chooser, deletion (M/L; three arcs)** | (a) **Idempotency keys** (diff:hard): all portfolio mutation endpoints (transaction create/edit/delete, cash deposit/withdraw/transfer/set-balance, custom-asset value points) accept a client-supplied `Idempotency-Key` UUID; server persists key→response ≥48 h per user and replays the stored response on duplicates — the backbone for the app's offline FIFO queue (mobile SPEC §7); (b) **OAuth account chooser** (diff:normal): the authorize page ALWAYS interposes "signed in as X — Continue / Use another account", INCLUDING first-party auto-approve clients (auto-approve skips the permission prompt, never the account confirmation — owner directive 2026-07-07; Custom Tabs share browser cookies); "Use another account" = logout → login → back into the flow; (c) **Account deletion** (diff:hard): Settings → danger zone; re-auth (password or fresh 2FA) + typed confirmation; hard-deletes account, portfolios, social edges, tokens/grants, notifications; chat messages anonymized for the partner (exact semantics planner-refined, §16-logged); all sessions revoked. Google Play requires in-app deletion — API + web UI now, the app links to it later | Same UUID twice → exactly one movement and byte-identical response (test); chooser renders for a first-party client (e2e); post-deletion, a schema-wide sweep finds zero rows keyed to the user and login is dead (test) |
| **V4-P3 — FCM push go-live (M, diff:intermediate; depends on V3-P11c)** | Turn the V3-P11c prep real: wire FCM server credentials (owner-provided Firebase service account; env-gated like SMTP), token-invalidation handling (FCM "unregistered" → delete the device-token row), and write `docs/mobile-push.md` for the app track (token obtain/refresh/delete flow, payload contract, deep-link keys). Manual end-to-end test to one physical Android device at the gate | With mock credentials an FCM send records correctly (integration test); without config the channel stays invisible and nothing crashes (regression); a real device receives a push (owner-verified at the gate) |
| **V4-P4 — Registration modes + Google login + passkeys (L, diff:hard, auth; two arcs)** | (a) **Registration modes live** (§6.12 design): admin-switchable closed / invite-token / approval-queue / open; invite tokens admin-generated (single/multi-use, expiry); approval queue holds applicants pending with admin approve/reject + decision email; product landing + login/register surfaces reflect the active mode (i18n); (b) **Google login** (OAuth sign-in; links to an existing account by verified email or registers a new one subject to the active registration mode) + **passkeys** (WebAuthn register/login, multiple named passkeys per account, managed in Settings → Security alongside 2FA) | Mode matrix test: each mode gates registration correctly incl. token expiry and approval mail; a Google-linked account signs in via Google AND via password; a passkey login succeeds with a virtual authenticator (e2e); closed mode still blocks everything (regression) |
| **V4-P5 — Product ops: error tracking + announcements (M; two arcs)** | (a) **Sentry** (diff:intermediate): api + web SDKs, env-gated DSN, release tagging, PII scrubbed (no emails/tokens in events — unit-tested scrubber), configurable sample rates; **bull-board** (or equivalent BullMQ inspector) mounted admin-only; **admin health page**: DB/Redis/provider/queue/gateway status, app version, uptime; (b) **Announcements** (diff:normal): admin composes title/body/severity/active-window in EN+DE → dismissible in-app banner + notification-inbox entry for all users; dismissal per user | A thrown test error lands in Sentry with release tag and zero PII (manual + scrubber unit); health page reflects a stopped Redis within 30 s (test); an announcement reaches every user and stays dismissed per user (e2e) |
| **V4-P6 — Data lifecycle: export + offsite backup (M; two arcs)** | (a) **Account data export** (diff:intermediate): Settings → "Export my data" → async job builds a zip (JSON of every user-owned entity + CSVs for transactions/cash movements/holdings) → notification with expiring re-auth-gated download link; rate-limited 1/day; (b) **Offsite DB backup** (diff:normal, ops): extend the live-deploy backup sidecar — after each daily pg_dump, encrypt (age or GPG) and upload to Google Drive via rclone (owner-provided credentials, env-gated), 30-day remote retention, documented restore drill in `docs/ops.md` | Export zip covers every user-owned table incl. cash-source movements and tax rows (completeness sweep vs schema); an encrypted dump lands in Drive and a documented restore succeeds onto a scratch DB (drill at the gate) |
| **V4-P7 — Backtest: custom benchmark + rebalanced mode (L, diff:max, engine; depends on V3-P10a)** | The two parked §14 designs, verbatim: (a) **Custom benchmark:** benchmark = any catalog asset/index (local search; current presets stay one-click) or any of the user's conglomerates (runs through the same engine with the same window/settings incl. late-listing mode — apples-to-apples); every bottom-panel stat (total return %, CAGR, max drawdown, best/worst day, …) rendered side-by-side conglomerate vs benchmark with an optional delta column; one benchmark at a time; (b) **Rebalanced backtest mode:** monthly/quarterly/yearly rebalancing to target weights, generalizing the V3-P10a entry-day primitive (shared code, not duplicated); mode-adaptive notice + optional rebalance markers; composition with late-listing modes planner-refined and §16-logged | Conglomerate-vs-conglomerate benchmark shows two full stat columns + delta (snapshot test); a yearly-rebalanced 60/40 two-asset backtest matches a hand-computed fixture (test); entry-day rebalance and scheduled rebalance share one primitive (review criterion) |
| **V4-P8 — Broker CSV imports (L, diff:max, money-math + messy input)** | Import framework + 4 mappers: **Trade Republic, George (Erste), Flatex, IBKR.** Upload → broker autodetect (or manual pick) → parse into a normalized staging model (buys/sells/dividends/cash movements, fees where derivable) → **preview table** with mapped/unmapped/duplicate row flags → confirm → transactional apply into a chosen portfolio + cash source. Dedupe against existing transactions (content hash: date+instrument+qty+price); per-row error handling (skip + report, never all-or-nothing); anonymized fixture files per broker committed; per-broker quirks documented; explicit non-goal: automatic broker/bank API sync | Each broker fixture imports to its expected transaction set (4 golden tests); re-importing the same file creates zero duplicates (test); a malformed row is reported while the rest lands (test); imported dividends respect cash source + tax mode (integration with V3-P4) |
| **V4-P9 — Ideas: saved & shareable Workboard analyses (M/L, diff:intermediate; depends on v3 chat + audiences)** | **Saved ideas:** "Save as idea" persists a named Workboard state — conglomerate ref or ad-hoc asset set, backtest params, and a free-text thesis note; an Ideas list surface (placement planner-refined) reopens any idea in the Workboard exactly as saved. **Sharing:** ideas join the §13.3 audience picker (private/specific/all/public + friction ladder) and appear as a new group in friend rows and Shared-With-Me. **Suggest-this-asset:** chat chips can carry an idea or a bare asset suggestion; recipient resolution honors the idea's audience — sending never widens access (V3-P8 rule) | Save → reopen reproduces the exact Workboard state (test); a friend opens a shared idea read-only and clones it (e2e); a private idea chip shows "not shared with you", never data (test); ideas group appears in the friend-row expansion |
| **V4-P10 — Telegram & Discord channels (S/M bundle, diff:normal; ⚡ channel interface)** | Two `NotificationChannel` implementations: **Telegram** (bot token env-gated; user links their chat via a code/deep-link handshake in notification settings; unlink/relink) and **Discord** (per-user webhook URL, validated + test-message button). Matrix columns appear only when the channel is configured (SMTP pattern); provider rate limits respected; secrets never logged | A matrix-routed event produces exactly one Telegram send against a mock bot API (integration test); an invalid Discord webhook is rejected at save with a clear error (test); columns absent when unconfigured (regression) |
| **V4-P11 — DE sweep, e2e & the v4 gate** | Human-eye German pass over every v4 string; empty/error/loading-state sweep over all new surfaces; extend Playwright: bearer-scope spot-checks, account deletion, invite-token registration, Google login (mocked IdP), passkey (virtual authenticator), broker import preview→apply, benchmark compare, idea share→clone, announcement banner; **pre-release top-tier review** (Opus 4.8 max per the MODELUSE succession note) over the whole v4 branch; file **check v4** (`awaiting-owner`) and pause feature planning per protocol | Nightly e2e green incl. all new flows; no untranslated DE string on any surface (sweep); review findings fixed; check-v4 filed |

Estimate given to the owner: **$180–350, 5–8 factory days, 2 workers** (v3 day-1 reference: $151, 22 runs, 8 features merged).

---

## 13.5 Milestones — the V5 build order

Owner-decided 2026-07-07: **v5 = everything that remains — the §14 parking lot empties.** All specs below are composable, but v5 launches only after a **mandatory scope re-confirmation pass**: v4 + mobile-app feedback will have generated new §14 entries and shifted priorities, so the Chief walks the owner through this list (one chat message, add/cut by number) before prep. Execution playbook: `factory/V5_KICKOFF.md`. Binding rules of §13.4 (sizing, bundles, mf-meta, i18n EN+DE, friction ladder) carry unchanged. **Scope re-confirmed by the owner in chat 2026-07-17** (this section reflects it; §16 carries the record). **Anti-bloat rule (owner 2026-07-17, binding on every v5 surface):** the main things stay immediately visible and easy to read; niche/sub-features fold away (compact, collapsed, secondary) — the app must not FEEL more bloated with each shipped feature; the deep uncluttering itself is the v6 redesign (#544).

**Owner-provided items:** LLM API key + spend cap for P12 (or P12 is cut), Grafana exposure decision (P2: localhost-only vs admin-proxied), Play-Store listing assets when the Android app ships (ops footnote). P13 needs no decision anymore: owner-confirmed 2026-07-08 — both privacy modes build (§16).

| Phase | Scope | Done when |
|---|---|---|
| **V5-P0 — v4-feedback quick wins (S/M bundle, diff:normal)** | Filled 2026-07-17 from the owner's check-v4 feedback (#542): (a) **login layout, final:** "Continue with Google" stays on top, the prominent sign-up box moves back to the BOTTOM with the OR divider restored between the password form and it (partially reverts the #525 addendum ordering); the register page gets the mirrored box — "Have an account? → Sign in" — so both surfaces read uniform; (b) **deactivate Telegram + Discord channels** (owner: "don't delete, deactivate"): a global env kill-switch (default OFF) hides the matrix columns and settings surfaces and refuses the link/webhook endpoints while code, schema, and existing rows stay intact — flipping the env re-enables everything unchanged; (c) **profile icons:** a curated finite avatar set (old-Xbox style), picker in profile/settings, rendered wherever a user appears (friend rows, chat, profiles, requests); file uploads deliberately deferred until a hardened upload pipeline is worth building | (a) both layouts owner-eyed at the gate; (b) switch off ⇒ nothing Telegram/Discord renders and their endpoints 404 (regression test), flipping env restores (test); (c) the chosen avatar persists and renders on every social surface (e2e) |
| **V5-P0b — API & permission surfaces declutter (M, diff:normal; owner 2026-07-17)** | The scope pickers (Settings → API Access, admin OAuth editor, OAuth consent screen) drop the per-scope row walls: ONE row per module (Portfolio, Social, …) with read/write marks where **write implies read** (matches the #371 scope model); verbose descriptions collapse into info-points, useless ones get deleted; sections collapsed by default so creating an OAuth app no longer scrolls past every API-key tick; same treatment user-side and admin-side | Creating an OAuth app requires zero scrolling through unrelated permission lists (owner-eye); a write tick auto-marks read (test); the consent screen shows grouped plain-language permissions (e2e) |
| **V5-P0c — Settings → Connections hub, foundation (M/L, diff:intermediate; owner 2026-07-17)** | A new **Connections** settings area becomes the single home for everything linking BetterTrack to the outside: the Google identity block MOVES here from Security (link/unlink + email-match rules unchanged); designed slots for the v6 connectors (Drive user-sync V6-12, bank/broker cash sync V6-10, Parqet V6-11) with per-connection sync semantics (one-time sync vs stay-connected auto-sync); **source-tag groundwork:** transactions/dividends/cash movements gain a source tag (`manual` / `import:<broker>` / `sync:<provider>`, rendered as a badge and filterable) so synced data can never be confused with hand-entered data — the V4-P8 imports adopt their tags (`import:traderepublic`, …) in the same arc; **asset capability tags:** per-asset integration-support flags (e.g. "syncs with Parqet") surfaced on asset pages/pickers so users see what can sync where | Google link/unlink lives under Connections and is gone from Security (e2e); every import/sync path writes its source tag and filtering by tag finds exactly those rows (test); capability tags render for a fixture asset (test) |
| **V5-P1 — Performance & scale foundation (L, diff:hard/max; three arcs)** | (a) **Daily snapshot table** (diff:max): precomputed per-portfolio daily series (value, cost, P/L; per-source cash split) that graphs/analytics read instead of recomputing; backfill job; invalidation rules for backdated edits are the correctness core — planner defines and §16-logs them; the live "today" point still computes fresh; (b) **Conditional requests** (diff:intermediate): ETag/Last-Modified on hot read endpoints (portfolio summary, series, catalog search) — tuned for the mobile app's refetch-reconcile cycle; (c) **Provider failover chain** (diff:hard): a second quote source behind the §5.1 abstraction (source planner-picked per asset class, e.g. Stooq/CoinGecko), health-based failover + recovery, admin visibility of which provider served what; (d) **Intraday portfolio graph (owner 2026-07-17: "not just 2 points for 1D — a nice little graph"):** the 1D/1W portfolio ranges render a real curve — portfolio valuation series at intraday granularity (provider intraday quotes + the Live-Mode/history-stitch machinery; granularity planner-picked, e.g. 5–15 min for 1D / hourly for 1W; served cache-friendly via arcs a+b; graceful when intraday history is missing for an asset) | Graph endpoints hit the snapshot path (probe test) and a backdated transaction edit invalidates exactly the affected range (test); a 304 round-trip serves an unchanged summary (test); with the primary provider mocked dead, quotes keep flowing and the switch is visible in admin (test); the 1D range renders a dense intraday curve (≥20 points) for a portfolio of market-hours holdings, not two closes (test) |
| **V5-P2 — Observability & admin ops (M/L, diff:intermediate; four arcs; owner mandates 2026-07-17)** | **Zero-owner-setup mandate:** everything here provisions itself inside the deploy stack — no external accounts, no consoles, no manual wiring (the owner's Firebase/Play-console experiences are the anti-pattern). **Exposure resolved:** Prometheus/Grafana bind to localhost/LAN ONLY, never public; **the admin dashboard is the single public management surface** and must carry everything the owner needs day to day. (a) **Prometheus + Grafana:** `/metrics` (API latency, queue depth, provider calls, cache hit rate, ws connections, per-route counters) + auto-provisioned dashboards in the deploy stack, local-network-only; (b) **Usage analytics in admin:** DAU/WAU/MAU, feature-usage counters, top assets, registration funnel — first-party only, no third-party trackers; (c) **Feature flags:** admin kill-switches (realtime, live mode, chat, alerts, imports, AI) read at request time — no redeploy to flip; (d) **Problems & insights in admin (the Sentry replacement, §16 2026-07-17):** unhandled errors/failed jobs/provider failures captured into the DB (rate-capped, PII-scrubbed) and surfaced as an admin "Problems" page with status/resolve flow, next to the #480 health page — great monitoring with zero owner setup | Dashboards render live data in the deploy stack and are unreachable from public origins (test); analytics page shows plausible counts seeded by e2e traffic (test); flipping the chat flag hides chat within one request cycle, no deploy (test); a thrown test error appears on the admin Problems page with zero configuration (test); owner-eye: full monitoring reached through the admin panel without the owner having set up anything |
| **V5-P3 — Notifications maturity: digest + quiet hours (M, diff:normal/intermediate)** | **Digest mode:** per-user, per-type choice instant / daily / weekly; a digest job renders one summary email/push per period, honoring the channel matrix; **quiet hours:** per-user window + timezone; non-urgent notifications queue and deliver at window end; the "urgent bypasses quiet hours" class is planner-defined and §16-logged; settings surfaces follow the anti-bloat rule (digest/quiet-hours = a compact block, not another settings wall) | A daily-digest user gets exactly one summary covering the day's events (test); a notification fired inside quiet hours arrives at window end (clock-mocked test); urgent class bypasses (test) |
| **V5-P4 — Tax expansion: Germany + report export (L, diff:max, money)** | (a) **Country #2 — Germany:** Abgeltungsteuer 25 % + Soli 5.5 %-of-tax, **FIFO cost basis** (vs AT's moving average — the engine's cost-basis strategy becomes pluggable), Sparer-Pauschbetrag allowance, separate loss pots (Aktien vs Sonstige) with their offset rules; exact rules researched, §16-logged, and encoded as a German fixture test set BEFORE implementation; (b) **Tax-report export:** per-year CSV + printable PDF of the V3-P4 report for AT and DE; (c) **Tax v2 (owner 2026-07-17):** the manual per-trade mode gains a **configurable default** prefilled into every trade/dividend tax field and editable per trade ("enter tax for each trade, with a default value"); plus a **custom rule-built tax system** as a fourth mode — user-parameterized: rate %, in-year loss offset on/off, refund-of-already-paid-tax on/off, reset at year boundary on/off, carry-forward on/off, cost-basis method (moving-average/FIFO via arc a's pluggable strategy) — "if we don't support your tax system, you can enter how it works"; AT/DE stay first-class presets and must be expressible as parameter sets of the custom engine | The DE fixture set (gains, dividends, allowance exhaustion, both loss pots) passes; switching AT→DE mid-year applies forward-only (per the V3-P4 cutover rule, test); exported CSV/PDF match the on-screen report (test); the manual default prefills and stays editable per trade (test); a custom parameter set configured like AT reproduces the AT fixture results (test) |
| **V5-P5 — Market intelligence: dividends, earnings, news — GO OVERBOARD (L, diff:intermediate/hard; owner 2026-07-17: "really really overboard")** | All behind the §5.1 provider abstraction, env-gated where keyed, invisible when unconfigured. (a) **Dividend intelligence:** per-holding dividend calendar (ex-dates, pay dates), historical payout series per asset, forward yield, and **projected dividend income** for the whole portfolio (monthly/yearly view — also feeds the V5-P6b Forecast); dividend-announcement/ex-date opt-in notifications; (b) **Earnings calendar:** upcoming earnings for held + watched assets — asset-page block + Workboard panel + opt-in notification; (c) **Per-asset news:** headline feed on asset pages + a portfolio-level news digest view (holdings + watchlist); (d) **Corporate-actions awareness:** upcoming/known splits surfaced on the asset page; (e) more where the provider gives it cheaply (52w context, analyst counts) — planner expands within reason; sentiment analysis stays out; every surface obeys the anti-bloat rule (calendars/feeds compact, expandable) | Dividend calendar + projected income render correctly from fixture provider data (tests); a held asset with a known earnings date shows it and fires the opt-in notification (mock-provider test); news + digest render with graceful empty/error states; splits show on a fixture asset (test); everything invisible when unconfigured (regression) |
| **V5-P6 — Workboard endgame (L, diff:hard/max engine; three arcs)** | (a) **Conglomerate-vs-conglomerate comparison page:** N-series overlay + stats grid, reusing the V4-P7 benchmark math (2-series) generalized; (b) **Nested conglomerates:** a conglomerate as a constituent — cycle detection (hard reject), depth cap planner-set, weights resolve recursively at backtest/valuation time; (c) **What-if sandboxes on shared views:** a viewer tweaks weights/params on a shared conglomerate locally without persisting or needing edit rights, with "reset to shared" | Three conglomerates compare on one chart with a full stats grid (snapshot test); a self-referencing nest is rejected with a clear error (test); a sandbox tweak never mutates the shared object (test) and reset restores exactly |
| **V5-P6b — Forecast tab + standing orders (pulled forward from V6-4; owner 2026-07-17: "I really want the futures tab") (L, diff:hard; two arcs + calculators)** | (a) **Standing orders** (the V6-4 spec verbatim): scheduled recurring actions that auto-record on their schedule — "buy X of asset Y monthly", "add €N as 'salary' monthly", "deduct €20 as 'Netflix' monthly" — daily/monthly cadences over transactions and cash movements; managed list with pause/resume/end-date; each auto-recorded row source-tagged (`standing-order`, V5-P0c tags); (b) **Forecast tab:** project net worth into the future with user-chooseable factors — standing orders continued forward, historical average return (e.g. sampled over the portfolio's last 5 years), and what-if plans ("what if I invest €200/month into the S&P 500") — "predict your net worth if you keep doing what you're doing", incl. projected dividend income (V5-P5a) as an optional factor; (c) **Calculator suite** (owner: see parqet.com/de/rechner — "cool shit over there"): compound-interest, savings-plan, dividend/yield, and withdrawal-plan calculators — standalone AND pre-fillable from the user's real portfolio ("your portfolio, continued"); naming of the tab (Forecast/Future/Prognose) owner-eyed at the gate; anti-bloat: calculators live compactly inside the tab, not as nav spam | A standing order fires on schedule and books the tagged transaction/movement exactly once per period (clock-mocked test); pausing stops it (test); the forecast reproduces a hand-computed fixture (deterministic factors, test); what-if overlays render; each calculator matches hand-computed fixtures (tests); portfolio-prefill works (e2e) |
| **V5-P7 — Group portfolios: the MIRRORCHAIN model (owner-specced 2026-07-17, supersedes the roles sketch; XL, diff:max, privacy + money + sync)** | ONE logical portfolio replicated as a **synced copy in every member's account** — "a portfolio both people have": it appears and behaves as one, any member's write propagates to all copies. **Membership & authority:** the chain has exactly one OWNER with final say — adds/removes members, grants/revokes chain-manage rights (managers may also add/remove), and can **transfer ownership**; invites run through friends + the friction ladder. **Kick/leave semantics (owner-critical):** a removed or leaving member KEEPS their copy as a normal, no-longer-synced portfolio (a fork) — "you still hold the portfolio copy, it just doesn't get synced anymore". **Account deletion:** any member's deletion (incl. via V4-P2c) leaves every other copy and the chain intact; if the OWNER deletes without transferring, succession is planner-defined in the design note (transfer-on-delete to oldest manager vs chain dissolving into forks). TWR/tax compute per-account on each copy (each member's books are their own). **Seamless-UX mandate: "as uncomplicated for the end user as possible — really go overboard"** — joining, seeing "who else is on this portfolio", and leaving must feel effortless; a member never configures sync. The FULL design note (replication/conflict rules for concurrent edits, sync transport, invite/transfer flows, deletion succession, cash-source handling across copies) is §16-logged BEFORE code | A member's buy appears in every copy, attributed (e2e); kick leaves the kicked member a fully working un-synced fork (test); ownership transfer works and the old owner becomes a normal member (test); a member's account deletion leaves the others' copies + sync intact (test); concurrent edits converge per the design note (test); per-copy audit trail is complete (test) |
| **V5-P8 — Social v5: comments, reactions, groups (M/L, diff:intermediate)** | **Comments** on shared items (per-item thread; visibility = the item's audience; delete-own, item-owner moderates all); **emoji reactions** on shared items and comments; **groups:** named friend circles usable as sharing audiences (sits between specific-friends and all-friends in the picker and the friction ladder). NO public comments — public links stay read-only (§16). **Owner mandate 2026-07-17: "simple but powerful"** — not feature-overloaded, never confusing; the social surfaces may be REDESIGNED ground-up inside this arc if that is what clarity takes; everything ships compact per the anti-bloat rule | A comment is visible to exactly the item's audience (matrix test); owner moderation deletes any comment (test); sharing to a group reaches exactly its members and updates when the group edits (test); public pages render without any comment UI (regression) |
| **V5-P9 — Personal finance: expense tracking (L × multi, diff:hard domain — the largest v5 arc, composer splits into ≥3 issues)** | A NEW top-level product area, strictly separate from portfolio money (zero TWR/tax interaction): (1) **bank-transaction CSV import** reusing the V4-P8 framework (AT-relevant mappers planner-refined: Erste/George, Raiffeisen ELBA, N26, Revolut); (2) **categorized spending:** rule-based auto-categorization (user-editable rules), category manager with sensible defaults, per-transaction recategorize; (3) **dashboards:** spend by category/month, income vs spend, trends; (4) **budgets:** per-category monthly targets with matrix-routed alerts. Nav placement planner-designed, owner-eye at the gate; bank APIs/PSD2 explicitly out (future) | Each bank fixture imports and auto-categorizes with editable results (golden tests); the monthly dashboard reconciles to the imported sum (test); a blown budget fires exactly one alert per period (test); portfolio surfaces are byte-identical with the feature unused (regression) |
| **V5-P10 — API platform: webhooks + key governance (M/L, diff:intermediate/hard)** | **Outbound webhooks:** user-defined URLs subscribed per event type, HMAC-signed payloads, retries with backoff, delivery log, auto-disable on persistent failure (re-enable manually); **per-key rate limits:** admin-configurable tiers; **key audit trail:** last-used, per-key request log with a bounded retention window | A subscribed event delivers a valid-signature payload to a test receiver (integration test); a dead receiver auto-disables after N failures and logs it (test); a key over its tier limit gets 429 without affecting other keys (test) |
| **V5-P11 — ~~UX expansion: light theme, widget builder, kiosk~~ → CUT from v5 (owner 2026-07-17: "let the redesign do it — why do work twice") | All three arcs (light theme, overview widget builder, kiosk/TV mode) move into the redesign program: #544 designs them, v6 builds them. Kept here as the scope record only | — |
| **V5-P12 — AI features via LOCAL OLLAMA + pluggable providers (L, diff:hard; owner-confirmed 2026-07-17)** | **Default provider = the owner's local Ollama** (his LAN PC, RTX 4080 Super 16 GB): pick a solid mid-size instruct model (~7–14B, quantized) — do NOT max the card, and a future 8 GB-VRAM host must stay viable; env-configured endpoint; near-zero owner setup (a one-liner install/pull runbook is the whole ask). **Admin LLM-provider settings:** the admin panel carries pluggable provider config, switchable without redeploy — Ollama (endpoint + model picker) OR API-token setups for Claude / OpenAI / Gemini (secrets stored encrypted, never logged, never sent to the web client). Features on top (provider-agnostic): (a) **AI insights** — portfolio observations (concentration, overlap, drawdown context), informational only, hard "not financial advice" framing (§16); (b) **NL conglomerate builder** — "30 % US tech, 30 % EU industrials, 40 % gold" → draft resolved through the local catalog, always user-reviewed before save. Per-user daily caps apply to every provider; no provider configured ⇒ features invisible | With a mocked provider an insight renders with the disclaimer and never auto-acts (test); NL output is always a draft requiring confirm (test); provider switch in admin takes effect without redeploy (test); absent provider hides everything (regression); caps enforce (test); a real end-to-end against the owner's Ollama at the gate (owner-eye) |
| **V5-P13 — Privacy modes: discreet + paranoid (M/L; two arcs; paranoid REDEFINED by owner 2026-07-17)** | (a) **Discreet mode** (diff:intermediate, UI): per-user quick toggle that hides every absolute money amount on every surface (balances, € values, cash, transaction amounts, tooltips, chart axes) and shows percentages/relative values instead — for showing the app to others without exposing amounts; persists per user until toggled back; (b) **Paranoid mode — FULL BUILD IN V5** (diff:max, architecture — supersedes the 2026-07-08 read-secret sketch; owner 2026-07-17: "paranoid happens in v5, everything you need for it moves to v5" — the per-user **Google-Drive connection ships here** as its storage medium, pulled forward from V6-12): "my data stays with just me" — **client-side encryption** (owner clarified 2026-07-17): a paranoid account's portfolio data is encrypted ON the client, and the ciphertext is stored/synced through whatever media the user picks — the BetterTrack server, the user's Google Drive, **both, or Drive-ONLY** ("if you completely don't even want the encrypted shit on BetterTrack servers" — zero bytes of portfolio data on our side); the media set is user-switchable. The server (and Drive) act as blind blob stores + sync relays and can NEVER read the contents; the key never leaves the user's devices. Everything that depends on the server reading the portfolio is **absent by design** for these accounts: no public profiles, no portfolio/watchlist/conglomerate sharing, no server-computed stats — valuations/stats compute **client-side** (web/mobile) after local decryption; server-side price alerts remain available (public market data, zero portfolio exposure); the app stays fully functional without BetterTrack servers when Drive/local media carry the blob. **High-usability mandate:** paranoid mode must feel as easy as normal mode wherever possible — enable flow, media choice, and day-to-day use are first-class UX, not an expert corner. **Autonomy-prep principle (owner 2026-07-17, architecture-binding beyond paranoid):** web AND mobile clients are structured so they can one day run fully autonomous from BetterTrack servers — client-side data-home adapters (server blob / Drive / local) and client-side market-data adapters (e.g. Yahoo direct) as clean seams; distant future, but every v5+ client feature respects the seam. The FULL design note (blob format + versioning/conflict rules across sync media; key derivation + custody; exact feature-kill list; enable/disable transitions; recovery semantics — lost key = lost data, by design; interplay with exports and admin) is §16-logged and owner-acked BEFORE any code | Discreet mode: full-surface sweep finds zero absolute amounts with the toggle on (test); percentages still render correctly; toggling back restores exactly (e2e). Paranoid mode: design note §16-logged + owner-acked BEFORE code, then BUILT in v5; with the mode on the server stores no cleartext portfolio data (schema/probe test); **Drive-only round trip: a paranoid account with Drive-only media has zero portfolio rows server-side and the app remains fully functional (e2e)**; media switching migrates the blob correctly (test); social/sharing surfaces are absent for the account (matrix test); a client computes correct stats from encrypted fixture data (test); alerts still fire (test) |
| **V5-P13b — Mobile-usable webapp (pulled forward from V6-2; owner 2026-07-15: "at latest in v5"; L, diff:intermediate)** | Full responsive pass over EVERY user-app AND admin surface: collapsible/burger navigation, usable menus and pickers, tap targets, overflow fixes — no clipped or unusable control at phone widths. Usability first; beauty stays V6-1 (the deep design unification). **Primary audience: iOS users (owner 2026-07-17)** — this IS the iOS app until the native one exists (§13.7 KMP): include an **installable-PWA polish pass for iOS** on the #350 PWA foundation (install flow, icons/splash, standalone-mode quirks, offline-shell sanity) so an iPhone user gets a genuinely app-like BetterTrack in the meantime. Phone-viewport checks join the gate checklist permanently from here on | No unusable or clipped surface at 390×844 and 360×800 across user app + admin (sweep checklist); primary flows (login incl. PIN, portfolio browse, add transaction, cash actions, social, notifications, admin user management) pass a phone-viewport Playwright run |
| **V5-P13c — Admin session policy (S/M, diff:normal; owner 2026-07-17, settles #430)** | Admin security = **login with 2FA, then peace**: NO step-up/re-prompt 2FA on destructive actions ("not another 2FA popup every 2 seconds") — #430 is REJECTED and closed; instead the **admin session expires early**: 6–24 h (default planner-picked in that window, env/admin-configurable), independent of the user-app session lifetime rules (#418) | Admin session expires per config while a user session persists (clock test); no admin action triggers an extra 2FA challenge post-login (regression); expiry config changes apply without redeploy (test) |
| **V5-P14 — DE + light-theme sweep, e2e & the v5 gate** | Human-eye DE pass over all v5 strings (light-theme sweep moved to v6 with P11); empty/error/loading sweep; extend Playwright: digest delivery, DE tax fixture flow, mirrorchain flows (join/write-propagation/kick-fork/transfer), comments/groups, expense import→budget alert, webhook delivery, standing-order + forecast flow, paranoid Drive-only round trip, NL builder draft; **pre-release top-tier review** of the whole v5 branch; file **check v5** (`awaiting-owner`) and pause | Nightly e2e green incl. all new flows; DE sweep clean; review findings fixed; check-v5 filed |

Estimate (post-re-confirmation 2026-07-17): **$550–950, 12–18 factory days, 2 workers** — the scope grew (Forecast pull-in, paranoid full build incl. Drive, market-intel overboard, mirrorchain) while P11 moved to v6. Run governance: the Chief guards a 60 % weekly-usage cap manually (no armed trigger). After v5 the parking lot is empty; the v6 scope is already owner-locked in **§13.6** (2026-07-12) and v7 seeds collect in **§13.7** — each gets its full phased plan at its own prep gate. Ops footnote: when the Android app ships to the Play Store, `mobile.bettertrack.at` flips from placeholder to store redirect (Chief ops task, not a factory issue).

---

## 13.6 Milestones — the V6 scope (owner-locked 2026-07-12; phases composed at v6 prep)

Theme (owner): **"make the app nice, feel more connected and compact, and make it ready for publishing."** v6 is the release milestone: deep UX/design unification, a phone-usable webapp, zero placeholder pages, full legal/publishing readiness, public-user hardening, and the governance rails (limits, developer role, monetization prep) for a truly public — and later possibly paid — product. The rows below are the binding scope record; the phased build order with acceptance criteria is authored at v6 prep (after check-v5), same pattern as §13.4/§13.5, preceded by the standard owner scope re-confirmation pass.

| # | Scope item |
|---|---|
| **V6-1** | **UX & design overhaul — the main arc.** The current webapp reads "strict": 4 tabs up top plus a rigid submenu, surfaces that don't flow together. Redesign the shell and the overall look so everything plays together the way it does on the phone — more connected, more compact, flowy navigation instead of the strict tab/submenu split. Full design-language pass across every surface. (The v3-endgame design pass is a light consistency polish; this is the deep one.) **Execution locked 2026-07-17 (owner):** runs as the ground-up redesign program, kickoff issue **#544**, on an **external Claude instance** (subagents per competitor/page-family — NOT the factory). Mandates: PROJECTPLAN layout prescriptions are void (design freely); **features-complete design** — every current AND planned feature gets a designed home ("coming soon" allowed, later bolt-ons are not); semantic data-viz color (green gains / red losses; brand gold = accent, not a straitjacket); absorbs the **Analytics reorganization** (owner-flagged worst offender) and the **#389 competitor research** (Phase 0). #544's Phase-4 output is the factory-ready implementation issue set that v6 then executes. Direction gets owner-eye mockups at the #544 Phase-1/2 check-in before any code |
| **V6-2** | **→ Pulled forward to v5 (V5-P13b; owner 2026-07-15: "at latest in v5").** iPhone users have only the webapp (native iOS via the KMP bridge is v7+, §13.7). Usability itself now ships in v5; v6 retains only the residual — phone-viewport POLISH inside the V6-1 design pass — and phone-viewport checks stay on every gate checklist permanently |
| **V6-3** | **Page inventory — zero "coming soon" left.** Sweep every stub/placeholder page: each either ships as a real page or is deleted (owner reviews the inventory and rules per page at prep). Owner examples: placeholder idea-pages he no longer plans get cut; the Forecast tab (V6-4) becomes real; a Transactions surface that "could already be made" stops being coming-soon |
| **V6-4** | **→ Pulled forward to v5 (V5-P6b; owner 2026-07-17: "I really want the futures tab").** v6 retains only redesign-alignment polish of the shipped Forecast tab. Original spec (now living in V5-P6b): **Forecast + standing orders** (working names, owner asked for better than "recurring tasks" — candidates: *Standing orders / Daueraufträge* for the engine, *Future / Forecast / Prognose* for the tab; final naming owner-eye at prep). (a) **Standing orders:** scheduled recurring actions that auto-record on their schedule — "buy X of asset Y once a month", "add €N to cash as 'salary' monthly", "deduct €20 as 'Netflix sub' monthly" — daily/monthly cadences, applied to transactions and cash movements. (b) **Forecast:** a new tab that projects net worth into the future with user-chosen factors: standing orders continued forward, historical average yearly return (e.g. sampled over the portfolio's last 5 years), and what-if plans ("what if I invest €200/month into the S&P 500 / SpaceX / anything") — "predict your net worth if you keep doing what you're doing." First formal record of this owner idea (no earlier §14 note existed) |
| **V6-5** | **Legal & publishing readiness.** Terms of Service + Privacy Policy + Impressum + in-product disclaimers written for maximum lawful owner protection: no-financial-advice framing everywhere relevant; market-data accuracy/delay disclaimer (a delayed provider quote must never be the app's liability); as-is service with liability limited to the fullest extent the law allows; chat/UGC terms (user-content responsibility, report/block/moderation path); versioned-terms acceptance with re-accept-on-change flow; GDPR pass (policy matches actual processing; export + deletion already shipped); and the full Play Store / App Store legal requirement set (privacy-policy URL, data-safety declarations, in-app account deletion — partly shipped in v4). ⚠️ Recorded honestly: absolute immunity ("not accountable for anything") does not exist under EU/AT law — intent, gross negligence, and consumer-protection rights cannot be disclaimed. Scope = the maximum protection drafting can achieve + a strong recommendation that a real lawyer reviews the final texts before public launch |
| **V6-6** | **Public-launch security hardening.** Ready for fully public users: registration abuse protection, rate-limit review under open registration, input-validation sweep, dependency + secrets audit, and whatever the v6 pre-release review flags |
| **V6-7** | **Resource limits / quotas (anti-nuke + paid backbone).** Admin-configurable per-user caps with safe defaults so no user can nuke the server: defaults around 10 portfolios, 10 watchlists, 25 alerts (full limit inventory planner-derived: custom assets, conglomerates, API keys, chat rate, …). Friendly limit-reached UX, per-account admin overrides |
| **V6-8** | **Monetization prep (no payments yet).** Plan/entitlement architecture so a paid tier can be flipped on later with minimal work — owner wants at least server + domain cost recovery. Limits (V6-7) wire into plans; entitlement checks centralized; explicitly NO payment-provider integration in v6 |
| **V6-9** | **Developer role + OAuth governance.** Creating OAuth apps stops being open to everyone (personal API keys stay universal): a **developer** account grant is required; users request it via an in-app form that lands in an admin queue, the owner approves/denies from the admin page. Every third-party OAuth consent screen must make the user know exactly what he's doing: the app's name, whose app it is (developer identity), the requested permissions in plain language, and a "this is not a BetterTrack app" disclaimer (first-party apps exempt) |
| **V6-10** | **Bank/broker cash sync via a licensed aggregator (read-only; owner 2026-07-17).** George/Erste, Raiffeisen, and Trade Republic CASH accounts sync into cash sources through ONE aggregator API (GoCardless Bank Account Data free live-AIS tier or equal — final pick at v6 prep): the aggregator holds the AISP license, BetterTrack never touches PSD2 certificates. Balances + booked transactions (deposits, withdrawals, dividend postings) land source-tagged (`sync:george`, …) in the V5-P0c hub, one-time and stay-connected modes. **Binding read-only mandate: AIS consents only — payment-initiation/PIS is NEVER implemented, no write capability toward any bank, ever.** Depot/securities positions are not in PSD2 scope anywhere — depots keep syncing via V4-P8 CSV (and V6-11 for Parqet users). Facts: `docs/integrations-research.md` |
| **V6-11** | **Parqet two-way sync (official Parqet Connect API; owner 2026-07-17).** OAuth 2.0 against Parqet's developer platform (self-serve console, read AND write scopes, integrations private-by-default until published via Parqet support): read holdings + activities (incl. tax/fee/broker fields) to import a Parqet portfolio into BetterTrack, and export/sync BetterTrack portfolios to Parqet where write scopes allow; per-portfolio link with direction choice; dedupe via the V4-P8 content-hash + `sync:parqet` source tags; per-asset "Parqet-supported" capability tags (V5-P0c) show what can sync |
| **V6-12** | **Google Drive for USERS (the #496 vision, owner re-stated 2026-07-17).** Per-user Drive connection in the Connections hub: first ship = account-export/backup sync to the user's own Drive; the direction = Drive (or a local file) as a full data home — sync between devices through the user's Drive, and the app stays functional WITHOUT BetterTrack servers (single instance or Drive-synced). **Partially pulled to v5 (owner 2026-07-17): the Drive connection + paranoid storage semantics ship in V5-P13 arc (b).** V6-12 keeps the general non-paranoid conveniences: export/backup auto-sync to Drive for normal accounts + the broader device-sync UX, redesign-aligned. For paranoid accounts the stored/synced blob is client-encrypted per V5-P13 arc (b) — Drive and the BetterTrack server are equal blind sync media; the redesign (#544) designs the full ladder in, shipping is incremental |
Estimate at v6 prep. Gate = **check-v6** per the standard protocol.

## 13.7 V7 — collected seeds (not planned; menu process with the owner after v6)

Seeds gathered so far, owner-dated 2026-07-12 unless noted:

- **Competitor market research** (#389) — **ABSORBED 2026-07-17 into the redesign program:** #544 Phase 0 runs it (one subagent per competitor) and feeds the feature-gap map straight into the design; #389 closes when #544 delivers it. The §14 paragraph spec transfers there.
- **Sub-portfolios (portfolio-in-portfolio):** a portfolio can contain sub-portfolios for grouping — e.g. one per bank plus a shared one under a single umbrella portfolio. The parent pools the stats of its children; any change made from the parent view is really performed in the owning sub-portfolio. (Kin to nested conglomerates V5-P6b, but for real portfolios — cycle/depth rules would follow the same discipline.)
- **Broker/platform integrations, split from import/export:** true *integrations* = live-linked providers (Trade Republic, Raiffeisenkasse, George, possibly even Parqet for cross-tracker sync) where linked assets are read-only in BetterTrack — sellable only at the broker itself; plain *import/export* = CSV in/out (the V4-P8 framework) stays the separate manual path. **Architecture decision explicitly reserved for the owner at v7 prep:** integrations as separate first-party OAuth apps under `integrations.bettertrack.at` (owner's lean — keeps the main app from becoming one mega-fat monolith) **versus** built into the main app; he wants the trade-offs laid out once more before deciding. **UPDATE 2026-07-17: researched (`docs/integrations-research.md`) and pulled forward — bank cash sync via aggregator = V6-10, Parqet = V6-11. The integrations-as-own-apps question resolves to in-app-via-aggregator for bank cash (no per-broker apps needed; revisit only if a real per-broker depot link ever appears). TR/George/Raiffeisen depot live-links remain unavailable (PSD2 = payment accounts only; TR has no retail developer API) — depots stay CSV (V4-P8).**
- **iOS app via Kotlin Multiplatform bridge** (owner note 2026-07-12: "will take longer, maybe v6 or later" → parked v7+): until it exists, iPhone = the mobile-usable webapp (V6-2).

---

## 14. Future Features

The intentional parking lot — scoped out of V1, designed-for in V1. Roughly ordered by leverage; ⚡ = nearly free thanks to the architecture.

**Pulled into the V2 build order (§13.2), no longer parked:** self-service password reset, 2FA, multi-portfolio UI, conglomerate/watchlist sharing to friends, per-type notification channels, portfolio cash balance, default portfolio visibility.

**Pulled into the V3 build order (§13.3), no longer parked:** price alerts + evaluator, realtime gateway, Live Mode v1, share links + per-item sharing audiences, multiple named watchlists, expandable friend rows, public profiles, friend chat, inflation/real-terms view, late-listed-constituent backtest modes (incl. the entry-day rebalance primitive; scheduled rebalancing stays parked), custom-asset value smoothing, gold/commodities + crypto symbols, per-user base currency, dividends + realized P/L & tax reports (broker CSV imports stay parked), session manager UI, PWA + web-push, FCM mobile-push server prep (the phone app itself stays a future project), i18n EN/DE.

**Pulled into the V4 build order (§13.4), no longer parked:** mobile-platform enablement (scope expansion, bearer parity, idempotency keys, OAuth account chooser, account deletion, FCM go-live), registration modes activation, Sentry + bull-board + admin health, announcements, account data export, offsite Drive backups, backtest custom benchmark + stats table, rebalanced backtest mode, broker CSV imports, ideas (saved/shareable Workboard analyses + suggest-this-asset), Telegram/Discord channels, Google login + passkeys.

**Pulled into the V5 build order (§13.5), no longer parked — the lot empties (owner decision 2026-07-07):** daily snapshot table, conditional requests, provider failover chains, Prometheus/Grafana, usage analytics, feature flags, digest mode + quiet hours, Germany tax + report export, earnings calendar + per-asset news, conglomerate-vs-conglomerate comparisons, nested conglomerates, what-if sandboxes, shared editable portfolios, comments/reactions/groups, personal finance/expense tracking, outbound webhooks + key governance, light theme, overview widget builder, kiosk/TV mode, AI insights + NL conglomerate builder, paranoid mode. The prose below stays as the design-notes archive those §13.5 rows reference.

**Parked for V7 planning (owner note 2026-07-09; moved v6 → v7 by the 2026-07-12 re-slot, see §13.7):** competitor market research — a full survey of competing portfolio managers/providers (Parqet, getquin, Portfolio Performance, Sharesight, Snowball Analytics, Delta, Simply Wall St, Finanzfluss Copilot, broker-integrated trackers, … esp. DACH): complete feature inventory + pricing structure per competitor, written down raw (tables) so the owner can turn it into his own report, then a gap/parity/differentiator map of every feature BetterTrack has implemented by then. Research work, not factory code — run via research subagents at v7 planning time. Tracking issue: #389.

**First picks after V1**
- **Price alerts + evaluator** — schema and notification fan-out already exist. Rule kinds `price_above/below`, `pct_up/down_from_ref` (ref captured at creation), `pct_day_up/down`; evaluated every minute against cached quotes; one-shot (fire once → `triggered`, manual re-arm) or `repeat` (24 h cooldown); idempotency key per (alert, trigger window). Workboard alerts panel + asset-page inline alerts.
- **Realtime gateway** (§4.5) — socket push for notifications, quotes, shared views; the event bus is already in place.
- **Live Mode v1** — the §6.3 design: shared per-asset polling loop → Redis ring buffer → `asset:{id}` fan-out; windows 1 m–12 h; auto-stop.
- **Conglomerate sharing** — share links (≥128-bit token URL, live read-only view + clone, revocable), sharing to friends, JSON export/import incl. the legacy `conglomerate.py` prototype format (`{version:1, conglomerates:{…holdings:[{ticker,name,weight}]}}`).
- **Multi-portfolio UI** — create/rename/archive portfolios, switcher, per-portfolio pages; the data model ships ready in V1.

**Sharing & Social**
- Public profiles & public links (customizable: whole portfolio, selected parts, conglomerates, ideas — freeze-shot or live); shared **editable** portfolios (permission levels: read-only / edit / manage); ideas (shareable Workboard analyses + "suggest this asset" messages to friends); later: comments/reactions/groups — always gated by explicit opt-in privacy.
- **Expandable friend rows** (owner idea, 2026-07-04): the V1 Friends page and a separate "Shared With Me" list force cross-referencing two surfaces to answer "what has *this* friend shared with me, and what can I do about them?". Instead keep Friends as a list but make each entry **expand in place** (accordion) to a per-friend panel with (1) **all friend options/actions** — remove friend today; nickname, notification/mute prefs, "share with them" shortcuts, view-public-profile slot in later — and (2) **everything that friend shared with you**, grouped by kind (portfolios, conglomerates, and any future shareable thing — watchlists, ideas, …), each item linking straight to its read-only view. Collapsed rows carry kind counts (e.g. "2 portfolios · 1 conglomerate") so scanning the list already tells who shared what. This is the **friend-centric** view; "Shared With Me" stays as the **item-centric** view (or becomes a filter) — both read the same sharing data, **no schema change implied**. Extensible by construction: new shareable types (conglomerate sharing, ideas) appear as new groups in the expanded panel without a redesign. Optional (planner's call): a small "you share with them" section in the same panel for two-directional privacy clarity. Out of scope for this idea: the new shareable types themselves (their own §14 items), comments/reactions/groups, public profiles.
- **Friend chat** (owner idea, 2026-07-04, #248): message friends, send investment ideas / invites to shared portfolios. No specifics yet.

**API access as a product** (§6.13)
- API keys / personal access tokens with scopes (never expire, revoke-only), OAuth apps, per-key rate limits and audit trail; outbound user-defined webhooks. The smart-fridge dashboard becomes possible here.

**Portfolio & finance**
- Broker CSV imports (Trade Republic, George, Flatex, IBKR mappers); FIFO/tax-lot accounting + realized P/L & dividend reports (AT tax view); dividends as cash-flow transactions; daily snapshot table; benchmark-vs-portfolio overlay; per-user base currency (currencyService is already parameterized).
- **Portfolio cash balance ("Bargeld")** (owner idea, 2026-07-04 → #220): per-portfolio cash ledger with deposit/withdraw; buy flow gets "Pay from cash balance", sell flow "Add proceeds to cash balance", with a live cash-after preview (available → after) and no silent negative balances; sticky per-portfolio default so repeat entry is one click but never guessed silently. Cash is a first-class line in the overview totals and every cash movement is a reconciling transaction (current cash = sum of movements). Calculator bulk buy-flow (§6.7) can pay the whole list from cash. Core correctness requirement: buying *from cash* is not a new deposit — it must NOT create a TWR cash-flow event in the performance-% graph mode (money already in the portfolio changed form). Out of scope: multi-currency cash, interest, margin, broker CSV cash imports.
- **Inflation comparison / real-terms view** (owner idea, 2026-07-04): measure portfolio performance against selectable inflation measures (AT/EU HICP, US CPI, or a custom flat rate) — as a benchmark-overlay series and/or a graph display mode normalizing the value curve to inflation-adjusted ("real") terms; composes with the performance-% display mode.
- **Personal finance / expense tracking** (larger future direction): upload bank-transaction CSVs → categorized spending analytics (by category/month), budgets, personal finance dashboard; later bank APIs and automatic import.

**Market data & assets**
- ⚡ Gold/commodities (`XAUEUR=X`, `GC=F`) and crypto (`BTC-EUR` or a CoinGecko provider) — register symbols or add a provider file; category browse pages fill in.
- Provider failover chains; earnings calendar; per-asset news.
- **Custom-asset value smoothing** (owner idea, 2026-07-04, #248): when recording custom asset value marks with timestamps, offer an option to interpolate between marks (linear transitions) instead of abrupt steps. Owner is unsure it's a good idea — parked, not committed.

**Conglomerates & Workboard**
- Rebalanced backtest mode (monthly/quarterly/yearly); conglomerate-vs-conglomerate comparisons; what-if sandboxes on shared views; nested conglomerates; multiple named watchlists; saved strategy ideas.
- **Full-window backtest for late-listed constituents** (owner idea, 2026-07-04): when a conglomerate holds an asset younger than the requested window (the "SpaceX case": listed 18 months ago, backtest is 5Y), today the engine clips the window to the youngest constituent (§13 P4 notice), silently turning a 5Y comparison into an 18-month one. Instead offer a backtest **mode selector** that runs the full requested window with a user-chosen rule for the pre-listing period. Example throughout: 25% Bayer / 25% SpaceX / 25% Coca-Cola / 25% BMW, 5Y, SpaceX listed 18 months ago.
  1. **Clip to common history** (current behavior, kept): window starts at the youngest constituent's first trading day, with the existing notice.
  2. **Cash until listing:** each constituent's share is invested from window start *if it exists*; a not-yet-listed asset's share sits as uninvested cash (0% return) until its first trading day, then is invested at that day's price. (75% works from day one, SpaceX's 25% waits 3.5Y.) Mirrors "I'd have kept that money aside until it went public."
  3. **Redistribute until listing:** a not-yet-listed asset's share is split **equally** among the already-listed constituents until its first trading day; on that day the portfolio **rebalances to the target weights**. (Bayer/Coca-Cola/BMW at ~33.3% each for 3.5Y, then an entry-day rebalance to 25/25/25/25.) Mirrors "I'd have invested everything in the rest, then rebalanced when it arrived."
  - **Design notes:** equal split is the owner default (a proportional variant may be offered later; state the chosen rule in the UI notice). Apply the rule per late asset independently — each listing date is an investment event (cash mode) or a rebalance event (redistribute mode). Chart affordances: vertical marker at each entry date ("SpaceX enters"), notice text adapts to the chosen mode instead of the clipping message, and in cash mode the idle-cash portion is made visible (subtle band or an "avg. uninvested: X%" stat). Benchmark (S&P 500) always runs the full window — in cash mode the idle-cash drag is real and intended, the honest comparison being asked for. Entry pricing = first available close on/after the listing date, FX/dividends unchanged. **Shares the rebalance primitive with the Rebalanced backtest mode above** (an entry-day rebalance is a special case of scheduled rebalancing — build the primitive once). Out of scope for this idea: scheduled periodic rebalancing itself (only the shared primitive), delisting/merger handling (asset stops existing mid-window — worth a code note), and any change to portfolio real-holdings graphs (backtest-only).
- **Backtest: custom benchmark + benchmark stats** (owner idea, 2026-07-04 → #232): the benchmark can be any of the user's other conglomerates or any catalog asset/index (found via local search; current preset indexes stay as one-click quick picks), and every stat the panel shows at the bottom (total return %, avg %/year (CAGR), max drawdown, …) is displayed for **both series side by side** — conglomerate column vs benchmark column, optional delta column — so the comparison is numeric, not just two chart lines. A benchmark conglomerate runs through the same engine with the same window/settings (including the same late-listed-constituent strategy above, keeping it apples-to-apples); an asset benchmark is the degenerate single-asset case. One benchmark at a time; no new metrics — this is the lightweight in-panel version of the conglomerate-vs-conglomerate comparisons above.

**Notifications**
- ⚡ Telegram/Discord channels (implement `NotificationChannel`, register, add settings fields); digest mode; quiet hours; web-push (PWA).

**Mobile & clients**
- **Firebase/mobile future architecture:** a native/React-Native app speaks the identical REST API; **Firebase Cloud Messaging** becomes a new NotificationChannel for phone push; no backend rewrite — this is the entire point of API-first. PWA (installable, offline shell) first; kiosk/TV read-only dashboard.

**Accounts & security**
- **2FA (TOTP) — planned** (§6.1) + passkeys; Google login; self-service password reset by email; session manager UI ("log out that device"); full account data export; Google Drive backups.
- **"Paranoid mode"** (owner idea, 2026-07-04, #248, very distant): optional per-account read token shown only once at account creation, required to read the account's data — nobody sees the portfolio except the owner and explicitly allowed people. Owner explicitly unsure; parked so it isn't forgotten.

**Admin & ops**
- Activate invite-token / approval / open registration modes (§6.12); full feature flags; announcements; usage analytics; bull-board; Sentry; Prometheus/Grafana.

**Speculative**
- AI insights ("71% US tech — concentration note"); natural-language conglomerate builder; i18n DE/EN; light theme; custom widget builder for the Portfolio overview.

---

## 15. Open Items (owner to provide — none block development)

1. ~~Domain name + server for deployment~~ **Resolved 2026-07-06:** `bettertrack.at`, Cloudflare-managed; host = the owner's always-on Mac until a dedicated server. Origin layout: `bettertrack.at` product page · `web.` app · `admin.` · `api.` · `mobile.` placeholder (§13.3 V3-P12). Go-live ops (DNS/tunnel/exposure) run by the Chief of Development with the owner's Cloudflare MCP, outside the factory.
2. ~~The Gmail account + app password for the email channel~~ **Resolved:** SMTP live via bettertrack.service@gmail.com.
5. **When V6-10 builds:** an aggregator account (GoCardless Bank Account Data free tier or the pick at v6 prep) + its API keys.
6. **When V6-11 builds:** a Parqet developer-console integration registration (self-serve; publishing later goes through Parqet support).
7. **When the offsite backup is wired (task open):** one Drive OAuth consent as bettertrack.service@gmail.com on the rclone setup (the Gmail app password is SMTP-only and cannot reach Drive).
3. Final accent color / logo direction (wordmark + favicon exist; refinement tracked in issues).
4. Exact Live Mode window set (1 m … 12 h are the working defaults, adjustable later).

## 16. Decision Log

| Date | Decision | Why |
|---|---|---|
| 2026-06-12 | Stack: React+Vite SPA + Express API, TS monorepo, Postgres, Redis, Docker | Owner choice — "proper big scale", matches existing experience |
| 2026-06-12 | Closed accounts; admin-created **and** invite links; email mandatory | Owner decision |
| 2026-06-12 | Notification channels abstracted; v1 = in-app + email (email primary) | Owner decision; Telegram/Discord later as plug-ins |
| 2026-06-12 | EUR base + FX conversion; per-user base deferred to Future Features | Owner decision |
| 2026-06-12 | Market data: Yahoo Finance behind the provider abstraction | Free, EU coverage, proven in the conglomerate.py prototype; swappable by design |
| 2026-06-13 | Strict 3-layer split: static SPA · API/business core · data; push via WebSocket; API-first for a future mobile app | Owner requirement ("display/business/data, app runs on the user's PC, SignalR-style communication") |
| 2026-06-13 | Renamed PortfolioMan → **BetterTrack** | Owner decision |
| 2026-06-15 | While Claude Fable 5 is unavailable, the build factory builds T1 (`tier:fable`) work on **Opus at max reasoning**, reverting to Fable automatically when it returns | Owner authorization — Fable access is gated ("Mythos"); the P1 provider/caching/currency keystone must not block the whole pipeline. Routing rule 3 is relaxed for the duration of the outage only (CLAUDE.md rule 7) |
| 2026-06-16 | A disabled account that logs in with the **correct** password gets a distinct `ACCOUNT_DISABLED` (403, "account suspended") instead of the generic `INVALID_CREDENTIALS`. The status is revealed **only after** the password is verified correct — wrong-password and unknown-user paths stay generic — so it is not a user-enumeration oracle | Owner authorization (chris-dev-at) — a suspended user authenticating correctly should understand *why* they can't get in rather than seeing a wrong-password message. Safe because the reveal sits after the password check (PROJECTPLAN.md §6.1) |
| 2026-06-16 | Relax the §6.1/§10 rate limits (all centralized in `apps/api/src/config/env.ts`): general API per-user 600 → **4500/15 min**, login 5 → **25/min/IP**, admin 120 → **600/15 min**; per-account throttle `accountFailuresPerHour` 10 → **20**, `lockoutThreshold` 10 → **20**, lockout **15 min → 5 min** | Owner authorization (chris-dev-at) — the original general limiter (~40 req/min) tripped 429s during normal logged-in use as rapid tab switching / multi-tab TanStack-Query refetches exceeded it. Spam/abuse protection relaxed substantially while keeping a real (if lenient) brute-force guard. Architecture, Redis store, and search 60/min/user limit unchanged |
| 2026-07-06 | **V2 signed off; V3 scope locked** — 30 items selected by the owner from the full candidate menu, encoded as §13.3. Explicitly parked instead: backtest custom-benchmark stats table, rebalanced backtest mode, broker CSV imports, Telegram/Discord channels, Google login + passkeys | Owner selection (chris-dev-at) in chat, 2026-07-06 |
| 2026-07-06 | **Tax mode semantics (V3-P4):** modes None / Manual-per-trade / Country-specific; v3 ships Austria only — 27.5 % KESt on realized gains and dividends, moving-average cost basis, intra-calendar-year loss offset with refund of tax already paid that year, hard reset Jan 1, **no cross-year carry** | Owner spec. His example sentence about a February loss against a November gain was ambiguous; the no-carry reading (matching "doesn't go over years / resets every year", and AT law) was flagged to the owner in chat and stood uncorrected |
| 2026-07-06 | **Privacy friction ladder:** publishing anything `public` (profile or link) requires a strong explicit-acknowledgment warning — the user must consciously accept that strangers can see their holdings/net worth; `all friends` gets a light confirm; `specific friends` none | Owner directive — "make sure you know how serious it is … before making your private information public" |
| 2026-07-06 | **bettertrack.at go-live topology:** 5 origins (product/web/admin/api/mobile) behind Cloudflare, hosted on the owner's always-on Mac until a dedicated server exists; the domain deployment becomes the main instance | Owner decision; placeholder-first for mobile (web app is desktop-first, store app later) |
| 2026-07-06 | **i18n:** English default + full German in v3; locale-file architecture where a new language = one translation file + registry entry; post-V3-P1 features must ship EN+DE keys in the same PR | Owner directive |
| 2026-07-07 | **V3 cash addendum:** every cash source gets a "set balance to X" operation alongside deposit/withdraw — the app computes the signed delta and records it as a regular movement (V3-P3); V3-P0 additionally fixes the reported withdraw-all bug (full withdrawal leaves €0.01 behind; must land at exactly €0.00) | Owner directive in chat, post-scope-lock |
| 2026-07-01 | Fable restored; outage fallback reverted (see docs/FableBackExecute.md) | Owner |
| 2026-07-01 | **Plan v2 rework per owner issue #79 + session directives** (new nav/product structure, multi-portfolio, Social, config topology, admin/user split, API docs) | Owner directive #79 is the new product truth: 5-tab structure, multi-portfolio data model, minimal friend sharing, local search index, caching/coalescing keystone, Live Mode design, expanded Settings, Gmail email + email log, admin global settings + 4 registration modes, 30-day sessions + PIN + progressive rate limits, API access as a product feature; session directives added the subdomains/ports deployment topology, the management-only admin account kind, and the V1 `/docs` page. Superseded rate-limit rows above are replaced by the §10 progressive scheme |
| 2026-07-03 | **Allocate response (`allocatePositionSchema`) extended with `nativePrice` + `currency`** (`packages/contracts/src/conglomerate.ts`, `apps/api/src/services/conglomerate/conglomerateService.ts`), out of issue #138's stated scope (which put the allocate contract/endpoint out of scope and said to flag-not-edit) | Building the Invest Calculator buy-flow (#138, PR #150) surfaced a real currency bug: the bulk prefill recorded the EUR-converted `costEur/qty` into a transaction's `price`, which `domain/holdings.ts` treats as **native-currency** and re-converts by `asset.currency` — silently double-converting the cost basis of any non-EUR position (e.g. USD-quoted NVDA/GOOGL). Reverting to a `costEur/qty` prefill would ship that bug, so the allocate response now carries each position's native `price` + ISO-4217 `currency` (read from the *same* cached quote already used for the EUR conversion — no second fetch, no drift). Additive-only. Ratified by Chief of Development 2026-07-03; issue re-tiered sonnet→opus (contracts/endpoint is T2, adjacent to the §5.3 currency keystone); tracked in #152. An independent tier:opus review of the exact diff found it safe/correct as-is |
| 2026-07-07 | **Fable succession (permanent):** Claude Fable 5 retires from interactive/subscription use after 2026-07-07 (API-only thereafter). Top tier everywhere = **Opus 4.8** — `models.json` re-routed (`diff:hard` → opus `xhigh`, `diff:max` → opus `max`) the same day; every "Fable"/"T1" reference in MODELUSE.md, CHIEF.md, and §13.x reads as "the top available tier". Unlike the 2026-06-15 outage row, there is no automatic revert | Anthropic model availability change; v3 was mid-build, so the routing flip happened immediately to avoid failed runs. The v4/v5 plans and kickoffs were written by Fable on its last day so Opus executes a finished design |
| 2026-07-07 | **V4 + V5 scope locked** — v4 = every COD-starred menu item (mobile platform enablement, registration modes + Google/passkeys, Sentry/announcements, export + offsite backup, backtest benchmark + rebalanced mode, broker CSV, ideas, Telegram/Discord), encoded as §13.4 with `factory/V4_KICKOFF.md` as playbook; v5 = **everything else in §14 — the parking lot empties**, encoded as §13.5 with `factory/V5_KICKOFF.md`, subject to a mandatory owner re-confirmation pass before v5 prep | Owner decision (chris-dev-at) in chat, 2026-07-07: "every feature you have starred right now gets into v4 … v5 will have all the features there are, like all of them" |
| 2026-07-08 | **V3-P4 tax-engine cutover + settlement semantics (issue #331, implements the planner refinement verbatim):** (1) tax mode (+ country) is per **user**; every sell/dividend is taxed per the mode active at the moment it is *recorded* (regardless of a backdated trade date), and the applied mode + computed tax freeze onto the row — mode switches apply forward only and never recompute existing rows; (2) year aggregation buckets by the trade date's calendar year in **Europe/Vienna**; the AT offset pool contains only rows themselves taxed under AT mode, and the pool is per **portfolio** (a portfolio models one depot — the report is portfolio-scoped and tax cash stays inside the portfolio's sources); (3) settlement is **append-only**: the tax held for a year is derived from movements (each taxed row's settlement movement is written atomically with it and carries a `tax_year`; a backdated trade joins *its* year's pool with a movement dated at the trade), and anything that re-shapes history — a backdated buy shifting existing AT gains, deleting a taxed sell or a feeding buy, deleting an AT dividend — re-derives the year and posts an unattached correcting withholding/refund movement (dated now, `tax_year`-attributed, against Main) instead of ever editing prior rows; (4) the tax base is the app's own realized-P/L definition computed in **EUR at each leg's trade-date FX** (moving-average basis, buy fees capitalized, sell fees deducted) — a deliberate simplification vs. strict §27a EStG fee non-deductibility so the report's P/L and the taxed gain are one truth; dividends are entered in EUR (cash is EUR-only); (5) rows carrying recorded tax are **financially immutable** (note edits allowed; a buy feeding AT-taxed sells is too) — the correction path is delete-and-re-add, which re-settles the year append-only; (6) TWR classification: `dividend` / `tax_withholding` / `tax_refund` movements are **internal** — performance reads inclusive of dividend income and net of taxes, exactly as it already reads net of fees | Factory implementation decisions for V3-P4 (owner spec 2026-07-06 + planner refinement in #331); logged per the issue's binding instruction |
| 2026-07-08 | **V4/V5 docs corrections + owner decisions** (after a max-effort review of this branch): (1) `models.json` is **owner-managed** — the owner runs the remaining Fable window on it and flips routing himself when Fable access ends; kickoff/prep docs no longer prescribe or "sanity-fix" it (the "already re-routed 2026-07-07" claim was wrong and is retracted; the retirement itself stands — owner-confirmed, the 2026-07-08 overlap was timezone lag); (2) **easy → Opus 4.8 `low` is deliberate policy** — Opus replaces Sonnet for easy work (better cost/usage/output); MODELUSE floor line updated; (3) **milestone-gate protocol:** the check-vN gate issue is closed the moment the owner signs off (Stage B), not later at feedback stage — removes the reboot window that re-ran the endgame QA; (4) **V5-P13 redefined as Privacy modes** — BOTH discreet mode (hide absolute amounts, show percentages, for showing the app to others) AND paranoid mode (hashed read-secret) build; (5) CHIEF.md ops rows updated to the live setup (public https URLs for QA/health, multi-factory compose project + logs) | Owner decisions (chris-dev-at) in chat, 2026-07-08 |
| 2026-07-09 | **V4-P2c account-deletion semantics (issue #362, the planner-refined "exact semantics → §16"):** (1) **chat anonymization** — `chat_conversations.user_a/user_b` and `chat_messages.sender_id` switch from ON DELETE CASCADE to nullable + ON DELETE SET NULL: the partner keeps the full thread, the deleted side renders as a localized "Deleted user" (`user: null` / `senderId: null` in the contract), messages the deleted user sent stay visible (they were delivered; deletion doesn't recall them) but carry no identity, share chips keep resolving per-viewer through the enforcement layer (the deleted owner's items are gone, so they resolve to the no-leak "not shared" state), and the thread is closed to new messages exactly like unfriending; a conversation whose BOTH sides are gone is purged (messages cascade with it) since nobody may ever read it again; (2) **re-auth** — current password, or for a 2FA-enrolled account a fresh TOTP code or unused recovery code (login-flow email codes are pending-token-bound and not reusable here; every account retains the password path), failures on a dedicated per-account progressive throttle; (3) **typed confirmation** — the username, case-insensitive, checked server-side (mirrors the admin delete); (4) **surface** — one capability: `DELETE /api/v1/account` callable by cookie session or bearer with `account:security`, and the stable public web URL `/account/delete` (bounces anonymous visitors through login) for the Play listing; admin-kind accounts are excluded (admin area owns their lifecycle); (5) the audit row (`user.deleted`, `via: self`) carries a null actor FK + username-only meta | Factory implementation decisions for V4-P2c (#362), logged per the issue's binding instruction |
| 2026-07-11 | **V4-P2b session-lifetime model (issue #418, part A of owner spec #399 §A):** sessions stop being immortal. (1) **"Stay signed in"** checkbox on the normal web login, **ticked by default** → a persistent session (the existing fixed 30-day window + a Max-Age cookie); unticked → an **ephemeral** session. The ephemeral model is the owner's *preferred* browser-session-only variant AND his *fallback* activity-timer, combined: the cookie is **browser-session-scoped** (no `Max-Age`/`Expires`, so it dies on browser close) and, so the server session is likewise not immortal, the Redis session carries a **sliding 45-minute idle window hard-capped at 6 hours from creation** (the cap alone bounds it — the sliding TTL is refreshed on the same throttled activity write that stamps last-seen, and can never exceed `createdAt + 6h`). A `persistent` marker on each session record (absent ⇒ persistent, so pre-deploy sessions are unaffected) drives the cookie flavour and the V3-P11a session-manager badge. (2) **OAuth-authorize logins** (Custom-Tab browsers share cookies → silent auto-re-login after app logout): a PIN-less OAuth login is **forced ephemeral server-side** regardless of the requested `staySignedIn` (authoritative enforcement in `authService.login`), and the SPA hides the checkbox for it. An account **with** a PIN may keep a browser session — the PIN gates access — so the SPA offers a post-credential-entry "stay signed in — your PIN protects this" choice (the PIN status is only known after login), wired through a new **`POST /auth/session/persist`** (cookie-session only, **PIN-gated**: a PIN-less account can never promote its forced-ephemeral OAuth session). The persistence intent computed at the password step rides the pending-2FA state to the verify step so a 2FA login honours it too | Factory implementation decisions for #418, logged per the issue's binding instruction and the planner's design-guidance note (browser-session-only cookie + bounded server TTL) |
| 2026-07-13 | **V4-P2b OAuth account memory + PIN quick re-auth (issue #419, part B of owner spec #399 §B):** the security-sensitive server design behind the client-side chooser state ladder. (1) **Remembered-device binding** — a **signed, httpOnly `bt_rdid` cookie** holds an opaque 256-bit CSPRNG device id, mapped in Redis (`remember_dev:<id>` → user id) with **no TTL** ("until cleared" — owner). The id is stored raw, exactly like a session id (`sess:<id>`): integrity comes from the cookie signature (a Redis leak yields no usable cookie without the signing secret), and a stolen cookie still cannot authenticate without the PIN. Set only by **`POST /auth/remembered-device`** (cookie user-kind only, **PIN users only** — a PIN-less account 400s `PIN_NOT_ENABLED`, server-authoritative; the SPA also hides the prompt). The response — and the localStorage record it feeds — carries **only** `{ userId, username, avatarUrl }`, **never a token or scope** (owner note); `avatarUrl` is always null today. (2) **PIN quick re-auth** — **`POST /auth/pin/quick-auth`** is public (no session yet — the whole point), per-IP `login`-limited; the identity is taken **from the `bt_rdid` cookie, never the body**. Its PIN check rides the **SAME per-account progressive limiter** as the #361 bearer PIN verify (`pin_token_account`, keyed by user id) — hammering either locks out on one schedule; with no session to drop, that limiter (not the 5-strike session counter) is the brute-force guard. The minted session is **always ephemeral** (Custom-Tab browsers share cookies — a quick re-auth must never silently leave a persistent session). A `pin`-less call is a **probe**: it auto-passes only while a **device-keyed ~15-min window** (`pin_quick_ok:<id>`, set on a real PIN entry, fixed TTL, never refreshed by an auto-pass) is open, else answers `{ pinRequired: true }`. This window is a **new server-side grace deliberately distinct from the client idle-lock** (`pinLockIdleMinutes`, a UI-only preference) — quick re-auth has no live session to time against. An absent/forgotten device, or a bound account that is gone/suspended/PIN-less, returns a **uniform `REMEMBER_DEVICE_UNKNOWN`** (no device/account oracle) and self-heals the dead binding. (3) **Forget** — **`DELETE /auth/remembered-device`** (public; only ever affects the presenting device) clears the binding + window + cookie; the SPA clears its localStorage record first so a close-then-reopen shows a blank login. (4) **No OAuth-route change** — the chooser ladder is entirely client-side (`LoginPage` + `OAuthAccountChooser`); `/oauth/authorize` stays `requireUser`, so state 1 (a live PIN-gated session) is the existing PIN gate above routing, and quick-auth sessions are ordinary ephemeral sessions that surface in the V3-P11a session manager. The one-time remember-me prompt rides the existing #418 PIN-user post-login step as a second opt-in (localStorage "asked" flag ⇒ shown once) | Factory implementation decisions for #419, logged per the issue's binding instruction and the planner's design-guidance note (server-side remember-me/PIN-re-auth must be §16-logged before merge) |
| 2026-07-12 | **V6 scope locked + roadmap re-slot (old v6 → v7)** — the owner assigned his new batch (initially aimed at v5) to a NEW v6 and pushed the previous v6 seed (competitor research #389) to v7. v6 = §13.6: UX/design overhaul as the main arc ("feel more connected and compact", the strict tab/submenu shell must flow), mobile-usable webapp (iOS native deferred — KMP bridge is v7+), zero coming-soon pages (build or delete, owner rules per page), Forecast + standing orders (recurring buy/salary/subscription rules + net-worth projection tab, first formal record), legal & publishing readiness (ToS/privacy/Impressum/disclaimers/store requirements at maximum lawful protection — absolute immunity impossible under EU/AT law, recorded, lawyer review recommended), public hardening, per-user limits with safe defaults (~10 portfolios / 10 watchlists / 25 alerts), monetization-prep entitlements ("keep the potential make-it-paid"; at least server+domain cost recovery), developer-role gating of OAuth-app creation (in-app request form → admin approval; personal API keys stay universal) + third-party consent transparency (app name, developer identity, plain-language permissions, non-first-party disclaimer), joint/group portfolios initially pulled into v6 but **moved back to v5 the same day (owner correction)** — stays V5-P7, its row enriched with the group/firm framing and the co-ownership design question. v7 seeds = §13.7: competitor research, sub-portfolios (portfolio-in-portfolio stat pooling), live broker/platform integrations split from CSV import/export (Trade Republic/Raiffeisen/George/Parqet-sync; integrations-as-own-apps under integrations.bettertrack.at vs in-main-app **reserved for the owner at v7 prep**), iOS via KMP | Owner directives (chris-dev-at) in chat, 2026-07-12: "move that all to v6 and make current plans for v6 to v7 … thats the scope of the new v6 … make the app nice feel more connected and compact and make it ready for publishing" |
| 2026-07-14 | **Alert-follow visibility model (issue #455, the planner-deferred "V3-P5 audience model vs. simpler flag → §16"):** alert visibility is a single per-user opt-in — `users.alerts_visible_to_followers`, default OFF — managed from the owner's alert UI via `GET/PUT /alerts/sharing`, NOT a per-alert V3-P5 audience row. Rationale: the consumers of alert follows are FOLLOWERS (asymmetric, no accept step, not necessarily friends), while the V3-P5 rungs are friend-scoped except `public_link`, whose token-URL surface has no alert equivalent — the audience model does not map cleanly onto the follower relation. The flag gates BOTH `follow.alert.created` and `follow.alert.fired` inside the recipient queries at emission time (owner unshares / follower unfollows / trigger flips off ⇒ delivery stops immediately, mirroring #439's visibility-loss behavior), and enabling it requires an explicit acknowledgment server-side (§16 friction ladder: all-followers ≈ public — anyone may follow). The per-follow triggers (`user_follows.notify_on_alert_create` / `notify_on_alert_fire`, both default OFF, independent) are set at follow time or via the #439 PATCH; notify-only — no alert is ever copied into the follower's list, and the owner's own `alert.triggered` delivery is untouched (self-follows are CHECK-impossible, so never doubled) | Factory implementation decision for #455, logged per the issue's binding instruction ("planner-refined and §16-logged"); the issue itself authorized the simpler per-user flag as an alternative to the audience model |
| 2026-07-15 | **V4-P0c Notification UX — lean email defaults, read=archive, deep links (issue #474, the planner refinement verbatim):** (1) **Lean email defaults are a PURE service-level flip, no data migration.** The account/security category = exactly `{account.invite, account.temp_password}` (the §6.10 `account` category; a contract test pins the two in lock-step). Email now defaults **OFF** for every other type and **ON** only for that category; the bell/in-app, phone-push and web-push defaults are unchanged (ON for all). The single source of truth is `notificationChannelDefaultEnabled(channel, type)` in `packages/contracts/src/notifications.ts`, resolved by BOTH the settings surface (`notificationSettingsService`) and the dispatcher's fan-out gate (`notificationRepository.typeChannelEnabled`/`routingFor`). Resolution precedence: explicit per-type `config` override wins → a channel master-off (`enabled: false`) forces off → else the per-(channel, type) default. Because storage is sparse (override-else-default) and the settings matrix UI only ever writes per-type overrides (never the channel `enabled` flag, which stays `true`), **existing users need no migrated rows**: every explicit ON/OFF override survives byte-identical, and every un-overridden non-account type flips to OFF from the default change alone. **Re-enable is the ordinary matrix toggle** — the flip leaves no hidden state. (2) **One-time announcement** — `announceLeanEmailDefaults` (run from `scripts/migrate.ts` after `migrate`) inserts exactly ONE in-app notification per existing user, type `account.notice` (account category, never event-dispatched), localized EN/DE via the `emailI18n` server-copy pattern, deep-linking to `/settings/notifications`; idempotent via a fixed `payload.eventKey` behind the `(user_id, eventKey)` partial unique index, so a re-run (or a later deploy) is a no-op and brand-new accounts (already lean) are never announced to. (3) **read ⟺ archived** — reading a notification archives it eagerly (`markRead`/`markAllRead` coalesce `readAt`+`archivedAt`; an already-read insert archives too), so the active inbox shows unread only and history lives under Archived; the #437 lazy `AUTO_ARCHIVE_READ_AFTER_DAYS` sweep is retired. (4) **Deep links** — `notificationLink()` covers EVERY type against the finalized `docs/mobile-push.md` §4 route-key matrix (web in-app targets and FCM `data` ids identical; `friend.activity` gains an additive `data.username` to mirror `follow.published`); a row missing its id falls back to the type's landing surface rather than a dead click | Factory implementation decisions for #474, logged per the issue's binding instruction (the planner refinement — final migration mechanics + account/security type mapping + re-enable semantics — must be §16-logged with the PR) |
| 2026-07-15 | **V4-P6 arc-b offsite backup — optional sixth service via override (issue #481):** the base topology stays at five services (§4.6); a new `backup-offsite` sidecar (alpine + `age` + `rclone`, ~40 MB) lives in a separate `infra/docker-compose.offsite.yml` override, `profiles: [offsite]`-gated so even a stack that layers the override never starts the container on `up -d`. Owners who don't need offsite backup never include the override — nothing changes for them. The sidecar mounts the shared `pgbackups` volume read-only, encrypts the newest local dump to an owner-provided PUBLIC `age` recipient (encrypt-only on the box; the identity stays offline with the owner), uploads via `rclone` to a configured remote (Google Drive in prod; `local` for the drill), and prunes remote objects older than `BT_BACKUP_REMOTE_RETENTION_DAYS` (default 30). Env-gated end to end: `BT_BACKUP_AGE_RECIPIENT_FILE` and/or `BT_BACKUP_RCLONE_REMOTE` unset ⇒ single `offsite skipped` log line + exit 0; a failed upload preserves the local `.sql.gz` and exits non-zero so cron surfaces it. Full runbook (keygen, rclone Drive setup, retention contract, restore drill) in `docs/ops.md`. `set -x` is deliberately never enabled in `offsite.sh` around secret handling | Factory implementation decision for #481, logged per the issue's binding instruction ("if the compose service topology changes, add a §16 note"). Sidecar-over-host-cron chosen so the tool set (age + rclone) ships in compose regardless of host OS; the override + `profiles` gating keeps the base five-service topology intact for the majority of owners who don't run offsite backup |
| 2026-07-15 | **V4-P7 arc-b scheduled backtest rebalancing — trading-day rule + late-listing composition (issue #490):** (1) **Rebalance day** — the **first trading day of each new calendar month/quarter/year on the series axis**, detected as an axis day whose calendar period differs from the previous axis day's; t₀ never rebalances (the initial allocation already sits at target weights) and a trading gap spanning several boundaries collapses into ONE rebalance on the next trading day. The reset executes at that day's closes, **after** any §14 entry events, and conserves value — the boundary day's index point is unchanged; the restored weights apply from the next day. (2) **One primitive** — a scheduled rebalance is the same `rebalanceToTargets` call the §14 entry days use (review criterion: no duplicated rebalance math); with a schedule active the engine routes even `clip` through the event-driven pipeline, while `clip` + `none` keeps the untouched pre-V4-P7 pipeline (bit-identical results). (3) **Composition with the late-listing modes** — `clip`: no late assets by construction (clipped window), the whole basket resets to the full target weights; `cash`: a not-yet-listed constituent enters the target set **only once listed** — the schedule rebalances ONLY the invested sleeve to the listed constituents' relative weights while the uninvested pool stays exactly the unlisted weight (0 % return, untouched), and a late constituent still buys in at its full target share on its listing day; `redistribute`: the schedule rebalances to the same *effective* targets as the entry-day rebalance (listed positive-weight constituents absorbing an equal share of unlisted weight), so an entry day coinciding with a boundary is idempotent. (4) **Events & semantics** — every executed scheduled rebalance is reported as a wire-typed `rebalanceEvents` entry (chart markers for the arc-a sibling UI), including mathematically no-op resets (single-asset basket — the schedule ran), but not when there is nothing to allocate to; the benchmark overlay is never rebalanced (a single asset — rebalancing is the identity); with a schedule active `contributionPct` is the money-weighted segment gain in every mode (still summing to the total return). (5) **API strictly additive** — optional `rebalance` on the preview request (`none`/`monthly`/`quarterly`/`yearly`, absent = `none` = buy-and-hold, today's behavior), echoed in the response; the frequency is part of the preview memo key, so previews differing only in frequency never share a cache entry | Factory implementation decisions for #490, logged per the issue's binding instruction (the rebalance trading day and the composition with late-listing modes are planner-refined and §16-logged) |
| 2026-07-16 | **V4-P4b Google login (issue #478, the planner-refined "§16 for verified-email-only linking + Security link/unlink surface + deviations"):** (1) **Resolution order** — existing `(provider='google', sub)` → sign in; else **verified-email match** (`email_verified: true` AND exact-lowercased match to a `users.email`) → link + sign in; else register subject to the active mode. An **unverified** Google email NEVER links (account-takeover guard) — it falls through to registration, where a taken email then surfaces `EMAIL_TAKEN` and creates nothing. (2) **Password-usability model** — a Google-registered account is created password-less: it still carries a random **unusable** argon2id hash (satisfies `users.password_hash` NOT NULL) plus a new `users.has_usable_password` boolean (default TRUE for every existing/password account, unchanged). `updatePassword` flips it TRUE, so a Google-only user who later completes a password reset can then unlink. This flag drives the unlink guard. (3) **Settings → Security link/unlink surface** — `GET /auth/google/link-status` + `POST /auth/google/unlink` (both `account:security` where a bearer applies, per #361). Unlink requires a **password re-auth** and is refused (`409 GOOGLE_ONLY_SIGN_IN`) while Google is the only usable sign-in method (`!has_usable_password`); a "Connect Google" affordance reuses the same start route (an authenticated session makes `start` a link flow, keyed by `linkUserId` in the state). (4) **Approval-mode Google application** — `registration_requests.password_hash` is now nullable and gains `provider`/`provider_subject`/`provider_email_verified`; a federated application stores NULL password + the verified identity, and admin approval mints the unusable hash + `has_usable_password:false` and **links the identity** before signing rights, reusing the #453 approve/reject + decision-email machinery. (5) **State/CSRF** — each single-use `state` is stored in Redis (10-min TTL) AND double-submitted via a signed httpOnly `bt_goog_state` cookie (SameSite=Lax, survives the top-level redirect back from Google); the callback requires query-state == cookie-state == a live Redis entry before any token exchange or account action, closing OAuth login-CSRF. **Deviations taken:** (a) **jose dependency added** (`apps/api`, the issue's sanctioned "jose-based verify") — verifies the ID token's signature against Google's JWKS + `iss`/`aud`/`exp` in the authorization-code exchange, injectable so tests run network-free. (b) **Google sign-in establishes a full session even when app-2FA is enabled** — federated login trusts Google as the factor (Google enforces its own 2FA); a 2FA-challenge-over-redirect was judged out of proportion and is documented here rather than built. (c) **The per-email `InvitePage` gets no Google button** — the federated flow registers per the active registration MODE and consumes registration TOKENS, not the distinct per-email `invites` mechanism; a naive button would be broken in `closed` mode (where per-email invites matter most), so it is deferred (the invite-token MODE is covered via the RegisterPage carrying its token into `start`). | Factory implementation decisions for #478, logged per the issue's binding instruction (verified-email-only linking, the Security-page link/unlink surface, plus any deviation taken) |
| 2026-07-16 | **Google auth v2 — email-match-only linking + connect→prefill→submit registration (issue #525, owner order; SUPERSEDES parts of the #478 row above):** (1) **Settings connect is email-match-only.** `linkToUser` now requires the Google claims' **verified** email to equal the account email (normalized/case-insensitive) before it links — a mismatched or unverified connect throws `GOOGLE_EMAIL_MISMATCH` (→ `?error=google_email_mismatch` on Settings → Security) and plants no identity, changes no session. The guard sits AFTER `assertNotAdmin` (admins still refused first) and BEFORE the already-linked checks, so a cross-user hijack is caught by the email guard, not the (now largely unreachable) "belongs to another user" branch. The sign-in **verified-email auto-link** path (`resolveSignIn` step 2) already matched by email and is UNCHANGED. (2) **Google-assisted registration no longer instant-registers at the callback.** A brand-new identity (`resolveSignIn` step 3) — for every mode except `closed` (which keeps its friendly callback rejection) — parks the verified claims in a **server-side one-time ticket** (`google_register_ticket:<id>`, Redis, ~10-min TTL) and lands the browser on `/register?google=connected` via a signed httpOnly **`bt_goog_reg`** cookie (SameSite=Lax; the ticket's browser-session binding — a ticket lifted to another browser is unusable). The connected form prefills the email **locked** + seeds the username from the Google name; the account is created only on explicit submit through **`GET /auth/google/register-ticket`** (display values) + **`POST /auth/google/register`** (`googleRegisterRequestSchema`). The email + subject to link are taken **from the ticket, never the body** — a tampered form email is ignored. The ticket is single-use, **spent only on success**, so a validation failure (taken username, bad token) leaves it live for a retry; a spent/expired ticket 400s `GOOGLE_REGISTER_TICKET_INVALID` and drops its cookie. **This supersedes #478 (1)'s callback-time registration and (2)'s password-usability model:** password rules are unchanged (the form still sets one), so a Google-assisted account is now created **WITH a usable password AND the linked identity** — no longer password-less. `approveRegistrationRequest` derives `has_usable_password` from whether the request carried a real hash (federated approval now keeps the applicant's password). The login-page Google click for a new identity in open mode stops instant-registering — it lands on the connected form too. **Deviations kept from #478:** the per-email `InvitePage` still gets no Google button (c); the invite-token MODE now enters its token ON the connected form rather than carrying it through `start` (that plumbing is removed). **Addendum (same order):** the **login surface** reorders top-to-bottom to Sign-up → Google → password form and **drops the "OR" divider** (register/invite keep their #467 layout, so `common.or` is retained for RegisterPage). | Factory implementation decisions for #525, logged per the issue's binding instruction ("this supersedes parts of #478's instant-registration semantics — add the §16 Decision Log row") |
| 2026-07-16 | **V4-P8 broker-import apply semantics + dedupe scope (issue #492, the "exact apply semantics §16-logged"):** (1) **Per-row apply, not one batch transaction** — the issue simultaneously requires "never all-or-nothing across rows" and that every portfolio write route through the EXISTING services (`portfolio.createTransactions`, `tax.recordDividend`, `depositCash`/`withdrawCash`), each of which owns its own DB transaction (trade + linked cash/tax legs atomic per call). So "the confirmed valid rows land atomically" is resolved as: **each row lands atomically WITH its legs** (the owning service's transaction — a row can never half-land), rows apply **chronologically** (deposits → dividends → trades in file order → withdrawals within a day, so moving-average/tax replays and cash solvency see history in order), a row the service rejects (`INSUFFICIENT_CASH`, `OVERSELL`, `DIVIDEND_ASSET_NOT_HELD`, …) is reported `failed` while the rest continue, and the batch flips to `applied` exactly once — **enforced by an atomic claim** (`UPDATE … SET status='applied' WHERE status='pending' RETURNING`) BEFORE the first row books, so a concurrent apply (the route is bearer-reachable and mobile clients retry by design) loses the compare-and-set and gets the same `409 IMPORT_ALREADY_APPLIED` as a sequential second apply; the apply route also mounts the V4-P2a `Idempotency-Key` middleware like every other portfolio mutation, so a keyed retry replays the recorded response instead of the 409 (duplicates are additionally re-derived against live data at apply time, so a preview-to-apply race cannot double-book). (2) **Dedupe hash spans all row kinds** — the §13.4 key (`date+instrument+qty+price`, sha-256, canonical decimals rounded to the persisted column scale — qty 8 dp, price/EUR 6 dp — so a value survives its `numeric`-column round-trip) is extended so "re-importing the same file creates zero duplicates" holds for the WHOLE file: trades hash day+`side:assetId`+qty+price (side INCLUDED — the spec key names dedupe intent, not a mandate to collapse opposite sides: a same-day flat round-trip, buy+sell at equal qty/price, is real broker data and must never self-dedupe, the same argument that keeps `cash:deposit` apart from `cash:withdrawal`), dividends put the EUR gross in the price slot, cash rows put `cash:<kind>` in the instrument slot; hashes derive from live entities (transactions/dividends/external movements), so deleting a mis-import makes the row importable again via a fresh upload (rows a stored preview already flags `duplicate` keep that verdict at apply). (3) **Resolution is exact-only** — symbol, ISIN-as-catalog-symbol, then whole-name equality via the search service (one enrichment-settled retry); a currency mismatch between the file row and the resolved listing is an `error` flag, never a silent conversion. (4) **Cash linkage is opt-in** (`linkCashOnTrades`, default off) — a partial export would otherwise overdraw a ledger that never saw the broker's deposits; dividends/cash rows always book against the chosen source (Main default). TR quirks (Zinsen→deposit-with-note, Sparplan→buy, Betrag untrusted for trades) in docs/imports.md | Factory implementation decisions for #492, logged per the issue's binding instruction ("exact apply semantics §16-logged") |

| 2026-07-17 | **check v4 (#542): v4 accepted, feedback routed.** Owner: "everything I have seen in the webapp works completely fine" (env-gated/unshipped surfaces explicitly not judged). The feedback wave lands as: V5-P0/P0b/P0c fills, V5-P4 arc c (tax v2), the V5-P13 paranoid redefinition, the V6-1 execution mandate + kickoff #544, V6-10/11/12, and the §13.7/§15 updates — all in this PR | Owner feedback wave (chris-dev-at), chat 2026-07-17 |
| 2026-07-17 | **Sentry is OUT — permanently.** The admin panel is the single management/insight home for the app (stats, problems, health). #480's Sentry SDKs stay env-dark (never activate a DSN; strip when convenient); an admin "problems/insights" surface (error capture into the DB + an admin page, extending #480's health page) covers the need — designed in the redesign, built v5/v6 | Owner directive 2026-07-17: "I don't want Sentry — the admin panel as insight into the app, and everything regarding the app's management in the admin panel" |
| 2026-07-17 | **Telegram + Discord notification channels: deactivate, don't delete** — global default-OFF kill switch (V5-P0 b); code, schema, and existing user rows stay; flipping env restores | Owner directive 2026-07-17 |
| 2026-07-17 | **Offsite DB backup target = bettertrack.service@gmail.com's Drive** (#481 sidecar). The account's existing Gmail app password is SMTP-only — rclone/Drive needs a one-time OAuth consent (§15 item 7); runbook goes to the live box when wired | Owner directive 2026-07-17 |
| 2026-07-17 | **Cloudflare SSL stays Flexible for now — no paid certificates.** The free zero-cost path (Cloudflare Origin CA cert + origin 443) is documented as #543 Part B for the live dev; Full (strict) flips only AFTER the origin serves HTTPS | Owner directive 2026-07-17 ("leave it as is; a free option needing almost nothing from me → go for it") |
| 2026-07-17 | **Broker-integration ground rules (research record, binding):** direct PSD2 production access requires a licensed AISP + eIDAS QWAC — permanently out of scope for BetterTrack; bank data comes only through a **licensed aggregator** (V6-10). **Read-only mandate: AIS consents only; PIS/payment endpoints are never implemented; no credential custody for unofficial/private APIs** (Trade-Republic-private-API-style access is rejected — credentials + ToS risk). PSD2 covers payment accounts only: depot/securities have NO PSD2 path — depots sync via CSV (V4-P8) and Parqet Connect (V6-11). Full findings: `docs/integrations-research.md` | Research 2026-07-17 + owner's explicit read-only order ("no possibility of doing anything finance-related-executing, just read") |
| 2026-07-17 | **Ground-up redesign program mandates** — V6-1 executes as kickoff issue **#544** on an external Claude instance (not the factory): PROJECTPLAN layout prescriptions void, features-complete design (every planned feature designed-in), semantic green/red data-viz (gold = accent), absorbs #389 + the Analytics rework; Phase-4 output = the factory implementation issue set | Owner directive 2026-07-17 ("completely shit on the projectplan if needed — brand new crazy good design, only limited by your capability") |
| 2026-07-17 | **V5 scope re-confirmation DONE (chat) — the §13.5 rewrite in this PR is the record.** Owner decisions: P11 CUT → redesign/v6 ("why do work twice"); V6-4 Forecast + standing orders PULLED INTO v5 (V5-P6b) + Parqet-Rechner-inspired calculator suite; P5 market intelligence "really really overboard" (dividend calendar + projected income, earnings, news digest, corporate actions); **P7 = the mirrorchain model** (replicated synced copies, one owner with final say + transferable, kick keeps an un-synced fork, member deletion never kills the chain — "really go overboard, seamless"); P8 social stays but "simple but powerful", ground-up redesign of social surfaces allowed; **P12 AI = local Ollama on the owner's 4080-Super PC (mid-size model, 8 GB-VRAM future-proof) + admin-pluggable providers (Claude/OpenAI/Gemini tokens) ready in admin settings**; **P13 paranoid BUILDS in v5** incl. the per-user Drive connection pulled from V6-12, media choice server/Drive/both/**Drive-only**, high-usability mandate, autonomy-prep principle (clients get data-home + market-data adapter seams so web/mobile can one day run server-less); P13b = the iOS story (installable-PWA polish); P9 expense tracking stays in v5; **#430 step-up 2FA REJECTED** → V5-P13c: admin login-2FA once + short admin sessions (6–24 h), no re-prompts; P2 monitoring = zero-owner-setup, Grafana/Prometheus local-network-only, admin dashboard is the only public management surface (+ Problems/insights arc = Sentry replacement); global anti-bloat rule added to the §13.5 intro | Owner scope pass (chris-dev-at), chat 2026-07-17 |
| 2026-07-17 | **v5 run governance:** weekly-usage cap stays ~60 % but the CHIEF guards it manually — no armed control-server trigger, no cap machinery per version ("don't make a mega fuzz about it"). First v5 factory window: owner-ordered timeboxed **30-minute run** (prep → run 30 min → close-down → Chief pauses) | Owner order (chris-dev-at), chat 2026-07-17 |
| 2026-07-17 | **V5-P1 daily-snapshot invalidation rules (issue #553 — the §16-logged deliverable).** The precomputed per-portfolio daily series (`portfolio_daily_snapshots`: one row per calendar day through *yesterday* — value = net-worth #311, open cost basis at that day's FX, P/L = holdings − cost, net external TWR flow, per-source cash split, per-asset EUR values as the V3-P9 analytics feed) is invalidated by **direct service calls, never the event bus** — the bus is at-most-once fan-out and this is a money-path correctness invariant; every invalidation synchronously (a) merges `dirty_from = LEAST(existing, fromDay)` into `portfolio_snapshot_state`, (b) deletes the rows on/after `fromDay` — **rows strictly before it are never touched** — and (c) best-effort enqueues `snapshots.recompute`; the read path refuses dirty/stale state and falls back to the live engine with an opportunistic refill, so correctness never depends on job delivery. **The earliest-affected-day (`fromDay`) rule set:** (1) transaction batch create → min(every input's day, every written cash/tax leg's day — a #378 settle-as-of-today leg lands later, an AT correction at now, so the minimum covers all rows); (2) transaction edit → min(old `executedAt` day, new day) — cash-linked/taxed rows stay financially immutable upstream; (3) transaction delete → min(row day, posted tax-correction days); (4) cash movements — deposit, withdrawal, both transfer legs, set-balance — → the movement's (possibly back-dated) day (transfers move only the per-source split but invalidate anyway: blanket rule stays simple and true; set-balance is always effective now); (5) dividend record → min over its written movements (gross + settlement share the dividend's day, corrections post at now); (6) dividend delete → min(dividend day, correction days); (7) custom-asset value points / smoothing toggles / asset deletion fan out to **every portfolio transacting the asset** (fixing the pre-V5 gap that only invalidated the default portfolio), each from max(anchor day, that portfolio's first transaction day on the asset) — a point before the first transaction still reshapes the close carried into it. The anchor: for a value-point edit with smoothing OFF it is the earliest changed mark's day (step/carry-forward reshapes only forward); with smoothing ON an edited/added/removed mark also linearly reshapes every interpolated day back to the **preceding surviving mark**, so the anchor moves to that mark's day + 1 (mark days themselves never move — interpolation endpoints are exact by construction; no preceding mark ⇒ the changed day); smoothing toggles and asset deletion reshape the whole series, i.e. anchor = the first-transaction floor; for the destructive delete the references are resolved BEFORE the cascade and invalidated AFTER commit so an eager recompute can never persist pre-delete data and be trusted; (8) portfolio deletion → rows + state die by FK cascade; (9) broker-import apply routes through the portfolio/tax services and inherits rules 1/4/5. **Writer discipline:** refills insert-missing-only (`ON CONFLICT DO NOTHING`) so clean earlier days keep their bytes; only the nightly `snapshots.backfill` roll (03:30 Vienna, after `prices.refreshDaily`; doubles as the first-run backfill of all portfolios) overwrites a trailing 35-day heal window, because the nightly price refresh can revise stored closes — older revisions surface only via explicit invalidation. Persisting runs under the state row's lock with an `updated_at` compare-and-set, so a computation raced by a concurrent invalidation is discarded, never trusted. **The "today" point is never persisted**: it is computed fresh per read from live quotes (fallback: last stored close — the engine's carry-forward without a provider history call), so a quote change reflects with zero snapshot writes. Per-asset **overlays stay computed** from the §5.3-cached provider price series — they are price curves, already durable in `price_history`, not value-engine output. | Planner-defined invalidation rules for #553 (V5-P1 arc a), logged per the issue's binding instruction; correctness core of the perf milestone |
| 2026-07-17 | **V5-P4a German tax — rules researched + fixture-encoded BEFORE the engine (issue #576; the engine follow-up implements against `apps/api/src/domain/__tests__/deTaxFixtures.ts`):** DE joins `country_specific` as country #2. **Researched rules (statute-referenced):** (R1) flat 25 % Abgeltungsteuer on capital income (§32d Abs. 1 EStG) plus Solidaritätszuschlag of 5.5 % **of the tax** (§3 Abs. 1 Nr. 5 + §4 SolzG 1995) — 26.375 % effective; NO Soli-Freigrenze at withholding (that exists only in assessment); (R2) cost basis is **FIFO per lot** for securities in collective custody (§20 Abs. 4 Satz 7 EStG); buy fees capitalize into the lot, sell fees reduce proceeds (§20 Abs. 4 Satz 1 — the app's existing fee rule is law-conform for DE, unlike AT where it was a logged simplification vs §27a öEStG); (R3) **Sparer-Pauschbetrag €1,000 per year** (§20 Abs. 9 EStG, since VZ 2023), applied AFTER loss offset, floored at zero, unused remainder does NOT carry; (R4) **two loss pots** (§20 Abs. 6 EStG; bank mechanics §43a Abs. 3 EStG): the Aktien pot holds share-sale losses and offsets ONLY share-sale gains (§20 Abs. 6 Satz 4); the Sonstige (allgemeiner) pot holds every other loss and offsets ALL positive capital income incl. share-sale gains and dividends; dividends are always Sonstige-side income (not Veräußerungsgewinne — the ring-fence never reaches them); offset order per bank practice: same-category pot → general pot → allowance; (R5) pots **carry forward across years indefinitely** (§20 Abs. 6 Sätze 2–3); the Verlustbescheinigung opt-out (§43a Abs. 3 Satz 4) is not modeled; (R6) **intra-year refunds are mandatory**: the paying agent nets later negative income against the year's already-taxed income and refunds the excess withholding (§43a Abs. 3 Satz 2). **Mapping onto the AT points (1)–(6) of 2026-07-08:** (1) inherited unchanged — per-user mode+country, tax frozen at record time, forward-only mode switches; (2) inherited — year buckets by trade date in Europe/Vienna (wall-clock identical to Europe/Berlin; one bucketing zone app-wide, chosen simplification); pots AND allowance are per **portfolio** (a portfolio = one depot; the allowance models that depot's Freistellungsauftrag, default the full €1,000 — keeping several DE portfolios inside the legal per-person total is the user's job, exactly as with real banks); (3) inherited unchanged — append-only settlement steering held tax to the year-to-date target, re-derivation + unattached correction movements on history re-shaping; R6 refunds fall out of the same delta mechanics; (4) **differs** — the tax base keeps the app's EUR-at-each-leg's-trade-date-FX realized-P/L definition and fee rule, but the cost-basis strategy becomes **pluggable**: rows taxed under DE realize against FIFO lots (per-unit lot basis incl. pro-rated buy fee), AT keeps the moving average; the DE tax report's per-sell P/L IS the FIFO number (taxed gain = reported gain within tax surfaces), while general portfolio surfaces keep the app-wide moving-average convention (`holdings.ts`) — a deliberate narrowing of AT's "one truth" to the tax report, since one display P/L cannot serve two basis methods; (5) inherited unchanged — financial immutability of tax-carrying rows, delete-and-re-add as the correction path; (6) inherited unchanged — dividend/withholding/refund movements stay TWR-internal. **DE year-target function** (the analog of `atYearTargetEur`, encoded + shape-tested in the fixtures): aktienRemainder = Σ Aktien-sale P/L − aktienPotIn; sonstigeRemainder = Σ dividends + Σ other-sale P/L − sonstigePotIn; a negative Sonstige remainder additionally offsets a positive Aktien remainder (never vice versa); taxableBeforeAllowance = remaining positives; allowanceUsed = min(1,000, that); KapESt = floorCents(25 % · base); Soli = floorCents(5.5 % · KapESt) (§4 Satz 2 SolzG "Bruchteile eines Cents bleiben außer Ansatz"; the KapESt cent-floor is the app's #370 floor-toward-zero policy — the statute fixes no cent rule at withholding); negative remainders leave as potOut; KapESt+Soli post as ONE combined withholding/refund movement per settlement delta (the report derives the split). **Pot classification:** app asset type `stock` → Aktien; everything else (ETF/fund/custom/crypto) → Sonstige; the §23 EStG crypto regime (tax-free after 1 year) is NOT modeled — DE mode taxes exactly what the app taxes. **Explicitly excluded** (noted per issue scope): Kirchensteuer, Vorabpauschale (§18 InvStG), Teilfreistellung (§20 InvStG), Günstigerprüfung/assessment options (§32d Abs. 4/6), foreign-withholding credit (§32d Abs. 5), pre-2009 Altbestand. **DE ↔ AT difference table (compact):** rate — AT 27.5 % KESt ↔ DE 25 % + 5.5 % Soli-on-tax (26.375 % eff.); cost basis — AT moving average ↔ DE FIFO lots (pluggable strategy); fees — same app rule (AT: simplification, DE: law-conform); loss offset — AT one per-year pool ↔ DE two ring-fenced pots with one-directional cross-offset; carry — AT hard Jan-1 reset ↔ DE pots carry forward while the allowance does not; allowance — AT none ↔ DE €1,000/yr after losses; intra-year refund — both; year zone — both Europe/Vienna; scoping — both per portfolio, mode per user. Fixture set: 8 scenarios (simple gain; FIFO≠moving-average multi-lot stating both numbers; mid-year allowance exhaustion; Aktien-loss ring-fence; Sonstige cross-offset with mid-year refund; KapESt+Soli floor demo; year-boundary pot-carry with allowance reset; refund-of-already-withheld) with step-by-step hand computations + a shape/consistency test; `domain/tax.ts` untouched. | Factory research record for V5-P4 arc (a) (issue #576) — the mandated fixtures-BEFORE-implementation step, granularity per the 2026-07-08 AT precedent; the DE engine follow-up must make these fixtures pass unchanged |
| 2026-07-18 | **V5-P3 quiet-hours urgent-bypass class (issue #579 — the §16-logged planner decision).** Quiet hours (per-user window + IANA timezone, off by default) defer the **outbound channels only** (email / phone push / browser push) — the in-app bell is the durable record and is never deferred. A non-urgent outbound notification fired inside the window is queued (the #575 digest queue extended with a `deliver_after` column) and delivered INDIVIDUALLY at window end by the `notifications.deferredDelivery` job (every 60 s; idempotent via the same claim-and-stamp barrier as the digest, restart-safe because pending rows live in the DB). Overnight windows (start > end, e.g. 22:00→07:00) are fully supported; digest boundaries and window math compute in the user's timezone (a daily digest buckets by the user's LOCAL day). A digest whose delivery moment lands inside the window is itself re-queued as a per-channel deferred summary. **The urgent-bypass class — delivered instantly even inside the window — is EXACTLY: (1) every type in `ACCOUNT_SECURITY_NOTIFICATION_TYPES` (`account.invite`, `account.temp_password`, `account.data_export`), and (2) admin announcements of `critical` severity. Nothing else bypasses — price alerts explicitly do NOT** (not being woken by market noise is the whole point of quiet hours). Encoded once as `isUrgentNotification({ type, announcementSeverity })` in `packages/contracts/src/notifications.ts`; the dispatcher gates on the type (announcements are in-app-only fan-outs, so they never reach the quiet-hours-gated outbound path and are urgent-by-construction). Deferred emails carry the already-rendered (English) title/body in localized chrome — consistent with how the #575 digest already presents deferred item content. | Planner-defined urgent-bypass class for #579 (V5-P3), logged verbatim per the issue's binding instruction |
| 2026-07-19 | **V4-P4 passkeys / WebAuthn (issue #479) — the passkey portion of the V4-P4 arc (b) auth work; carried forward and shipped in v5.** Factory implementation decisions: (1) **A user-verified passkey login is strong auth and raises NO follow-up 2FA challenge (§16 decision).** Registration and login both set `userVerification: 'required'` and verify with `requireUserVerification: true`, so every stored passkey carries a second factor (possession + biometric/PIN) intrinsically — a verified assertion is already multi-factor, and re-prompting for TOTP would be security theatre. This is why passkey login mints a session directly (`authService`-style: `sessions.create` + `setLastLogin` + a `login.success` audit with `meta.via = 'passkey'`) instead of routing through the password login's 2FA gate; a passkey-holding account with 2FA armed still signs in with one gesture. (2) **Login UX = discoverable credentials (usernameless).** `POST /auth/passkeys/login/options` mints authentication options with an empty `allowCredentials`; the authenticator offers its resident keys and the owning account is resolved from the returned credential id (`residentKey: 'preferred'`). No email-first step — the modern passkey flow — and it keeps the public login endpoint from being an account-existence oracle. (3) **Challenges are single-use with a short (5 min) Redis TTL:** registration keyed per user (`passkey_reg_chal:<userId>`), login by an opaque handle (`passkey_login_chal:<challengeId>`) returned to the client; both are `DEL`'d on presentation, so a replayed or expired challenge fails closed (`PASSKEY_CHALLENGE_INVALID`). (4) **Clone detection:** a non-increasing signature counter rejects the login (`PASSKEY_COUNTER_REGRESSION`) and writes a `passkey.login_fail` audit — an explicit service-level check layered on `@simplewebauthn`'s own guard. (5) **Add + delete are re-auth-gated** (fresh password, or a TOTP/recovery factor) on their own per-account progressive throttle (`account_passkey_account`), mirroring the account-deletion/export re-auth so the endpoints are never a lighter brute-force oracle; rename + list ride the session alone; deleting the last passkey is allowed (password login always remains) with warning copy. (6) **rpID/origin derive from the web origin** (`config.webauthn`, new but env-var-free — the topology already pins origins). (7) Management is user-kind session only (`requireUser`), alongside 2FA; admin-app passkeys and passkey-as-second-factor stay out of scope. Deps: `@simplewebauthn/server` (api) + `@simplewebauthn/browser` (web); the server engine is injected for fixture-driven tests (no real authenticator/browser/crypto). | Factory implementation decisions for #479, logged per the issue's binding "§16 Decision Log entry: passkey-login-satisfies-2FA decision, chosen login UX" acceptance item |
| 2026-07-18 | **V5-P7 MIRRORCHAIN design note (issue #578) — the §13.5 design-note-first deliverable; FULL design in `docs/mirrorchain-design.md` (binding).** Headline decisions: (1) **N real portfolio copies + additive chain tables** (`mirror_chains` / `mirror_chain_members` / `mirror_chain_invites` / `mirror_chain_ops` / `mirror_rows`), never one shared row — each copy is an ordinary portfolio so holdings/TWR/snapshots/tax/sharing work per copy unchanged and the kick-fork is pre-materialized; zero existing-table changes. (2) **Replication = op-based, job-driven, through the existing services** (never multi-copy SQL transactions, never row copies): every write to a synced copy appends a full-state op to a per-chain oplog (`last_seq` `UPDATE…RETURNING` row lock = the linearization point, membership/role checked under the same lock), applies origin-first in strict seq order, then a per-chain BullMQ job applies ops to every other copy through THAT member's services — cash legs and tax derive per copy, replicas run force-mode (overdraw/archive guards waived; holdings-based guards agree by construction), tax-immutable rows edit via the sanctioned delete-and-re-add correction path; idempotent per-op apply + per-copy watermarks; stalls dead-letter to the Problems page and never skip/reorder. Chain creation synthesizes **genesis ops** from a converted portfolio's history so a join is a plain oplog replay — one code path, no snapshot/reference-copy logic. Replicated rows are attributed via `mirror_rows` (+ an `audit_log` row per applied op per copy — the per-copy audit criterion) and tagged **`sync:mirrorchain`** (origin copy keeps its real write-path tag). (3) **Convergence = total order + full-state last-writer-wins** (deletes terminal), fronted by an optimistic **`baseSeq` guard** (`409 MIRROR_CONFLICT` on stale edits) so member edits are never silently clobbered; worked example in the note. (4) **Invites friends-only** (checked at send and accept), owner+managers send; the friction-ladder acknowledgment IS the accept screen; join is zero-config (auto copy, auto Main mapping, syncing progress); member cap 16 (env-tunable). (5) **Authority matrix:** ledger writes = every member (one shared book, any member may edit any row); invite/kick-member = owner+managers (autonomous, owner-offline irrelevant); kick-manager/grant-revoke-manage/transfer/dissolve = owner only; transfer demotes the old owner to plain member. (6) **Kick/leave** flips the membership tombstone under the chain lock and freezes the copy at its watermark — everything stays (rows, attribution, audit, tags), replication/member-list/oplog access sever; re-invite creates a new copy, forks never re-merge. (7) **Owner-deletion succession DECIDED: transfer-on-delete to the oldest active manager (earliest `joined_at`, deterministic tie-break); no manager ⇒ the chain dissolves into forks** — managers accepted stewardship, plain members never did, so no surprise promotions and no zombie chains; same rule for owner-leave and owner copy-deletion; runs synchronously in the V4-P2c pipeline before the user-row delete (SET NULL + denormalized usernames keep history), with an ownerless-chain repair sweep as defense-in-depth. (8) **Cash sources are chain entities** mapped per copy (chain Main ⇔ each copy's Main); balances may legitimately skew by copy-local tax movements; set-balance replicates as the origin-computed delta so TWR flows stay identical. (9) **Per-copy independence:** replicated rows freeze THAT copy's user's tax mode at apply time (V3-P4 forward-only rule); tax rows never replicate and are never visible cross-member. (10) **Privacy:** members see the member list + attribution + activity feed and nothing else of each other; sharing a synced copy strips co-member attribution for non-member viewers. (11) UX = one avatar-stack affordance on the portfolio header (anti-bloat); mirrorchain is **absent by design for paranoid accounts**. (12) Build order M1 schema → M2 replication core (diff:max) → M3 membership/invites → M4 succession → M5 UI → M6 e2e | Factory design decisions for V5-P7 (issue #578), logged per §13.5's binding "design note §16-merged BEFORE code" rule; implementation issues are composed from the note's §12 |
| 2026-07-21 | **Per-portfolio settings scoping framework (issue #636) — first consumer: tax country/mode.** Decisions: (1) **Resolution is a live cascade `effective(setting, portfolio) = portfolio override ?? user default ?? system default`**, encoded as the pure `domain/settingsScope.ts#resolvePortfolioSetting` (returns the value + the layer that supplied it, powering the inheriting/overridden UI). (2) **LINK semantics, NOT create-time snapshot.** A portfolio with no override tracks the user's **live** default — creating a portfolio seeds no override, and later changing a default **does** retro-affect every portfolio that never overrode; reset-to-default simply drops the override back to inheriting. Chosen over the owner's "seed at create-time, no retro-change" suggestion because it is the model the stated resolution formula literally describes (a live `user default` layer), gives a real reset affordance, and avoids stale create-time copies silently diverging. It stays compatible with the tax engine's forward-only freeze (§16 2026-07-08): the *effective* mode is resolved per portfolio and frozen onto each sell/dividend at recording time, so a later default change never rewrites already-recorded tax. (3) **Storage** = a generic `portfolio_settings(portfolio_id, key, value jsonb, PK(portfolio_id,key))` override table (any scopeable setting opts in by key, no migration; first key `tax` → `{ mode, country }`, validated at the service edge through the tax contract) **+** the existing typed `user_tax_settings` kept as tax's user-default home (no data migration, exact pre-#636 behaviour for portfolios with no override). (4) **Surfaces:** Settings → **"New portfolio defaults"** edits the user default (the former Settings → Taxes page, reframed); each portfolio's Tax report carries the override / reset-to-default control and gates the report on the portfolio's effective mode; `GET/PUT/DELETE /portfolios/:id/settings/tax` resolve / override / reset. Other candidates (base currency, DRIP #613, analytics defaults) adopt the same framework later. | Factory implementation decisions for #636, logged per the issue's binding "Log the scoping/resolution model in PROJECTPLAN.md §16" |
| 2026-07-21 | **V5-P4c tax v2 (issue #584) — custom rule-built mode + manual default; the mandated DE-expressibility resolution.** (1) **DE stays a documented APPROXIMATION under the custom engine — the parameter set is NOT expanded.** The spec's six parameters (`ratePct`, `lossOffset`, `refund`, `yearReset`, `carryForward`, `costBasis`) are kept exactly; DE's core rate is expressible as an effective **26.375 % = 25 % × 1.055** with `{lossOffset, refund, yearReset, carryForward, fifo}`, but the §20 Abs. 6 dual loss pots (Aktien ring-fence) and the Sparer-Pauschbetrag are structurally outside that list and remain **DE-preset-only** (`country_specific`/DE keeps the exact #576 engine). Chosen over adding allowance/split-pot/surcharge parameters: the extra parameters would exist for exactly one country that already has a first-class preset, and every added switch multiplies the tested regime matrix — anti-bloat applies to parameter surfaces too. AT IS exactly expressible (`{27.5 %, offset, refund, reset, no carry, moving-average}`) and the custom engine reproduces the AT fixtures fixture-for-fixture (pinned by test, `AT_AS_CUSTOM_PARAMS`). (2) **Parameter semantics** (encoded in `domain/tax.ts#settleCustomYear`, the parameterized generalization of the AT delta-steering): `yearReset` ON gives per-year pools (with `carryForward` ON chaining a year-end net-loss pot DE-style; OFF = the AT hard reset); `yearReset` OFF runs ONE cumulative pool across all years whose per-year held component = cumulative target − tax attributed to prior years (so a later-year loss refunds prior years' tax); in that regime `carryForward` OFF additionally forfeits a NEGATIVE cumulative balance at each Jan 1 (positive balances always carry — they are already-taxed income). `lossOffset` OFF drops losses entirely (no refund, no pot accrual); `refund` OFF turns held tax into a ratchet over taxable EVENTS, making the year target path-dependent — frozen events therefore replay chronologically; history-reshape reconciliation (backdated buy, delete) is EXEMPT from the ratchet: a reshape is a data correction, not an economic refund, so settlement steers held exactly onto the replay target (signed — it may claw back). This keeps a year's held tax equal to the sum of every regime's replay target on EVERY path (custom write, delete, cross-regime) and therefore derivable append-only; clamping would strand an excess no row represents, which the delete paths and coexisting regimes would then absorb as mislabeled refunds. (3) **A parameter change is a mode switch** (§16 2026-07-08 forward-only rule): the full parameter set freezes onto every custom-taxed row (`tax_params` jsonb on transactions/dividends), and each DISTINCT frozen set settles as its own independent regime — a portfolio-year's held tax is the SUM of the AT pool target + DE year target + one component per frozen custom parameter group, each engine steering only its own component (the AT/DE coexistence frame of #580 generalized, `services/tax/customState.ts`). Delete/re-add reconciliation uses the combined target with corrections noted "Tax year correction (custom rules)". (4) **Manual default** = amount-or-% stored in `user_tax_settings` (`manual_default_amount_eur` XOR `manual_default_rate_pct`, manual mode only, DB-CHECKed), applied server-side wherever a MANUALLY recorded sell/dividend arrives without an explicit entry (an explicit entry — including 0 — always wins; `import:<broker>`/`sync:*`-sourced rows never receive the default — imported broker history settled its taxes at the broker) and prefilled client-side into the TransactionDialog tax field (pristine fields only; a user-touched field is never overwritten). Blank default = byte-identical pre-V5-P4 behavior. Dividends have no recording dialog in the web app, so their prefill is the server-side application alone. | Factory implementation decisions for #584 (V5-P4 arc c), logged per the issue's binding "DE-expressibility resolution §16-logged (expanded parameters OR documented approximation)" |
| 2026-07-21 | **V5-P13 PARANOID MODE design note (issue #651) — the §13.5 design-note-first deliverable; FULL design in `docs/paranoid-design.md` (binding); implementation is NOT composed until the owner acks (the spec's "§16-logged and owner-acked BEFORE any code" — an `awaiting-owner` gate issue is filed).** Headline decisions: (1) **One client-encrypted vault per user** — the client decrypts locally and does ALL money math itself with the SAME audited domain code the server uses: `holdings`/`cashLedger`/`tax`/`seriesStats`/`settingsScope` are physically extracted to `packages/domain` (`@bettertrack/domain`, api keeps re-export shims) and money math is never reimplemented client-side; parity is by construction, no client clone of the #553 snapshot machinery (on-device recompute + local cache at personal-finance scale). (2) **Vault boundary is mechanical, not per-feature:** a `PARANOID_TABLE_CLASSIFICATION` (`vault`|`server`) over `schemaTableNames` in the export-manifest style with a completeness test — the enable purge sweep, the zero-cleartext probe test and disable rehydration all iterate the `vault` set, so future tables (incl. V5-P9 expenses) cannot silently leak. (3) **Blob format `BTVAULT1`:** cleartext JSON header (crypto params, wrapped keys, monotonic `vaultVersion` = the CAS token, device/write ids — zero portfolio info) + AES-256-GCM payload with the header bound as AAD (tamper ⇒ decrypt failure); payload = zod-versioned vault document (per-entity uuidv7 + `rev` + `editedAt` + ≥180-day tombstones), deflated; 16 MiB cap; newer-version blobs open read-only ("update the app"), never best-effort parsed. (4) **Keys:** random 256-bit VK wrapped by an Argon2id-derived KEK from a NEW vault passphrase (deliberately distinct from the login password, which transits to the server); wrapped-VK rides in every blob header so any blob + passphrase recovers on a fresh device; forced recovery-kit download (raw VK); passphrase change = re-wrap only; **lost key ⇒ lost data, by design — no escrow, no reset, no support path; the only server-side answer is destructive "start fresh"**; the enable flow carries the strong-rung acknowledgment. (5) **Sync = blind blob stores with CAS:** server `GET/PUT /api/v1/vault` with `If-Match: vaultVersion` (412 on miss) + bounded ciphertext history (10 versions/30 days) as the corruption net; Drive approximates CAS via `appProperties`+`headRevisionId`; primary = server when present, else Drive; conflicts merge at ENTITY granularity (higher `rev` → later `editedAt` → deviceId tiebreak, total determinism; edit beats tombstone — resurrected beats silently-vanished money data; never field merges, the mirrorchain §3 rationale). (6) **Media set** ⊆ {server, drive}, non-empty, switchable via **migrate-then-drop with a verified round trip before anything destructive**; removing `server` hard-deletes blob + history in the PATCH transaction — the Drive-only zero-bytes moment. (7) **Drive medium:** `drive.appdata` scope ONLY, and the OAuth flow is ENTIRELY client-side (GIS token client, SPA client id env var) — the BetterTrack server never holds tokens, file ids or proxy endpoints, so in Drive-only mode it has zero portfolio bytes AND zero capability to fetch them; per-user consent is end-user OAuth, not owner setup. (8) **Enable** = client builds+writes+round-trip-verifies the vault, THEN the server transactionally flips `users.privacy_mode`, runs the purge sweep (V4-P2c sweep pattern over the `vault` classification), revokes all shares/audiences/follows, deletes authored comments/reactions, forces the public profile off; preconditions: no active mirrorchain membership (leave-with-fork first; mutual exclusion recorded on both sides), no in-flight imports/exports. **Disable** = unlocked-vault rehydration through the normal services (tax/snapshots re-derive server-side; vault-derived rows are not trusted), flag flips only after commit. (9) **Exact feature-kill list** (server 403 `PARANOID_MODE` from ONE registry + surfaces absent client-side, matrix test = registry × routes): public profile; ALL sharing both directions incl. share links + audiences; comments/reactions; mirrorchain; every server-computed portfolio read (series/analytics/live frames/projected dividend income/portfolio news digest/real-portfolio backtest/AI insights/server tax engine+export); broker+bank CSV imports (client-side import = v6 candidate, not promised); portfolio/tax/import API scopes + portfolio webhook events; standing-order server execution (definitions live in the vault, the client materializes due rows with deterministic uuidv5(orderId,dueDate) ids so multi-device catch-up dedupes); portfolio jobs + cleartext offsite backup. **Kept:** full auth stack, friendships+chat (carry no portfolio data — deliberate reading of "social/sharing surfaces", owner may veto at ack), price alerts (all six kinds are pure asset-price predicates — the evaluator runs untouched; any FUTURE portfolio-conditioned alert kind is client-evaluated), watchlists/conglomerates/ideas as private hypothetical baskets, asset-level market intel, calculators, Forecast, discreet mode. (10) **Autonomy-prep seams, architecture-binding for every v5+ client feature:** `PortfolioStore` (api-backed + vault-backed impls), `DataHome` (server/drive/local-cache blob adapters), `MarketDataSource` (v5 impl = BetterTrack API; Yahoo-direct = interface-only) — Drive-only paranoid already runs with the server reduced to auth + market data. (11) **Interplay:** account export ships ciphertext (+ a client-built cleartext export from the decrypted vault); account deletion also kills blob+history, Drive file best-effort client-deleted else remains the user's own encrypted property; admin sees mode/media/blob metadata only — no reset, no recovery, no portfolio numbers. (12) **UX:** 4-step enable wizard (default medium = server; Drive-only behind one advanced disclosure), PIN-gate-style unlock, ONE header sync chip, killed surfaces absent not greyed (anti-bloat), EN+DE. (13) Build order PD1 domain extraction ∥ PD2 vault store → PD3 enable/kill matrix → PD4 crypto core → PD5 sync engine + store seam (both diff:max keystones) → PD6 Drive → PD7 client valuation/tax engine (diff:max, money) → PD8 UX → PD9 e2e. | Factory design decisions for V5-P13 arc (b) (issue #651), logged per §13.5's binding "design note §16-logged and owner-acked BEFORE any code" rule; composition of the §14 implementation issues waits on the owner-ack gate issue |
| 2026-07-21 | **#635 tax engine rebuild — LIVE open-year derivation, closed-year lock boundary, FI country module.** Root cause of the owner's "2026 shows €0 net tax" (confirmed): tax facts froze onto each row at recording time (`taxMode` stamped from the setting live at entry, §16 2026-07-08), so sells recorded while the setting was `none` (or before the engine existed, `taxMode` NULL) could never enter the AT pool — realized P/L showed, tax stayed €0, and switching the setting back never re-taxed them. **This row SUPERSEDES the 2026-07-08 "mode switches apply forward only" rule for open years.** The new model: (1) **Boundary** — Vienna years ≥ the current year are OPEN; years before are CLOSED/locked. Open years re-derive IN FULL under the portfolio's CURRENT effective settings (a continuously-derived withholding balance): every derivable row (sell/dividend not frozen `manual_per_trade`; NULL/`none`/other-regime rows included) enters the active regime's settlement, on every write path and every report read; drift posts as unattached correction movements (append-only, never edits). Closed years keep recording-time semantics exactly — frozen AT/DE/FI/custom components coexist and sum to the year's held target, backdated mutations still settle into them append-only, but a settings switch never wholesale re-taxes them (a year locks in whatever settled state it reached at rollover). The lock is implemented as **ΔF/baseline settlement** (#656 review round 2): a closed-year mutation computes the year's residue `held − Σ standalone frozen targets` on the PRE-mutation state and settles onto `Σ standalone frozen targets (post-mutation) + residue` — held shifts by exactly the change in the frozen decomposition, so the open era's state survives whether it lives in unattached live corrections OR in attached joint-pool marginals that don't decompose into standalone components (DE allowance sharing, the FI €30,000 threshold, chained custom carry). Live open-year corrections still carry dedicated note strings ("Live tax correction (…)" / the tax-tracking-off note), now purely for cash-history clarity — the residue is derived from state, not markers. On data whose held equals its decomposition (all pre-#635 history) the residue is 0 and closed-year behavior is unchanged; the same lock covers the no-rollover reachability (switching to `manual_per_trade` makes the current year closed-machinery immediately). The ΔF settlement runs on EVERY mutation path regardless of the recording mode (#656 round 4): batch writes under `none`/`manual_per_trade` settle the closed-machinery years their (back)dated trades reshape exactly like the delete path does (`manual` treats every year as closed-machinery; `none` settles closed years only, its open years re-derive on the next read), so held can never silently drift from the decomposition and be misread later as locked open-era state. (2) **Regime meanings on open years** — country/custom: full re-derivation (incl. re-taxing `none`-frozen rows — the bug fix); `none`: engine target €0, previously-withheld engine tax refunds back out (symmetric, reversible); `manual_per_trade`: no derivation anywhere (manual amounts are user-stated facts, never engine-refunded — pre-existing doctrine kept). (3) **Self-healing** — report reads reconcile open years (posting corrections); per-portfolio tax-setting writes reconcile immediately; the user-level default heals on the next read of inheriting portfolios. A withholding correction that would overdraw a cash source is deferred (never breaks the no-negative ledger invariant from a read path) and retried on the next read; the summary's open-year `taxNetEur` reports the derived target regardless, and the new optional `locked: true` flag marks closed years on the wire. DE carry pots (and custom-group carry) chain from the closed years' FROZEN events into the open-year derivation. (4) **FI module** — third `country_specific` country: 30 % capital-income tax to €30,000/year, 34 % above (TVL 124 §), FIFO cost basis, within-year loss offset with refund, hard Jan-1 reset (loss carry-forward is a documented v1 simplification, extensible like the DE pots); shares the pool-style settlement core with AT (`settlePoolYear`). The €30,000 bracket applies **per portfolio-year** — the whole report is portfolio-scoped (the issue's mandate; the DE Sparer-Pauschbetrag shares the scope), so the figure is a per-depot estimate, not a taxpayer-level filing number where real pääomatulovero brackets aggregate across accounts (stated in the picker description). No migration needed: `country` is an unconstrained `char(2)`, the contracts enum gates values. Adding a country = domain settle fn + openYear.ts branch + countryState frozen component + contracts enum + picker/i18n. | Factory implementation decisions for #635, logged per the issue's binding "log the rebuild + the new multi-country/live model as a §16 decision" + "decide + document the closed-year boundary in §16" acceptance items |
| 2026-07-22 | **#635 owner decision — Option A confirmed; tax = overview, NOT a filing instrument.** The closed-year fork from PR #656's triage was put to the owner (Option A: years always self-adjust — a backdated/edited/deleted transaction touching a closed year still adjusts that year's net tax append-only (the ΔF/baseline lock above); nothing ever hard-locks after rollover. Option B: truly-immutable filed years + current-year prior-period adjustments). **Owner chose A** (issue comment 2026-07-22): the tax mode is *an overview of taxes paid, not a filing instrument* — there is no correctness guarantee and the user is responsible for anything they file, so the "a filed number must never move" premise behind B does not apply. A government-filing-grade mode is a far-future maybe, recorded as a **non-goal — do not design for it now**. Consequences: (1) #656 stands as implemented. (2) Owner-mandated disclaimer (binding liability framing, not optional copy) on the Tax Report page and the tax settings surface, EN + DE: "Estimates for your personal overview only — not tax advice, no guarantee of correctness, not a filing document." (`settings.taxes.disclaimer`, rendered via the shared `Disclaimer` footnote on `TaxReportPage` + `/settings/taxes`). (3) Compensating control for the exhausted review rounds: when #627 (deepened tax tests) re-arms it MUST cover backdate/edit/delete-into-rolled-over-year cases under AT/DE/FI coupling, incl. the DE €1,000-allowance joint-pool marginal case from #656 review round 2. | Owner decision on #635 (needs-human round-trip); supersedes nothing — it confirms the boundary chosen in the row above and adds the disclaimer mandate |
| 2026-07-22 | **V5-P7 M2 (#644 / PR #673) — two §2 amendments to `docs/mirrorchain-design.md` (binding text updated in the same PR).** (1) **Submit-path order swap:** the origin service apply runs BEFORE the op append — §1's rule that `mirror_id` IS the origin row's local id means the origin service call mints it, so the note's original append-first order was mechanically impossible; the per-chain lock keeps origin apply order = seq order and the append keeps the in-transaction guards. Cost: a crash between origin commit and append leaves an origin-only mirror-linked row with no op — detectable by the §2 repair-sweep query (a); the M4 repair sweep must wire it. (2) **Replicate-job serialization moves out of BullMQ job ids:** a fixed per-chain `jobId` is silently swallowed by BullMQ once a completed/failed job is retained under it (replication would halt after the first run; one dead-letter would block the whole chain), so producers enqueue plainly and `replicateChain` replays each copy under the SAME token-fenced, TTL-renewed per-chain Redis lock the submit path holds — exactly one applier per copy, redundant jobs no-op off the watermark. Also §16-logged here: the tax-immutable correction path heals its delete-then-crash window at re-delivery (re-create from the full-state payload + re-point); the narrower re-create-before-re-point residual is documented as sweep (b) | Factory, PR #673 review (blocking findings 1–2 + nits); design-note deviations logged per project rule |

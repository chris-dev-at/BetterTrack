node_modules/
dist/
coverage/
*.tsbuildinfo

# env
.env
.env.*
!.env.example
!.env.production.example

# logs / os
*.log
.DS_Store

# tooling caches
.vite/
.eslintcache

# Playwright e2e
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/

# factory knowledge pack — generated outputs (generator is committed; run pnpm knowledge:build)
factory/knowledge/graph.json
factory/knowledge/MAP.md

# factory usage ledger — per-issue token/cost data (code is committed; data is not)
factory/usage/*.jsonl
factory/usage/*.json

# multi-factory runtime state — protocol files, never committed
multi-factory/state/
compose.extra.yml
